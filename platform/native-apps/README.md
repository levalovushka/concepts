# Приложения концептов

Нативный концепт начинается с `concept.json`, а продуктовый UI пишется
на SwiftUI вручную. HTML-прототип может служить референсом для старого
концепта, но не является входом или шаблоном нативного pipeline.

```bash
npm run app -- <slug>       # Xcode-проект: Info.plist, entitlements, pbxproj
npm run app:run -- <slug>   # собрать, поставить в симулятор, запустить
```

Нужен macOS с Xcode 26 и симулятором `iPhone 17 Pro` (другой — через `CAMO_DEVICE`).

## Порядок работы

Читать **до** того, как писать код:

0. `concepts/<slug>/concept.json` — источник продуктовых фактов;
1. `dist/<slug>/ux-spec.json` — собранная спецификация: модель мира,
   граф навигации, разметка действий, доступы с жестами и fallback, состояния
   экранов, токены и строки. Собирается командой `npm run spec -- <slug>`.
   Секция `gaps` честно перечисляет, чего в спецификации нет;
2. `docs/01-product-vision.md`, если он есть, — что здесь продукт;
3. `docs/03-design-system.md`, если он есть, — направление и антипаттерны;
   у «Двора» там прямо написано: без large title, без segmented control, без
   капсульных кнопок, без разделителей внутри карточки;
4. Скриншоты предыдущего прогона, если они есть, — материал для
   визуальной итерации, а не шаблон раскладки.

Писать в `native-apps/<slug>/Sources/`. Дальше цикл: собрать → снять экран →
сравнить с эталонным PNG → починить.

Ветку отказа и ветку разрешения проходить launch-аргументами: системный промпт
живёт в SpringBoard и не нажимается инъекцией тапа.

```bash
npm run app:run -- <slug> -deny:location
npm run app:run -- <slug> -grant:camera,mic
```

У `dvor` есть детерминированный preview-вход для визуальной приёмки root-экранов:

```bash
npm run app:run -- dvor -capture:signed-in -capture:tab=home
# tab: home | chats | yard | menu
```

## Модель мира идёт первой

`product.world` в `concept.json` описывает, из чего состоит продукт и что с
этим можно делать: сущности с полями и связями, действия с эффектом и
наблюдаемым результатом. **Каждое действие несёт список доступов**, которые ему
нужны.

Из этого выводится главная проверка: доступ, не привязанный ни к одному
действию, — это ключ без фичи, ровно то, что заворачивает ревью App Store.
`npm run spec` называет такой ключ в `gaps`.

Модель мира заполняется до экранов, а не после: из неё выводятся и экраны, и
привязка доступов.

## Что не пишется руками

Только то, где нет решений, а есть точность:

- **Info.plist** — ключи из набора доступов, `alert.text` концепта становится
  usage description: той строкой, которую видит пользователь в системном промпте
  и читает ревьюер App Store;
- **`.entitlements`**, фоновые режимы, `BGTaskSchedulerPermittedIdentifiers`;
- **Xcode-проект** и схема;
- **`Fixtures.swift`** — демо-данные из секции `fixtures` концепта. Контент,
  скопированный в Swift руками, разъезжается с концептом на первой же правке,
  поэтому лента, чаты, счётчики и события живут в `concept.json` и попадают в
  приложение как есть.

Всё это выводится из `concept.json` командой `npm run app`. Опечатка в
`com.apple.developer.usernotifications.communication` — это не «некрасиво», это
молча несобирающийся бандл, поэтому такие вещи не переписывают вручную.

Ещё общий `native-runtime/AccessRuntime.swift` — реальные запросы доступов,
цепочки и журнал. В нём нет ни одного дизайнерского решения.

## Что пишется руками — всё остальное

Токены, компоненты, раскладки, навигация, композиция каждого экрана и его
содержимое.

Общего кита компонентов **нет намеренно**: у «Двора» плотный список на серой
странице, у «Такта» тёмный плеер, у «Птиц» полевой справочник. Кит навязал бы
всем язык одного концепта.

Генератора экранов тоже нет. Попытки вывести композицию из HTML или
начать каждый экран с универсальной заглушки якорили слабые решения.
Поэтому SwiftUI view сразу проектируется вокруг задачи конкретного продукта.

## HTML-витрина и native app

У части старых концептов есть **обвязка HTML-страницы**: журнал доступов,
матрица доступов, список прототипов, ссылки на доки. Это инструменты
презентации, они живут в `kernel/page.html`. Новое native app не переносит
HTML в SwiftUI: оно берёт продуктовые факты из `concept.json`, а UI проектируется
заново.

Проверка простая: показали бы вы это пользователю App Store? Технические
имена plist и матрица доступов остаются в документации. В UI попадает
только сама фича и её наблюдаемый результат.

Инструментарий, нужный для приёмки, уходит в консоль: рантайм печатает каждый
запрос строкой `[access] granted camera · NSCameraUsageDescription · problem`.

## Состояние

| Концепт | Состояние |
|---|---|
| `polka` | основной эталон: 9 экранов, 22/22 доступа |
| `dvor` | сделан: 4 вкладки, вход, 20/20 доступов |
