# Шеллак — дизайн-система

## Идея визуального языка

Версия v2 строится как нишевый музыкальный сервис, а не как музейный каталог. Из ВК Музыки взята грамматика: тёмная Главная, «Моя музыка», карточка релиза, постоянный mini-player, полноэкранный плеер и музыкальный профиль. Своими остаются матрица, сторона, шеллак, бумажная этикетка и сканер.

Базовые поверхности нейтрально-чёрные, чтобы архивные обложки были контентом, а не фоновой текстурой. Музыкальный акцент — фиолетовый `#a584ff`: он закреплён за главным действием и активным состоянием, но не используется как декоративная заливка соседних карточек. Тёплые цвета остались только в бумаге конвертов и сцене сканера; сам шеллак почти чёрный, с холодным бликом.

## Токены

| Токен | Значение |
|---|---|
| `--sm-accent` | `#a584ff` · главный жест, активный tab, фокус |
| `--sm-bg` / `--sm-surface` | `#0b0b0d` / `#19191d` |
| `--refused` | `#9c3f2e` — подписи fallback при отказе |
| `--face` | Inter 400 / 500 / 600 / 700 / 800 |
| `--mono` | Roboto Mono 400 / 500 — матричные номера, таймкоды, ключи |
| `--device-w` | 390px, высота 844 (iPhone 13/14) |
| `--safe-top` / `--safe-bottom` | 47px / 34px |
| `--r-card` / `--r-panel` / `--r-device` | 10 / 12 / 40 |

Радиусы, высоты строк, навбар, таб-бар, свитчи, сегменты, системный алерт и snackbar берутся из ядра (`kernel/base.css`), собранного по `components.html`. Своих аналогов системных контролов в концепте нет.

## Где моноширинный

Матричный номер — главные «данные» этого продукта: по нему сторона опознаётся однозначно. Он всегда моноширинный, с табличными цифрами:

- `.matrix` — номер в карточке и списке
- `.mono-chip` — номер или ключ в плашке (`shellac.app/want`, `PHPicker`)
- `.read-row span` — значения в таблице «прочитано с этикетки»
- `.lv-time`, `.pl-remain` — время и остаток стороны

## Поверхности

| Тип | Экраны | Статус-бар |
|---|---|---|
| Тёмная музыкальная | `archive`, `record`, `player`, `shelf`, `settings`, `copy`, `want`, `deeplink` | светлый |
| Тёмная task/state | `scanfound`, `scanfail`, `notfound` | светлый |
| Иммерсивная | `scan`, `background` | светлый |
| Системная | `pick` (PHPicker), `web` (Safari) | тёмный |

## Компоненты концепта

| Класс | Что это |
|---|---|
| `.sm-mix` · `.sm-mix-art` | Нейтральная карточка «Продолжить» с конвертом и отдельной play-кнопкой |
| `.sm-recognize` | Единственный залитый акцентом блок на Главной — распознавание пластинки |
| `.sm-mini` | Постоянный контекст воспроизведения над tab bar |
| `.sm-album-art` | Квадратный конверт в карточке релиза |
| `.side-badge` | Метка стороны A / B в списке |
| `.sm-player-*` · `.sm-vinyl` | Fullscreen-плеер: глянцевый диск, метаданные, прогресс, transport и доступы |
| `.sm-login-*` | Тёмный музыкальный вход и OTP без светлого системного шаблона |
| `.np-*` | Карточка Now Playing на локскрине |
| `.scanwin` · `.scan-corner` · `.scan-hint` · `.scan-read` | Сканер: рамка кадра, подсказка, панель «прочитано» |
| `.shutter` · `.shutter-side` · `.shutter-row` | Затвор и две боковые кнопки камеры |
| `.found-card` · `.found-art` | Карточка найденного совпадения |
| `.read-list` · `.read-row` | Таблица «поле → значение» для распознанного |
| `.sm-release-rail` · `.sm-library-list` | Релизы и коллекция в «Моей музыке» |
| `.copy-hero` · `.cond-note` | Экземпляр: фото конверта и записи о состоянии |
| `.pick-bar` · `.pick-grid` | Системный PHPicker |
| `.forum-*` | Страница форума в Safari |
| `.app-mark` · `.link-url` | Приложение, открытое по Universal Link |
| `.matrix` · `.mono-chip` | Матричный номер и плашка с ключом |
| `.art-*` | Носители фонов: диски `d1–d6`, конверты `s1–s3`, сцены |

## Медиа

Всё рисуется кодом — `concepts/shellac/media.mjs`. Стоковых фото в проекте нет, поэтому вопросов по правам не возникает. Псевдослучай детерминированный (seed), пересборка даёт побайтово тот же результат.

| Файл | Что это | Где используется |
|---|---|---|
| `d1–d6.svg` | Почти чёрный диск 400×400: канавки, пыль, микроцарапины, холодные блики, фактурная этикетка | Плеер, превью в списках, сетка PHPicker |
| `s1–s3.svg` | Бумажный конверт с вырезом под этикетку, кольцевой след диска | Полка, фото конверта |
| `label.svg` | Крупная этикетка 520×520 с потёртостями | Кадр сканера |
| `scene-table.svg` | Стол с пластинкой и конвертом, 390×844 | Превью камеры |
| `wide-table.svg` | Пластинка и конверт на столе, 390×260 | Широкие превью |

Раскладка этикетки повторяет настоящую: лейбл дугой сверху, название над шпиндельным отверстием, исполнитель под ним, матрица и год дугой снизу. Строк по центру нет — там отверстие.

## HIG

- Хит-таргеты ≥ 44pt: утилита `.tap` подкладывает псевдоэлемент `max(100%, 44px)`.
- Safe areas 47 / 34, home indicator на каждом экране приложения.
- `prefers-reduced-motion` гасит анимации (правило в ядре).
- Статус-бар переключается на `.dark-ink` для светлых экранов — флаг `light` в спеке.
