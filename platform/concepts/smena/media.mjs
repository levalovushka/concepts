/**
 * «Смена» использует ядровой .ph для всех фотографий животных.
 * Реальные фото здесь являются пользовательским контентом, поэтому
 * декоративные изображения и внешние URL в прототип не добавляются.
 */
console.log('«Смена»: медианосители представлены ядровым .ph.');
