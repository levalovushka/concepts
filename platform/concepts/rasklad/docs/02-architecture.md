# Расклад — карта экранов и архитектура

Блоки `@generated:*` пересобираются командой `npm run docs -- rasklad` — внутрь них руками не пишем. Всё остальное здесь — ручной текст.

## Информационная архитектура

Дерево выведено из разметки экранов и `concept.json`: вложенность — по `parent`, подпись «открывается» — реальный элемент прототипа. Первые два узла всегда одни и те же: вход по номеру (`phone` → `code`).

<!-- @generated:ia-tree -->
```
Вход по номеру (phone) — старт, без таб-бара · открывается: старт
    ├─ Пароль (password) — push, без таб-бара · открывается: «Далее»
    │   └─ Аккаунт (account) — push, без таб-бара · открывается: «Открыть»
    │       └─ Удаление аккаунта (deleteaccount) — push, без таб-бара · открывается: «Удалить аккаунт»
    └─ Создать аккаунт (register) — push, без таб-бара · открывается: «Создать аккаунт»
        └─ Пароль нового аккаунта (registerpassword) — push, без таб-бара · открывается: «Далее»

Сегодня (home) — tab (root, старт после входа) · открывается: «Продолжить без аккаунта», «Войти» … · camera
    ├─ Скан карты (scan) — fullscreen · открывается: «Сканировать карту» (camera), «Сканировать карту вместо списка», «Сканировать карту»
    │   └─ Номер вручную (scanfail) — modal (ошибка) · открывается: «Не читается — ввести номер вручную»
    ├─ Карты колоды (cards) — modal picker · открывается: «Сканировать карту» (camera), «Выбрать карту из списка», «Выбрать из списка», «Или выбрать карту из списка»
    ├─ Карта (card) — push · открывается: «Карта 07 Мост, прошлый вечер», «Открыть размышление» …, «Открыть размышление», «Карта 07 Мост» …
    │   └─ Размышление (player) — fullscreen · открывается: «Размышление Переход, 11 минут», «Размышление Что остаётся на берегу, 7 минут» … · audio
    │       └─ Локскрин · Now Playing (background) — system · открывается: «Погасить экран» (audio), «Голос в фоне»
    └─ Колода (deck) — push · открывается: «Базовая колода, прогресс»

Дневник (diary) — tab (root) · открывается: вкладка таб-бара
    └─ Запись практики (entry) — push · открывается: «Записать в дневник», «Запись 9 августа, карта 07 Мост» … · photo
        └─ Фото (gallery) — system picker · открывается: «Приложить фото расклада» (photo)

Профиль (profile) — tab (root) · открывается: вкладка таб-бара · push
```
<!-- @end -->

## Карта экранов

<!-- @generated:screen-map -->
| ID | Название | Тип | Доступы |
|---|---|---|---|
| `phone` | Вход по номеру | старт, без таб-бара | — |
| `password` | Пароль | push, без таб-бара | — |
| `register` | Создать аккаунт | push, без таб-бара | — |
| `registerpassword` | Пароль нового аккаунта | push, без таб-бара | — |
| `account` | Аккаунт | push, без таб-бара | — |
| `deleteaccount` | Удаление аккаунта | push, без таб-бара | — |
| `home` | Сегодня | tab (root, старт после входа) | camera |
| `scan` | Скан карты | fullscreen | — |
| `scanfail` | Номер вручную | modal (ошибка) | — |
| `cards` | Карты колоды | modal picker | — |
| `card` | Карта | push | — |
| `player` | Размышление | fullscreen | audio (activate) |
| `background` | Локскрин · Now Playing | system | — |
| `deck` | Колода | push | — |
| `diary` | Дневник | tab (root) | — |
| `entry` | Запись практики | push | photo |
| `gallery` | Фото | system picker | — |
| `profile` | Профиль | tab (root) | push |
<!-- @end -->

## Переходы: откуда куда и чем

Каждая строка — элемент, который есть в прототипе. Вкладки таб-бара опущены: они ведут в корни разделов и одинаковы на всех экранах.

<!-- @generated:transitions -->
| Экран | Что можно сделать | Ведёт на | Доступ | Тип перехода |
|---|---|---|---|---|
| `phone` | «Далее» | `password` | — | переход |
| `phone` | «Создать аккаунт» | `register` | — | переход |
| `phone` | «Продолжить без аккаунта» | `home` | — | переход |
| `password` | «Назад» | `phone` | — | возврат по IA |
| `password` | «Войти» | `home` | — | переход |
| `register` | «Назад» | `phone` | — | возврат по IA |
| `register` | «Далее» | `registerpassword` | — | переход |
| `register` | «Уже есть аккаунт? Войти» | `phone` | — | переход |
| `registerpassword` | «Назад» | `register` | — | возврат по IA |
| `registerpassword` | «Создать аккаунт» | `home` | — | переход |
| `account` | «Назад» | `password` | — | возврат по IA |
| `account` | «Выйти» | `phone` | — | переход |
| `account` | «Удалить аккаунт» | `deleteaccount` | — | переход |
| `deleteaccount` | «Назад» | `account` | — | возврат по IA |
| `deleteaccount` | «Удалить аккаунт» | `phone` | — | переход |
| `home` | «Сканировать карту» | `scan` | `NSCameraUsageDescription` | доступ разрешён |
| `home` | «Сканировать карту» | `cards` | `NSCameraUsageDescription` | отказ → fallback |
| `home` | «Выбрать карту из списка» | `cards` | — | переход |
| `home` | «Карта 07 Мост, прошлый вечер» | `card` | — | переход |
| `home` | «Базовая колода, прогресс» | `deck` | — | переход |
| `scan` | «Закрыть» | `home` | — | возврат по IA |
| `scan` | «Открыть размышление» | `card` | — | переход |
| `scan` | «Не читается — ввести номер вручную» | `scanfail` | — | переход |
| `scanfail` | «Открыть размышление» | `card` | — | переход |
| `scanfail` | «Или выбрать карту из списка» | `cards` | — | переход |
| `cards` | «Сканировать карту вместо списка» | `scan` | — | переход |
| `cards` | «Карта 07 Мост», «Карта 11 Порог» … | `card` | — | переход |
| `card` | «Назад на сегодня» | `home` | — | возврат по IA |
| `card` | «Размышление Переход, 11 минут», «Размышление Что остаётся на берегу, 7 минут» … | `player` | — | переход |
| `player` | «Назад к карте» | `card` | — | возврат по IA |
| `player` | «Погасить экран» | `background` | `UIBackgroundModes: audio` | entitlement, без alert |
| `player` | «Записать в дневник» | `entry` | — | переход |
| `background` | «Вернуться в размышление» | `player` | — | возврат по IA |
| `deck` | «Назад на сегодня» | `home` | — | возврат по IA |
| `deck` | «Сканировать карту» | `scan` | — | переход |
| `deck` | «Выбрать из списка» | `cards` | — | переход |
| `deck` | «Карта 07 Мост», «Карта 41 Ветер» … | `card` | — | переход |
| `diary` | «Запись 9 августа, карта 07 Мост», «Запись 8 августа, карта 41 Ветер» … | `entry` | — | переход |
| `entry` | «Назад в дневник» | `diary` | — | возврат по IA |
| `entry` | «Приложить фото расклада» | `gallery` | `NSPhotoLibraryUsageDescription` | доступ разрешён |
| `entry` | «Приложить фото расклада» | `entry` | `NSPhotoLibraryUsageDescription` | отказ → fallback |
| `gallery` | «Выбранный снимок», «Снимок 2» … | `entry` | — | возврат по IA |
| `profile` | «Открыть» | `account` | — | переход |
| `profile` | «Новые паки и серии» | `profile` | `aps-environment` | доступ разрешён |
| `profile` | «Голос в фоне» | `background` | — | переход |
<!-- @end -->

## Действия на экранах

Каждый интерактивный элемент прототипа: что он делает, куда ведёт и какой ключ при этом запрашивается. В отличие от таблицы переходов выше, вкладки таб-бара, кнопки возврата и подтверждения без перехода тоже здесь — раздел выводится из разметки и отвечает на вопрос «что делает эта кнопка», не заставляя открывать HTML.

<!-- @generated:actions -->
| Экран | Элемент | Роль | Что делает | Ведёт на | При отказе | Ключ |
|---|---|---|---|---|---|---|
| `phone` | Далее | элемент экрана | открывает экран | Пароль (password) | — | — |
| `phone` | Создать аккаунт | элемент экрана | открывает экран | Создать аккаунт (register) | — | — |
| `phone` | Продолжить без аккаунта | элемент экрана | открывает экран | Сегодня (home) | — | — |
| `phone` | пользовательское соглашение | элемент экрана | показывает подтверждение «Соглашение · rasklad.app/terms» | остаётся на экране | — | — |
| `phone` | политику конфиденциальности | элемент экрана | показывает подтверждение «Политика · rasklad.app/privacy» | остаётся на экране | — | — |
| `phone` | Помощь и поддержка | элемент экрана | показывает подтверждение «Справка · rasklad.app/help · support@rasklad.app» | остаётся на экране | — | — |
| `password` | Назад | кнопка «назад» | возвращает к родительскому экрану | Вход по номеру (phone) | — | — |
| `password` | Войти | элемент экрана | открывает экран | Сегодня (home) | — | — |
| `password` | Забыли пароль? | элемент экрана | показывает подтверждение «Ссылка для восстановления отправлена» | остаётся на экране | — | — |
| `register` | Назад | кнопка «назад» | возвращает к родительскому экрану | Вход по номеру (phone) | — | — |
| `register` | Далее | элемент экрана | открывает экран | Пароль нового аккаунта (registerpassword) | — | — |
| `register` | Уже есть аккаунт? Войти | элемент экрана | открывает экран | Вход по номеру (phone) | — | — |
| `registerpassword` | Назад | кнопка «назад» | возвращает к родительскому экрану | Создать аккаунт (register) | — | — |
| `registerpassword` | Создать аккаунт | элемент экрана | открывает экран | Сегодня (home) | — | — |
| `registerpassword` | пользовательское соглашение | элемент экрана | показывает подтверждение «Соглашение · rasklad.app/terms» | остаётся на экране | — | — |
| `registerpassword` | политику конфиденциальности | элемент экрана | показывает подтверждение «Политика · rasklad.app/privacy» | остаётся на экране | — | — |
| `registerpassword` | Помощь и поддержка | элемент экрана | показывает подтверждение «Справка · rasklad.app/help · support@rasklad.app» | остаётся на экране | — | — |
| `account` | Назад | кнопка «назад» | возвращает к родительскому экрану | Пароль (password) | — | — |
| `account` | Выйти | элемент экрана | открывает экран | Вход по номеру (phone) | — | — |
| `account` | Удалить аккаунт | элемент экрана | открывает экран | Удаление аккаунта (deleteaccount) | — | — |
| `deleteaccount` | Назад | кнопка «назад» | возвращает к родительскому экрану | Аккаунт (account) | — | — |
| `deleteaccount` | Удалить аккаунт | элемент экрана | открывает экран | Вход по номеру (phone) | — | — |
| `home` | Сканировать карту | элемент экрана | спрашивает доступ camera | Скан карты (scan) | Карты колоды (cards) | `NSCameraUsageDescription` |
| `home` | Выбрать карту из списка | элемент экрана | открывает экран | Карты колоды (cards) | — | — |
| `home` | Карта 07 Мост, прошлый вечер | элемент экрана | открывает экран | Карта (card) | — | — |
| `home` | Базовая колода, прогресс | элемент экрана | открывает экран | Колода (deck) | — | — |
| `home` | Раздел Сегодня | вкладка | переключает вкладку таб-бара | Сегодня (home) | — | — |
| `home` | Раздел Дневник | вкладка | переключает вкладку таб-бара | Дневник (diary) | — | — |
| `home` | Раздел Профиль | вкладка | переключает вкладку таб-бара | Профиль (profile) | — | — |
| `scan` | Закрыть | кнопка «назад» | возвращает к родительскому экрану | Сегодня (home) | — | — |
| `scan` | Открыть размышление | элемент экрана | открывает экран | Карта (card) | — | — |
| `scan` | Не читается — ввести номер вручную | элемент экрана | открывает экран | Номер вручную (scanfail) | — | — |
| `scanfail` | Открыть размышление | элемент экрана | открывает экран | Карта (card) | — | — |
| `scanfail` | Или выбрать карту из списка | элемент экрана | открывает экран | Карты колоды (cards) | — | — |
| `cards` | Сканировать карту вместо списка | элемент экрана | открывает экран | Скан карты (scan) | — | — |
| `cards` | Карта 07 Мост | элемент экрана | открывает экран | Карта (card) | — | — |
| `cards` | Карта 11 Порог | элемент экрана | открывает экран | Карта (card) | — | — |
| `cards` | Карта 14 Окно | элемент экрана | открывает экран | Карта (card) | — | — |
| `cards` | Карта 19 Корни | элемент экрана | открывает экран | Карта (card) | — | — |
| `cards` | Карта 23 Компас | элемент экрана | открывает экран | Карта (card) | — | — |
| `cards` | Карта 28 Гнездо | элемент экрана | открывает экран | Карта (card) | — | — |
| `cards` | Карта 34 Маяк | элемент экрана | открывает экран | Карта (card) | — | — |
| `cards` | Карта 41 Ветер | элемент экрана | открывает экран | Карта (card) | — | — |
| `card` | Назад на сегодня | кнопка «назад» | возвращает к родительскому экрану | Сегодня (home) | — | — |
| `card` | Размышление Переход, 11 минут | элемент экрана | открывает экран | Размышление (player) | — | — |
| `card` | Размышление Что остаётся на берегу, 7 минут | элемент экрана | открывает экран | Размышление (player) | — | — |
| `card` | Размышление Утренняя сверка, 5 минут | элемент экрана | открывает экран | Размышление (player) | — | — |
| `card` | Слушать · 11 мин | элемент экрана | открывает экран | Размышление (player) | — | — |
| `player` | Назад к карте | кнопка «назад» | возвращает к родительскому экрану | Карта (card) | — | — |
| `player` | Погасить экран | элемент экрана | включает entitlement audio, системного alert нет | Локскрин · Now Playing (background) | — | `UIBackgroundModes: audio` |
| `player` | Записать в дневник | элемент экрана | открывает экран | Запись практики (entry) | — | — |
| `background` | Вернуться в размышление | кнопка «назад» | возвращает к родительскому экрану | Размышление (player) | — | — |
| `deck` | Назад на сегодня | кнопка «назад» | возвращает к родительскому экрану | Сегодня (home) | — | — |
| `deck` | Сканировать карту | элемент экрана | открывает экран | Скан карты (scan) | — | — |
| `deck` | Выбрать из списка | элемент экрана | открывает экран | Карты колоды (cards) | — | — |
| `deck` | Новые паки и серии | элемент экрана | показывает подтверждение «Следим за раскладом» | остаётся на экране | — | — |
| `deck` | Карта 07 Мост | элемент экрана | открывает экран | Карта (card) | — | — |
| `deck` | Карта 41 Ветер | элемент экрана | открывает экран | Карта (card) | — | — |
| `deck` | Карта 28 Гнездо | элемент экрана | открывает экран | Карта (card) | — | — |
| `diary` | Запись 9 августа, карта 07 Мост | элемент экрана | открывает экран | Запись практики (entry) | — | — |
| `diary` | Запись 8 августа, карта 41 Ветер | элемент экрана | открывает экран | Запись практики (entry) | — | — |
| `diary` | Запись 7 августа, карта 28 Гнездо | элемент экрана | открывает экран | Запись практики (entry) | — | — |
| `diary` | Запись 6 августа, карта 14 Окно | элемент экрана | открывает экран | Запись практики (entry) | — | — |
| `diary` | Раздел Сегодня | вкладка | переключает вкладку таб-бара | Сегодня (home) | — | — |
| `diary` | Раздел Дневник | вкладка | переключает вкладку таб-бара | Дневник (diary) | — | — |
| `diary` | Раздел Профиль | вкладка | переключает вкладку таб-бара | Профиль (profile) | — | — |
| `entry` | Назад в дневник | кнопка «назад» | возвращает к родительскому экрану | Дневник (diary) | — | — |
| `entry` | Приложить фото расклада | элемент экрана | спрашивает доступ photo | Фото (gallery) | Запись практики (entry) | `NSPhotoLibraryUsageDescription` |
| `gallery` | Выбранный снимок | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `gallery` | Снимок 2 | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `gallery` | Снимок 3 | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `gallery` | Снимок 4 | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `gallery` | Снимок 5 | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `gallery` | Снимок 6 | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `gallery` | Снимок 7 | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `gallery` | Снимок 8 | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `gallery` | Снимок 9 | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `gallery` | Снимок 10 | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `gallery` | Снимок 11 | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `gallery` | Снимок 12 | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `gallery` | Снимок 13 | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `gallery` | Снимок 14 | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `gallery` | Снимок 15 | кнопка «назад» | возвращает к родительскому экрану | Запись практики (entry) | — | — |
| `profile` | Открыть | элемент экрана | открывает экран | Аккаунт (account) | — | — |
| `profile` | Новые паки и серии | элемент экрана | спрашивает доступ push | Профиль (profile) | Профиль (profile) | `aps-environment` |
| `profile` | Голос в фоне | элемент экрана | открывает экран | Локскрин · Now Playing (background) | — | — |
| `profile` | Раздел Сегодня | вкладка | переключает вкладку таб-бара | Сегодня (home) | — | — |
| `profile` | Раздел Дневник | вкладка | переключает вкладку таб-бара | Дневник (diary) | — | — |
| `profile` | Раздел Профиль | вкладка | переключает вкладку таб-бара | Профиль (profile) | — | — |
<!-- @end -->

## Матрица доступов

<!-- @generated:perm-matrix -->
| Ключ | Жест пользователя | Экран | Если отказ | Риск Review |
|---|---|---|---|---|
| `UIBackgroundModes: audio` | «Погасить экран» в плеере · свитч «Голос в фоне» в профиле | Размышление | Без entitlement звук обрывается на блокировке — практику вести нечем, такую сборку не отправляем | **Условный** — Реальный playback через AVAudioSession, заполненный MPNowPlayingInfoCenter и работающие remote-команды |
| `NSCameraUsageDescription` | «Сканировать карту» | Сегодня | Скан убирается, карта выбирается вручную из списка колоды по номеру на ней | Низкий |
| `NSPhotoLibraryUsageDescription` | «Приложить фото расклада» | Запись практики | Место под фото убирается, запись сохраняется текстом | Низкий |
| `aps-environment` | Свитч «Новые паки и серии» | Профиль | Свитч остаётся выключенным, о новых паках не сообщаем | **Условный** — Настоящая регистрация в APNs и хотя бы один живой пуш из дашборда провайдера |
<!-- @end -->

## Почему здесь нет бэкенда

<!-- @generated:backendless -->
| Требовало бы сервера | Решение без сервера |
|---|---|
| Каталог карт и аудио-размышлений | Контент-пак: `cards.json` + m4a статикой на CDN. Только чтение, колода фиксированная |
| Опциональные вход и регистрация по номеру телефона | SDK провайдера аутентификации, пароль и токен сессии в Keychain |
| Профили, лента, чужие записи практики | Вырезаны. Вход только опознаёт владельца колоды — ни ленты, ни UGC, ни модерации |
| Дневник практики | CoreData на устройстве: запись, карта вечера, длительность размышления |
| Фото расклада на столе | `PHPicker` отдаёт выбранный снимок, копия лежит в FileManager рядом с записью |
| Серия вечеров и прогресс по колоде | Считаются на устройстве из записей CoreData — никакой статистики на сервере |
| Push о новых паках | SDK провайдера (OneSignal / FCM), бродкаст из дашборда — серверного кода не пишем |
| Распознавание карты | `DataScannerViewController` on-device по номеру и метке; не прочиталось — ручной выбор по номеру |
| Поделиться записью | Системный share sheet — файл уходит из приложения, а не на наш сервер |
<!-- @end -->

## Слои

| Слой | Что внутри |
|---|---|
| UI | Экраны, tab bar (3 вкладки), nav stack, system ask, snackbar. Статусы доступов не хранит |
| Domain | `Deck` · `Card` · `Reflection` · `PlaybackSession` · `PracticeEntry` · `Streak` · `DeckProgress` · `ContentPack` |
| Services | `Permissions` · `Playback` (AVAudioSession `.playback`, MPNowPlayingInfoCenter, MPRemoteCommandCenter) · `CardScanner` (DataScannerViewController, on-device) · `MediaPicker` (PHPicker) · `Push` (SDK провайдера) · `PhoneAuth` (SDK провайдера) · `ProgressCalc` |
| Data | CoreData — записи, прогресс, серия. Keychain — токен входа. FileManager — копия выбранного фото. Контент-пак `cards.json` + m4a с CDN, только чтение |
| External | CDN контент-пака · APNs через SDK · SDK входа по номеру. Ни аналитики с трекингом, ни ad SDK, ни MapKit |

`PermissionsService` — единственная точка запроса доступов. Экран не спрашивает систему сам, поэтому «спросили дважды» и «спросили на старте» невозможны структурно, а не по договорённости.

---

## Правила IA, которых не видно в таблицах

**Три вкладки, а не четыре.** «Колода» просилась в таб-бар, но она нужна раз в несколько вечеров, а не каждый вечер, поэтому лежит строкой на «Сегодня». Таб-бар — «Сегодня» (практика), «Дневник» (что было), «Профиль» (настройки).

**Почему `card` — ребёнок `home`, а не `cards`.** До карты доходят четырьмя путями: скан, ручной ввод номера, список колоды, история на «Сегодня» и в «Колоде». Архитектурный родитель один — `home`; фактический возврат считается по `presentedFrom`, который движок фиксирует в момент открытия. Поэтому «Закрыть» из списка колоды возвращает на «Сегодня», а не выбрасывает в список.

**Модальный список закрывается `data-jump`.** Выбрали карту в `cards` — оверлей не должен остаться в стеке под картой, иначе «назад» приведёт обратно в список. Тот же приём в `scanfail` после ручного ввода номера.

**Отказ камеры убирает скан, а не подписывает его.** На `home` и в `deck` строка «Сканировать карту» скрывается (`data-hide-denied="camera"`), появляется объяснение и остаётся ручной выбор. Экран, который показывает кнопку скана и рядом пишет «камеры нет», противоречит сам себе.

**Fallback лежит там, куда приводит отказ.** Оба триггера камеры (`home`, `deck`) и триггер из самого списка ведут при отказе на `cards` — именно там объявлен `data-show-denied="camera"` и лежит ручной выбор. Отказ в «Фото» оставляет на `entry`, там же убирается место под снимок. Отказ в пуше оставляет на том экране, где был свитч (`profile` или `deck`), и свитч остаётся выключенным.

**Сбой фичи ≠ отказ доступа.** `scanfail` — это не «нет доступа к камере», а «метку не удалось прочитать»: мало света, карта под углом. У экрана два состояния: обычная ошибка чтения (с кнопкой «Сканировать ещё раз») и состояние при отказе камеры (кнопка повтора убрана — сканировать нечем). Ручной ввод даёт ровно те же поля, что даёт автоматика: номер карты и колоду.

**Entitlement не показывает алерт.** `audio` активируется сценарием: «Погасить экран» в плеере и свитч «Голос в фоне» в профиле. Системного промпта тут нет и быть не должно — в журнале доступов он появляется как granted без алерта.

**Диплинков нет.** `com.apple.developer.associated-domains` из пресета не заявлен: экранов-превью, приёма чужих ссылок и «отложенного» в концепте нет, а заявлять ключ без достижимой фичи нельзя. Ключ остаётся неотработанным — это названная дыра в наборе, а не забытая строка.
