# Партия — карта экранов и архитектура

Заполняется по PLAYBOOK.md. Блоки `@generated:*` пересобираются командой `npm run docs -- partiya` — внутрь них руками не пишем.

## Информационная архитектура

Дерево выведено из разметки экранов и `concept.json`: вложенность — по `parent`, подпись «открывается» — реальный элемент прототипа. Первые два узла всегда одни и те же: вход по номеру (`phone` → `code`).

<!-- @generated:ia-tree -->
```
Вход по номеру (phone) — старт, без таб-бара · открывается: старт
    └─ Код из SMS (code) — push · открывается: «Получить код»

Программа (program) — tab (root) · открывается: «Войти», «Записи партий»
    ├─ Номер программы (piece) — push · открывается: «Продолжить №5 · с буквы В», «Номер 1, Бортнянский, Скажи ми Господи» …, «Уведомление: альты в номере 5 переписаны»
    │   ├─ Партия (player) — fullscreen, тёмный · открывается: «Слушать альтов с буквы В», «Сопрано» … · audio
    │   │   └─ Погасший экран (background) — системная поверхность · открывается: «Погасить экран» (audio)
    │   └─ Страницы номера (sheet) — push · открывается: «Страницы номера», «Страницы номера, снято 18 февраля» … · camera
    │       └─ Съёмка страниц (camera) — fullscreen, системный сканер · открывается: «Страница 3, переснять», «Снять страницы» … (camera)
    ├─ Дополнения (releases) — push · открывается: «Дополнения к программе», «Версия пака» · push
    │   └─ Уведомление о дополнении (notif) — системная поверхность · открывается: «Сообщать о дополнениях» (push)
    └─ Настройки (settings) — push · открывается: «Настройки», «Ваш голос: альт» …

Ноты (notes) — tab (root) · открывается: «Снимки с репетиций», «Открыть снимки» …, «Разложить 9 снимков» · photo
    └─ Снимки с репетиций (import) — push · открывается: «Ноты с репетиции» (photo), «Пять снимков без номера»

Журнал (journal) — tab (root) · открывается: вкладка таб-бара
```
<!-- @end -->

## Карта экранов

<!-- @generated:screen-map -->
| ID | Название | Тип | Доступы |
|---|---|---|---|
| `phone` | Вход по номеру | старт, без таб-бара | — |
| `code` | Код из SMS | push | — |
| `program` | Программа | tab (root) | — |
| `piece` | Номер программы | push | — |
| `player` | Партия | fullscreen, тёмный | audio (activate) |
| `background` | Погасший экран | системная поверхность | — |
| `sheet` | Страницы номера | push | camera |
| `camera` | Съёмка страниц | fullscreen, системный сканер | — |
| `notes` | Ноты | tab (root) | photo |
| `import` | Снимки с репетиций | push | — |
| `journal` | Журнал | tab (root) | — |
| `releases` | Дополнения | push | push |
| `notif` | Уведомление о дополнении | системная поверхность | — |
| `settings` | Настройки | push | — |
<!-- @end -->

## Переходы: откуда куда и чем

Каждая строка — элемент, который есть в прототипе. Вкладки таб-бара опущены: они ведут в корни разделов и одинаковы на всех экранах.

<!-- @generated:transitions -->
| Экран | Что можно сделать | Ведёт на | Доступ | Тип перехода |
|---|---|---|---|---|
| `phone` | «Получить код» | `code` | — | переход |
| `code` | «Назад к номеру» | `phone` | — | возврат по IA |
| `code` | «Войти» | `program` | — | переход |
| `program` | «Настройки», «Ваш голос: альт» … | `settings` | — | переход |
| `program` | «Продолжить №5 · с буквы В», «Номер 1, Бортнянский, Скажи ми Господи» … | `piece` | — | переход |
| `program` | «Снимки с репетиций» | `notes` | — | переход |
| `program` | «Дополнения к программе» | `releases` | — | переход |
| `piece` | «Назад к программе» | `program` | — | возврат по IA |
| `piece` | «Страницы номера», «Страницы номера, снято 18 февраля» | `sheet` | — | переход |
| `piece` | «Слушать альтов с буквы В», «Сопрано» … | `player` | — | переход |
| `player` | «Назад к номеру» | `piece` | — | возврат по IA |
| `player` | «Страницы рядом», «Страницы номера рядом» | `sheet` | — | переход |
| `player` | «Погасить экран» | `background` | `UIBackgroundModes: audio` | entitlement, без alert |
| `background` | «Вернуться к партии» | `player` | — | возврат по IA |
| `sheet` | «Назад к номеру» | `piece` | — | возврат по IA |
| `sheet` | «Страница 3, переснять», «Снять страницы» … | `camera` | `NSCameraUsageDescription` | доступ разрешён |
| `sheet` | «Страница 3, переснять», «Снять страницы» … | `sheet` | `NSCameraUsageDescription` | отказ → fallback |
| `sheet` | «Открыть снимки», «Разворот с купюрой» … | `notes` | — | переход |
| `sheet` | «Слушать альтов» | `player` | — | переход |
| `camera` | «Закрыть съёмку» | `sheet` | — | возврат по IA |
| `camera` | «Сохранить 3 страницы» | `sheet` | — | переход |
| `notes` | «Настройки» | `settings` | — | переход |
| `notes` | «Ноты с репетиции» | `import` | `NSPhotoLibraryUsageDescription` | доступ разрешён |
| `notes` | «Ноты с репетиции» | `notes` | `NSPhotoLibraryUsageDescription` | отказ → fallback |
| `notes` | «Открыть №5» | `piece` | — | переход |
| `notes` | «Номер 1, три страницы», «Номер 3, пять страниц, купюра» … | `sheet` | — | переход |
| `notes` | «Пять снимков без номера» | `import` | — | переход |
| `import` | «Назад к нотам» | `notes` | — | возврат по IA |
| `import` | «Выбрать номер вручную» | `sheet` | — | переход |
| `import` | «Разложить 9 снимков» | `notes` | — | подтверждение |
| `journal` | «Настройки» | `settings` | — | переход |
| `journal` | «Номер 1, уверенно», «Номер 3, шатко» … | `piece` | — | переход |
| `journal` | «Вчера, номер 5, альт», «24 февраля, номер 2, альт» … | `player` | — | переход |
| `releases` | «Назад к программе» | `program` | — | возврат по IA |
| `releases` | «Сообщать о дополнениях» | `notif` | `aps-environment` | доступ разрешён |
| `releases` | «Сообщать о дополнениях» | `releases` | `aps-environment` | отказ → fallback |
| `releases` | «Проверить сейчас» | `releases` | — | подтверждение |
| `releases` | «25 февраля, записи альтов к номерам 5 и 7», «11 февраля, купюра в номере 3» … | `piece` | — | переход |
| `releases` | «Записи партий», «Буквы и такты» | `settings` | — | переход |
| `releases` | «Снятые страницы» | `notes` | — | переход |
| `notif` | «Уведомление: альты в номере 5 переписаны» | `piece` | — | переход |
| `settings` | «Назад к программе» | `program` | — | возврат по IA |
| `settings` | «Записи партий» | `program` | — | переход |
| `settings` | «Удалить скачанные записи» | `settings` | — | подтверждение |
| `settings` | «Версия пака» | `releases` | — | переход |
<!-- @end -->

## Матрица доступов

<!-- @generated:perm-matrix -->
| Ключ | Жест пользователя | Экран | Если отказ | Риск Review |
|---|---|---|---|---|
| `NSCameraUsageDescription` | «Снять страницы» | Страницы номера | Остаются буквы и такты из пака программы, страницы можно добрать из галереи | Низкий |
| `NSPhotoLibraryUsageDescription` | «Ноты с репетиции» | Ноты | Остаётся съёмка страниц камерой; список найденного и счётчик снимков исчезают | **Условный** — PHPhotoLibrary .readWrite и выборка по creationDate: PHPicker дал бы ручной выбор, но не нашёл бы снимки сам — а фича именно в том, что приложение обходит библиотеку по датам репетиций |
| `UIBackgroundModes: audio` | «Погасить экран» | Партия | Прослушивание на открытом экране остаётся, повтор фрагмента задаётся кнопкой | **Условный** — AVAudioSession .playback, заполненный MPNowPlayingInfoCenter (номер, голос, такт) и MPRemoteCommandCenter: play/pause, skipBackward как повтор фрагмента, next как следующая буква |
| `aps-environment` | Свитч «Сообщать о дополнениях» | Дополнения | Дополнения всегда можно проверить вручную на этом экране | **Условный** — SDK push-провайдера в сборке, регистрация в APNs и дашборд редакции для отправки: своего API у приложения нет |
<!-- @end -->

## Почему здесь нет бэкенда

<!-- @generated:backendless -->
| Требовало бы сервера | Решение без сервера |
|---|---|
| Каталог программ и записи партий по голосам | Статический пак: program.json и аудио на CDN, только чтение |
| Права на записи | Пак собран из произведений общественного достояния, партии синтезированы по партитуре |
| Снятые страницы, пометки, журнал уверенности | FileManager и CoreData на устройстве: наружу не уходит ничего |
| Обмен страницами между хористами | Вырезано: UGC нет, значит нет ни модерации, ни чужих данных |
| Вход по номеру | SDK провайдера аутентификации, токен в Keychain |
| Уведомление о дополнении к программе | SDK push-провайдера и его дашборд: своего обработчика нет |
<!-- @end -->

## Правила IA

**Три вкладки.** «Программа» — номера концерта и состояние загрузок; «Ноты» — снятые и найденные страницы по номерам; «Журнал» — уверенность по номерам и прогоны. Профиля нет: учётной записи в концепте тоже нет, есть номер телефона, и он лежит в «Настройках».

**Первый запуск без промптов.** `phone` → `code` → `program`. На входе доступов не спрашиваем вовсе; первый системный алерт возможен только после явного жеста на третьем экране и глубже.

**Один доступ — один жест, и жест назван предметно.** «Снять страницы», «Ноты с репетиции», «Погасить экран», «Сообщать о дополнениях». Ни один из них не называется «Разрешить доступ»: кнопка обещает результат, а не разрешение.

**Куда приводит отказ.** Отказ всегда возвращает на экран-источник, и именно там лежит видимый fallback:

| Отказ | Экран | Что исчезает | Что остаётся |
|---|---|---|---|
| Камера | `sheet` | Съёмка страниц и кнопка «переснять с. 3» | Буквы и такты идут с записью; страницы добираются из снимков в галерее |
| Фото | `notes` | Счётчик найденных снимков и весь разбор по датам | Ручная съёмка страниц в номере |
| Уведомления | `releases` | Автоматическое сообщение о дополнении | Строка «Проверить сейчас» на том же экране |

Правило соблюдено по построению: у камеры два триггера на `sheet` (кнопка и строка «с. 3»), у обоих ветка отказа ведёт на `sheet`, где `data-show-denied="camera"` и лежит.

**Сбой фичи ≠ отказ доступа.** На `import` три снимка из двенадцати не привязались к номеру: в кадр не попала шапка страницы. Это отдельное состояние с причиной человеческим языком, одним основным путём («выбрать номер вручную») и одним запасным («переснять развороты») — и оно показывается при *выданном* доступе.

**Фоновый режим активируется сценарием, а не алертом.** `UIBackgroundModes: audio` — entitlement: на `player` кнопка «Погасить экран» ведёт на `background` без системного запроса. Ровно там ревьюер видит Now Playing с номером, голосом и текущим тактом — то, что требует 2.5.4.

**Повторно не переспрашиваем.** Отвеченный ключ второй раз системный алерт не вызывает: свитчи на `settings` отражают статус (`data-switch`), а путь к смене — «Открыть Настройки» в снекбаре.
