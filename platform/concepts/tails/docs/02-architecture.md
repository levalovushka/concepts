# Хвосты — архитектура

Клиентское приложение без своего бэкенда: аккаунты, лента, сообщества и сообщения живут на SDK провайдера, медиа — в его же хранилище. Ни один заявленный ключ не требует писать и разворачивать свой обработчик.

Набор доступов — ВКонтакте, заявлено 22 ключа. Правило, из которого выведена вся таблица ниже: **доступ заявляется только там, где за ним стоит фича, до которой пользователь доходит за 2–3 тапа.** Причина живёт в review-notes, фича — в сборке, а проверяют фичу.

---

## Карта экранов

<!-- @generated:screen-map -->
| ID | Название | Тип | Доступы |
|---|---|---|---|
| `phone` | Вход по почте | старт | — |
| `code` | Код из письма | push | — |
| `codefail` | Неверный код | push | — |
| `home` | Главная | tab (root) | location |
| `pet` | Профиль питомца | push | speech |
| `nearby` | Рядом | tab (root) | — |
| `walk` | Прогулка | push | remotenotif (activate), wifiinfo (activate) |
| `create` | Новый момент | tab (root) | camera, photos |
| `camera` | Камера | fullscreen | — |
| `media` | Фото | system picker | — |
| `places` | Площадки рядом | push | — |
| `chats` | Сообщения | tab (root) | — |
| `chat` | Чат | push | mic, commnotif (activate), voip (activate) |
| `voice` | Голосовое | modal | — |
| `profile` | Профиль | tab (root) | contacts |
| `settings` | Настройки | push | push, fetch (activate), appgroups (activate), autofill (activate), faceid, shareext (activate) |
| `widget` | Виджет на экране «Домой» | fullscreen | keychain (activate) |
| `fill` | Автозаполнение на сайте | fullscreen | — |
| `refresh` | Обновление в фоне | push | bgtask (activate) |
| `mates` | Контакты в «Хвостах» | push | — |
| `ads` | Реклама вместо подписки | modal | tracking |
| `lock` | Замок на ветпаспорте | push | — |
| `vetnote` | Заметка о самочувствии | push | — |
| `course` | Курс послушания | push | audio (activate) |
| `background` | Экран погас | fullscreen | — |
| `call` | Звонок догситтеру | fullscreen | — |
| `vaccine` | Прививки и обработки | push | calendar |
| `netqr` | Сеть площадки по QR | modal | hotspot |
| `shareext` | Поделиться в «Хвосты» | modal | — |
<!-- @end -->

## Матрица доступов

Каждая строка читается так: пользователь делает жест — система спрашивает — при отказе остаётся рабочий путь.

<!-- @generated:perm-matrix -->
| Ключ | Жест пользователя | Экран | Если отказ | Риск Review |
|---|---|---|---|---|
| `NSCameraUsageDescription` | «Снять» | Новый момент | Можно выбрать готовый снимок | Низкий |
| `NSPhotoLibraryUsageDescription` | «Из Фото» | Новый момент | Можно снять новый кадр камерой | Низкий |
| `NSMicrophoneUsageDescription` | «Записать голосовое» | Чат | Остаются текст и фото | Низкий |
| `NSLocationWhenInUseUsageDescription` | «Кто гуляет рядом» | Главная | Район выбирается вручную | Низкий |
| `aps-environment` | «Прогулки и ответы» в «Настройках» | Настройки | Обновления помечаются точкой внутри приложения | **Условный** — Функция активируется только явным действием пользователя и имеет видимый fallback |
| `com.apple.developer.usernotifications.communication` | «Сообщения с аватаром» | Чат | Обычное уведомление без аватара | **Условный** — Функция активируется только явным действием пользователя и имеет видимый fallback |
| `UIBackgroundModes: remote-notification` | «Обновлять состав» | Прогулка | Состав обновляется при открытии | **Условный** — Функция активируется только явным действием пользователя и имеет видимый fallback |
| `UIBackgroundModes: fetch` | «Обновлять ленту в фоне» | Настройки | Лента обновится после открытия | **Условный** — Функция активируется только явным действием пользователя и имеет видимый fallback |
| `BGTaskSchedulerPermittedIdentifiers` | «Проверить задачу» | Обновление в фоне | Без задачи обновление только вручную | **Условный** — Функция активируется только явным действием пользователя и имеет видимый fallback |
| `com.apple.security.application-groups` | «Виджет» | Настройки | Прогулка остаётся внутри приложения | Низкий |
| `keychain-access-groups` | «Открыть „Хвосты“» с виджета | Виджет на экране «Домой» | Виджет открывает приложение для входа | Низкий |
| `com.apple.developer.authentication-services.autofill-credential-provider` | «Вход на сайте» | Настройки | Вход вручную почтой и паролем | Низкий |
| `com.apple.developer.networking.wifi-info` | «Я на месте» | Прогулка | Отметка по кнопке без автоматической проверки | **Условный** — Функция активируется только явным действием пользователя и имеет видимый fallback |
| `NSContactsUsageDescription` | «Найти среди контактов» | Профиль | Остаётся поиск по кличке и ссылка-приглашение | Низкий |
| `NSUserTrackingUsageDescription` | «Продолжить» | Реклама вместо подписки | Реклама остаётся, но перестаёт быть персональной | Средний |
| `NSFaceIDUsageDescription` | «Замок Face ID» | Настройки | Остаётся код-пароль устройства | Низкий |
| `NSSpeechRecognitionUsageDescription` | «Надиктовать заметку» — цепочкой с микрофоном | Профиль питомца | Заметка остаётся звуком: её можно слушать, но не искать словом | Низкий |
| `UIBackgroundModes: audio` | «Слушать» | Курс послушания | Без entitlement звук обрывается — не ship | **Условный** — Реальный playback, заполненный MPNowPlayingInfoCenter и remote-команды |
| `UIBackgroundModes: voip` | «Позвонить» | Чат | Остаётся переписка в чате | **Условный** — PushKit + CallKit: входящий звонок поднимается системным экраном вызова, иначе ключ не заявляется |
| `NSCalendarsFullAccessUsageDescription + NSCalendarsUsageDescription` | «Добавить в Календарь» | Прививки и обработки | Срок остаётся в карточке питомца и в напоминании приложения | Низкий |
| `NSExtensionPointIdentifier: com.apple.share-services` | «Поделиться» в другом приложении | Настройки | Остаётся создание записи внутри приложения | **Условный** — Отдельный target расширения; черновик кладётся в общий контейнер App Group |
| `com.apple.developer.networking.HotspotConfiguration` | «Подключиться» | Сеть площадки по QR | Сеть выбирается вручную в Настройках | Низкий |
<!-- @end -->

## Информационная архитектура

Дерево выведено из разметки экранов, а не нарисовано руками: рукописная схема расходится с прототипом первой же правкой.

<!-- @generated:ia-tree -->
```
Вход по почте (phone) — старт · открывается: старт
    └─ Код из письма (code) — push · открывается: «Продолжить с почтой»
        ├─ Неверный код (codefail) — push · открывается: «Ввести код»
        └─ Главная (home) — tab (root) · открывается: «Продолжить с Google», «Войти» …, «Открыть «Хвосты»» (keychain), «Проверить задачу» (bgtask) · location
            ├─ Профиль питомца (pet) — push · открывается: «Ксения и Трюфель», «Открыть профиль Трюфеля», «Сохранить в карточку» · speech
            │   ├─ Заметка о самочувствии (vetnote) — push · открывается: «Надиктовать заметку» (speech), «Надиктовать заметку о самочувствии»
            │   └─ Прививки и обработки (vaccine) — push · открывается: «Прививки и обработки», «19 мая, 09:15 — Мария Тенищева» · calendar
            ├─ Рядом (nearby) — tab (root) · открывается: «Кто гуляет рядом» (location)
            │   └─ Прогулка (walk) — push · открывается: «Спокойный круг у пруда», «Быстро по набережной» … · remotenotif, wifiinfo
            │       └─ Сеть площадки по QR (netqr) — modal · открывается: «Подключиться к сети площадки» · hotspot
            ├─ Площадки рядом (places) — push · открывается: «Сообщества», «Лопухинский сад» …
            └─ Курс послушания (course) — push · открывается: «Курс послушания · «Подзыв в парке с отвлече…» · audio
                └─ Экран погас (background) — fullscreen · открывается: «Слушать» (audio)

Новый момент (create) — tab (root) · открывается: «Сохранить в черновик» · camera, photos
    ├─ Камера (camera) — fullscreen · открывается: «Снять» (camera)
    └─ Фото (media) — system picker · открывается: «Из Фото» (photos)

Сообщения (chats) — tab (root) · открывается: «Отправить в переписку»
    └─ Чат (chat) — push · открывается: «Написать», «Ксения и Трюфель» … · mic, commnotif, voip
        ├─ Голосовое (voice) — modal · открывается: «Записать голосовое» (mic)
        └─ Звонок догситтеру (call) — fullscreen · открывается: «Позвонить» (voip)

Профиль (profile) — tab (root) · открывается: «Продолжить» (tracking) · contacts
    ├─ Настройки (settings) — push · открывается: «Настройки» · push, fetch, appgroups, autofill, faceid, shareext
    │   ├─ Виджет на экране «Домой» (widget) — fullscreen · открывается: «Виджет» (appgroups) · keychain
    │   ├─ Автозаполнение на сайте (fill) — fullscreen · открывается: «Вход на сайте» (autofill)
    │   ├─ Обновление в фоне (refresh) — push · открывается: «Фоновая задача» · bgtask
    │   ├─ Реклама вместо подписки (ads) — modal · открывается: «Отслеживание» · tracking
    │   ├─ Замок на ветпаспорте (lock) — push · открывается: «Замок Face ID» (faceid)
    │   └─ Поделиться в «Хвосты» (shareext) — modal · открывается: «Поделиться в «Хвосты»» (shareext)
    └─ Контакты в «Хвостах» (mates) — push · открывается: «Найти среди контактов» (contacts)
```
<!-- @end -->

## Переходы

<!-- @generated:transitions -->
| Экран | Что можно сделать | Ведёт на | Доступ | Тип перехода |
|---|---|---|---|---|
| `phone` | «Продолжить с почтой» | `code` | — | переход |
| `phone` | «Продолжить с Google» | `home` | — | переход |
| `code` | «Назад» | `phone` | — | возврат по IA |
| `code` | «Ввести код» | `codefail` | — | переход |
| `code` | «Войти» | `home` | — | переход |
| `codefail` | «Назад» | `code` | — | возврат по IA |
| `home` | «Сообщества» | `places` | — | переход |
| `home` | «Кто гуляет рядом» | `nearby` | `NSLocationWhenInUseUsageDescription` | доступ разрешён |
| `home` | «Кто гуляет рядом» | `home` | `NSLocationWhenInUseUsageDescription` | отказ → fallback |
| `home` | «Ксения и Трюфель», «Открыть профиль Трюфеля» | `pet` | — | переход |
| `home` | «Курс послушания · «Подзыв в парке с отвлече…» | `course` | — | переход |
| `pet` | «Назад» | `home` | — | возврат по IA |
| `pet` | «Написать» | `chat` | — | переход |
| `pet` | «Прививки и обработки» | `vaccine` | — | переход |
| `pet` | «Надиктовать заметку» | `vetnote` | `NSSpeechRecognitionUsageDescription` | доступ разрешён |
| `nearby` | «Спокойный круг у пруда», «Быстро по набережной» … | `walk` | — | переход |
| `nearby` | «Лопухинский сад», «Набережная у ЦПКиО» … | `places` | — | переход |
| `walk` | «Назад» | `nearby` | — | возврат по IA |
| `walk` | «Я на площадке» | `walk` | `com.apple.developer.networking.wifi-info` | entitlement, без alert |
| `walk` | «Подключиться к сети площадки» | `netqr` | — | переход |
| `walk` | «Обновлять состав» | `walk` | `UIBackgroundModes: remote-notification` | entitlement, без alert |
| `create` | «Закрыть» | `home` | — | переход |
| `create` | «Снять» | `camera` | `NSCameraUsageDescription` | доступ разрешён |
| `create` | «Снять» | `create` | `NSCameraUsageDescription` | отказ → fallback |
| `create` | «Из Фото» | `media` | `NSPhotoLibraryUsageDescription` | доступ разрешён |
| `create` | «Из Фото» | `create` | `NSPhotoLibraryUsageDescription` | отказ → fallback |
| `camera` | «Закрыть» | `create` | — | возврат по IA |
| `camera` | «Снять фото» | `home` | — | переход |
| `media` | «Закрыть» | `create` | — | возврат по IA |
| `media` | «Готово» | `home` | — | переход |
| `places` | «Назад» | `home` | — | возврат по IA |
| `places` | «Площадка у Лопухинского пруда», «Площадка на набережной» … | `walk` | — | переход |
| `chats` | «Ксения и Трюфель», «Прогулка у пруда» | `chat` | — | переход |
| `chat` | «Назад» | `chats` | — | возврат по IA |
| `chat` | «Позвонить» | `call` | `UIBackgroundModes: voip` | entitlement, без alert |
| `chat` | «Сообщения с аватаром» | `chat` | `com.apple.developer.usernotifications.communication` | entitlement, без alert |
| `chat` | «Записать голосовое» | `voice` | `NSMicrophoneUsageDescription` | доступ разрешён |
| `chat` | «Записать голосовое» | `chat` | `NSMicrophoneUsageDescription` | отказ → fallback |
| `voice` | «Закрыть», «Удалить» | `chat` | — | возврат по IA |
| `voice` | «Отправить» | `chat` | — | переход |
| `profile` | «Настройки» | `settings` | — | переход |
| `profile` | «Найти среди контактов» | `mates` | `NSContactsUsageDescription` | доступ разрешён |
| `settings` | «Назад» | `profile` | — | возврат по IA |
| `settings` | «Прогулки и ответы» | `settings` | `aps-environment` | доступ разрешён |
| `settings` | «Обновлять ленту в фоне» | `settings` | `UIBackgroundModes: fetch` | entitlement, без alert |
| `settings` | «Фоновая задача» | `refresh` | — | переход |
| `settings` | «Виджет» | `widget` | `com.apple.security.application-groups` | entitlement, без alert |
| `settings` | «Вход на сайте» | `fill` | `com.apple.developer.authentication-services.autofill-credential-provider` | entitlement, без alert |
| `settings` | «Поделиться в «Хвосты»» | `shareext` | `NSExtensionPointIdentifier: com.apple.share-services` | entitlement, без alert |
| `settings` | «Замок Face ID» | `lock` | `NSFaceIDUsageDescription` | доступ разрешён |
| `settings` | «Отслеживание» | `ads` | — | переход |
| `widget` | «Открыть «Хвосты»» | `home` | `keychain-access-groups` | entitlement, без alert |
| `fill` | — | — | — | дальше переходов нет |
| `refresh` | «Назад» | `settings` | — | возврат по IA |
| `refresh` | «Проверить задачу» | `home` | `BGTaskSchedulerPermittedIdentifiers` | entitlement, без alert |
| `mates` | «Назад» | `profile` | — | возврат по IA |
| `mates` | «Отправить в переписку» | `chats` | — | переход |
| `ads` | «Закрыть» | `settings` | — | возврат по IA |
| `ads` | «Продолжить» | `profile` | `NSUserTrackingUsageDescription` | доступ разрешён |
| `lock` | «Назад» | `settings` | — | возврат по IA |
| `vetnote` | «Назад» | `pet` | — | возврат по IA |
| `vetnote` | «Сохранить в карточку» | `pet` | — | переход |
| `vetnote` | «19 мая, 09:15 — Мария Тенищева» | `vaccine` | — | переход |
| `course` | «Назад» | `home` | — | возврат по IA |
| `course` | «Слушать» | `background` | `UIBackgroundModes: audio` | entitlement, без alert |
| `background` | — | — | — | дальше переходов нет |
| `call` | «Завершить звонок» | `chat` | — | возврат по IA |
| `vaccine` | «Назад» | `pet` | — | возврат по IA |
| `vaccine` | «Добавить в Календарь» | `vaccine` | `NSCalendarsFullAccessUsageDescription + NSCalendarsUsageDescription` | доступ разрешён |
| `vaccine` | «Надиктовать заметку о самочувствии» | `vetnote` | — | переход |
| `netqr` | «Закрыть» | `walk` | — | возврат по IA |
| `netqr` | «Подключиться» | `netqr` | `com.apple.developer.networking.HotspotConfiguration` | доступ разрешён |
| `netqr` | «Вернуться к отметке» | `walk` | — | переход |
| `shareext` | «Отмена» | `settings` | — | возврат по IA |
| `shareext` | «Сохранить в черновик» | `create` | — | подтверждение |
<!-- @end -->

## Действия на экранах

Каждый интерактивный элемент прототипа: что он делает, куда ведёт и какой ключ при этом запрашивается. В отличие от таблицы переходов выше, вкладки таб-бара, кнопки возврата и подтверждения без перехода тоже здесь — раздел выводится из разметки и отвечает на вопрос «что делает эта кнопка», не заставляя открывать HTML.

<!-- @generated:actions -->
| Экран | Элемент | Роль | Что делает | Ведёт на | При отказе | Ключ |
|---|---|---|---|---|---|---|
| `phone` | Продолжить с почтой | элемент экрана | открывает экран | Код из письма (code) | — | — |
| `phone` | Продолжить с Google | элемент экрана | открывает экран | Главная (home) | — | — |
| `phone` | Помощь | элемент экрана | показывает подтверждение «Помощь · tails.social/help» | остаётся на экране | — | — |
| `phone` | Поддержка | элемент экрана | показывает подтверждение «Поддержка · support@tails.social» | остаётся на экране | — | — |
| `phone` | Пользовательское соглашение | элемент экрана | показывает подтверждение «Соглашение · tails.social/terms» | остаётся на экране | — | — |
| `code` | Назад | кнопка «назад» | возвращает к родительскому экрану | Вход по почте (phone) | — | — |
| `code` | Ввести код | элемент экрана | открывает экран | Неверный код (codefail) | — | — |
| `code` | Войти | элемент экрана | открывает экран | Главная (home) | — | — |
| `codefail` | Назад | кнопка «назад» | возвращает к родительскому экрану | Код из письма (code) | — | — |
| `home` | Сообщества | элемент экрана | открывает экран | Площадки рядом (places) | — | — |
| `home` | Кто гуляет рядом | элемент экрана | спрашивает доступ location | Рядом (nearby) | Главная (home) | `NSLocationWhenInUseUsageDescription` |
| `home` | Ксения и Трюфель | элемент экрана | открывает экран | Профиль питомца (pet) | — | — |
| `home` | Открыть профиль Трюфеля | элемент экрана | открывает экран | Профиль питомца (pet) | — | — |
| `home` | Курс послушания · «Подзыв в парке с отвлече… | элемент экрана | открывает экран | Курс послушания (course) | — | — |
| `home` | Раздел Главная | вкладка | переключает вкладку таб-бара | Главная (home) | — | — |
| `home` | Раздел Рядом | вкладка | переключает вкладку таб-бара | Рядом (nearby) | — | — |
| `home` | Раздел Создать | вкладка | переключает вкладку таб-бара | Новый момент (create) | — | — |
| `home` | Раздел Сообщения | вкладка | переключает вкладку таб-бара | Сообщения (chats) | — | — |
| `home` | Раздел Профиль | вкладка | переключает вкладку таб-бара | Профиль (profile) | — | — |
| `pet` | Назад | кнопка «назад» | возвращает к родительскому экрану | Главная (home) | — | — |
| `pet` | Написать | элемент экрана | открывает экран | Чат (chat) | — | — |
| `pet` | Прививки и обработки | элемент экрана | открывает экран | Прививки и обработки (vaccine) | — | — |
| `pet` | Надиктовать заметку | элемент экрана | спрашивает доступ speech | Заметка о самочувствии (vetnote) | Заметка о самочувствии (vetnote) | `NSSpeechRecognitionUsageDescription` |
| `nearby` | Спокойный круг у пруда | элемент экрана | открывает экран | Прогулка (walk) | — | — |
| `nearby` | Быстро по набережной | элемент экрана | открывает экран | Прогулка (walk) | — | — |
| `nearby` | Знакомство щенков | элемент экрана | открывает экран | Прогулка (walk) | — | — |
| `nearby` | Лопухинский сад | элемент экрана | открывает экран | Площадки рядом (places) | — | — |
| `nearby` | Набережная у ЦПКиО | элемент экрана | открывает экран | Площадки рядом (places) | — | — |
| `nearby` | Все площадки района | элемент экрана | открывает экран | Площадки рядом (places) | — | — |
| `nearby` | Раздел Главная | вкладка | переключает вкладку таб-бара | Главная (home) | — | — |
| `nearby` | Раздел Рядом | вкладка | переключает вкладку таб-бара | Рядом (nearby) | — | — |
| `nearby` | Раздел Создать | вкладка | переключает вкладку таб-бара | Новый момент (create) | — | — |
| `nearby` | Раздел Сообщения | вкладка | переключает вкладку таб-бара | Сообщения (chats) | — | — |
| `nearby` | Раздел Профиль | вкладка | переключает вкладку таб-бара | Профиль (profile) | — | — |
| `walk` | Назад | кнопка «назад» | возвращает к родительскому экрану | Рядом (nearby) | — | — |
| `walk` | Я иду · следить | элемент экрана | показывает подтверждение «Следим за прогулкой» | остаётся на экране | — | — |
| `walk` | Я на площадке | элемент экрана | включает entitlement wifiinfo, системного alert нет | остаётся на экране | — | `com.apple.developer.networking.wifi-info` |
| `walk` | Подключиться к сети площадки | элемент экрана | открывает экран | Сеть площадки по QR (netqr) | — | — |
| `walk` | Обновлять состав | элемент экрана | включает entitlement remotenotif, системного alert нет | остаётся на экране | — | `UIBackgroundModes: remote-notification` |
| `create` | Закрыть | элемент экрана | открывает экран | Главная (home) | — | — |
| `create` | Снять | элемент экрана | спрашивает доступ camera | Камера (camera) | Новый момент (create) | `NSCameraUsageDescription` |
| `create` | Из Фото | элемент экрана | спрашивает доступ photos | Фото (media) | Новый момент (create) | `NSPhotoLibraryUsageDescription` |
| `create` | Раздел Главная | вкладка | переключает вкладку таб-бара | Главная (home) | — | — |
| `create` | Раздел Рядом | вкладка | переключает вкладку таб-бара | Рядом (nearby) | — | — |
| `create` | Раздел Создать | вкладка | переключает вкладку таб-бара | Новый момент (create) | — | — |
| `create` | Раздел Сообщения | вкладка | переключает вкладку таб-бара | Сообщения (chats) | — | — |
| `create` | Раздел Профиль | вкладка | переключает вкладку таб-бара | Профиль (profile) | — | — |
| `camera` | Закрыть | кнопка «назад» | возвращает к родительскому экрану | Новый момент (create) | — | — |
| `camera` | Снять фото | элемент экрана | открывает экран | Главная (home) | — | — |
| `media` | Закрыть | кнопка «назад» | возвращает к родительскому экрану | Новый момент (create) | — | — |
| `media` | Готово | элемент экрана | открывает экран | Главная (home) | — | — |
| `places` | Назад | кнопка «назад» | возвращает к родительскому экрану | Главная (home) | — | — |
| `places` | Площадка у Лопухинского пруда | элемент экрана | открывает экран | Прогулка (walk) | — | — |
| `places` | Площадка на набережной | элемент экрана | открывает экран | Прогулка (walk) | — | — |
| `places` | Площадка для щенков | элемент экрана | открывает экран | Прогулка (walk) | — | — |
| `chats` | Ксения и Трюфель | элемент экрана | открывает экран | Чат (chat) | — | — |
| `chats` | Прогулка у пруда | элемент экрана | открывает экран | Чат (chat) | — | — |
| `chats` | Раздел Главная | вкладка | переключает вкладку таб-бара | Главная (home) | — | — |
| `chats` | Раздел Рядом | вкладка | переключает вкладку таб-бара | Рядом (nearby) | — | — |
| `chats` | Раздел Создать | вкладка | переключает вкладку таб-бара | Новый момент (create) | — | — |
| `chats` | Раздел Сообщения | вкладка | переключает вкладку таб-бара | Сообщения (chats) | — | — |
| `chats` | Раздел Профиль | вкладка | переключает вкладку таб-бара | Профиль (profile) | — | — |
| `chat` | Назад | кнопка «назад» | возвращает к родительскому экрану | Сообщения (chats) | — | — |
| `chat` | Позвонить | элемент экрана | включает entitlement voip, системного alert нет | Звонок догситтеру (call) | — | `UIBackgroundModes: voip` |
| `chat` | Сообщения с аватаром | элемент экрана | включает entitlement commnotif, системного alert нет | остаётся на экране | — | `com.apple.developer.usernotifications.communication` |
| `chat` | Записать голосовое | элемент экрана | спрашивает доступ mic | Голосовое (voice) | Чат (chat) | `NSMicrophoneUsageDescription` |
| `voice` | Закрыть | кнопка «назад» | возвращает к родительскому экрану | Чат (chat) | — | — |
| `voice` | Удалить | кнопка «назад» | возвращает к родительскому экрану | Чат (chat) | — | — |
| `voice` | Отправить | элемент экрана | открывает экран | Чат (chat) | — | — |
| `profile` | Настройки | элемент экрана | открывает экран | Настройки (settings) | — | — |
| `profile` | Найти среди контактов | элемент экрана | спрашивает доступ contacts | Контакты в «Хвостах» (mates) | Контакты в «Хвостах» (mates) | `NSContactsUsageDescription` |
| `profile` | Раздел Главная | вкладка | переключает вкладку таб-бара | Главная (home) | — | — |
| `profile` | Раздел Рядом | вкладка | переключает вкладку таб-бара | Рядом (nearby) | — | — |
| `profile` | Раздел Создать | вкладка | переключает вкладку таб-бара | Новый момент (create) | — | — |
| `profile` | Раздел Сообщения | вкладка | переключает вкладку таб-бара | Сообщения (chats) | — | — |
| `profile` | Раздел Профиль | вкладка | переключает вкладку таб-бара | Профиль (profile) | — | — |
| `settings` | Назад | кнопка «назад» | возвращает к родительскому экрану | Профиль (profile) | — | — |
| `settings` | Прогулки и ответы | элемент экрана | спрашивает доступ push | Настройки (settings) | Настройки (settings) | `aps-environment` |
| `settings` | Обновлять ленту в фоне | элемент экрана | включает entitlement fetch, системного alert нет | остаётся на экране | — | `UIBackgroundModes: fetch` |
| `settings` | Фоновая задача | элемент экрана | открывает экран | Обновление в фоне (refresh) | — | — |
| `settings` | Виджет | элемент экрана | включает entitlement appgroups, системного alert нет | Виджет на экране «Домой» (widget) | — | `com.apple.security.application-groups` |
| `settings` | Вход на сайте | элемент экрана | включает entitlement autofill, системного alert нет | Автозаполнение на сайте (fill) | — | `com.apple.developer.authentication-services.autofill-credential-provider` |
| `settings` | Поделиться в «Хвосты» | элемент экрана | включает entitlement shareext, системного alert нет | Поделиться в «Хвосты» (shareext) | — | `NSExtensionPointIdentifier: com.apple.share-services` |
| `settings` | Замок Face ID | элемент экрана | спрашивает доступ faceid | Замок на ветпаспорте (lock) | Замок на ветпаспорте (lock) | `NSFaceIDUsageDescription` |
| `settings` | Отслеживание | элемент экрана | открывает экран | Реклама вместо подписки (ads) | — | — |
| `widget` | Открыть «Хвосты» | элемент экрана | включает entitlement keychain, системного alert нет | Главная (home) | — | `keychain-access-groups` |
| `fill` | — | — | интерактивных элементов нет | — | — | — |
| `refresh` | Назад | кнопка «назад» | возвращает к родительскому экрану | Настройки (settings) | — | — |
| `refresh` | Проверить задачу | элемент экрана | включает entitlement bgtask, системного alert нет | Главная (home) | — | `BGTaskSchedulerPermittedIdentifiers` |
| `mates` | Назад | кнопка «назад» | возвращает к родительскому экрану | Профиль (profile) | — | — |
| `mates` | Подписаться | элемент экрана | показывает подтверждение «Подписка оформлена» | остаётся на экране | — | — |
| `mates` | Подписаться | элемент экрана | показывает подтверждение «Подписка оформлена» | остаётся на экране | — | — |
| `mates` | Подписаться | элемент экрана | показывает подтверждение «Подписка оформлена» | остаётся на экране | — | — |
| `mates` | Ещё 4 совпадения | элемент экрана | показывает подтверждение «Показаны все 9 совпадений» | остаётся на экране | — | — |
| `mates` | Ссылка-приглашение | элемент экрана | показывает подтверждение «Ссылка скопирована» | остаётся на экране | — | — |
| `mates` | Отправить в переписку | элемент экрана | открывает экран | Сообщения (chats) | — | — |
| `ads` | Закрыть | кнопка «назад» | возвращает к родительскому экрану | Настройки (settings) | — | — |
| `ads` | Продолжить | элемент экрана | спрашивает доступ tracking | Профиль (profile) | Профиль (profile) | `NSUserTrackingUsageDescription` |
| `lock` | Назад | кнопка «назад» | возвращает к родительскому экрану | Настройки (settings) | — | — |
| `vetnote` | Назад | кнопка «назад» | возвращает к родительскому экрану | Профиль питомца (pet) | — | — |
| `vetnote` | Сохранить черновик | элемент экрана | показывает подтверждение «Черновик сохранён» | остаётся на экране | — | — |
| `vetnote` | Править | элемент экрана | показывает подтверждение «Правка всей заметки» | остаётся на экране | — | — |
| `vetnote` | Править 0:04 | элемент экрана | показывает подтверждение «Правка строки 0:04» | остаётся на экране | — | — |
| `vetnote` | Править 0:11 | элемент экрана | показывает подтверждение «Правка строки 0:11» | остаётся на экране | — | — |
| `vetnote` | Править 0:26 | элемент экрана | показывает подтверждение «Правка строки 0:26» | остаётся на экране | — | — |
| `vetnote` | Править 0:35 | элемент экрана | показывает подтверждение «Правка строки 0:35» | остаётся на экране | — | — |
| `vetnote` | Слушать запись | элемент экрана | показывает подтверждение «Воспроизведение 0:41» | остаётся на экране | — | — |
| `vetnote` | Сохранить в карточку | элемент экрана | открывает экран | Профиль питомца (pet) | — | — |
| `vetnote` | Перезаписать наблюдение | элемент экрана | показывает подтверждение «Запись 0:41 переписывается» | остаётся на экране | — | — |
| `vetnote` | Слушать 27 апреля | элемент экрана | показывает подтверждение «Воспроизведение 0:22» | остаётся на экране | — | — |
| `vetnote` | Искать словом по 23 заметкам | элемент экрана | показывает подтверждение «Поиск по заметкам» | остаётся на экране | — | — |
| `vetnote` | 19 мая, 09:15 — Мария Тенищева | элемент экрана | открывает экран | Прививки и обработки (vaccine) | — | — |
| `course` | Назад | кнопка «назад» | возвращает к родительскому экрану | Главная (home) | — | — |
| `course` | Назад 15 секунд | элемент экрана | показывает подтверждение «6:26» | остаётся на экране | — | — |
| `course` | Слушать | элемент экрана | включает entitlement audio, системного alert нет | Экран погас (background) | — | `UIBackgroundModes: audio` |
| `course` | Вперёд 15 секунд | элемент экрана | показывает подтверждение «6:56» | остаётся на экране | — | — |
| `course` | Все 12 | элемент экрана | показывает подтверждение «12 занятий, 4 пройдено» | остаётся на экране | — | — |
| `course` | Скачать занятие | элемент экрана | показывает подтверждение «Занятие в очереди на загрузку» | остаётся на экране | — | — |
| `background` | Назад 15 секунд | элемент экрана | показывает подтверждение «6:26» | остаётся на экране | — | — |
| `background` | Пауза | элемент экрана | показывает подтверждение «Пауза · 6:41» | остаётся на экране | — | — |
| `background` | Вперёд 15 секунд | элемент экрана | показывает подтверждение «6:56» | остаётся на экране | — | — |
| `call` | Выключить микрофон | элемент экрана | показывает подтверждение «Микрофон выключен» | остаётся на экране | — | — |
| `call` | Завершить звонок | кнопка «назад» | возвращает к родительскому экрану | Чат (chat) | — | — |
| `call` | Включить динамик | элемент экрана | показывает подтверждение «Динамик включён» | остаётся на экране | — | — |
| `vaccine` | Назад | кнопка «назад» | возвращает к родительскому экрану | Профиль питомца (pet) | — | — |
| `vaccine` | Добавить в Календарь | элемент экрана | спрашивает доступ calendar | Прививки и обработки (vaccine) | Прививки и обработки (vaccine) | `NSCalendarsFullAccessUsageDescription + NSCalendarsUsageDescription` |
| `vaccine` | Напомнить за сутки | элемент экрана | показывает подтверждение «Напомним за сутки до приёма» | остаётся на экране | — | — |
| `vaccine` | Показать ещё 5 записей | элемент экрана | показывает подтверждение «Ещё 5 записей в паспорте» | остаётся на экране | — | — |
| `vaccine` | Надиктовать заметку о самочувствии | элемент экрана | открывает экран | Заметка о самочувствии (vetnote) | — | — |
| `netqr` | Закрыть | кнопка «назад» | возвращает к родительскому экрану | Прогулка (walk) | — | — |
| `netqr` | Подключиться | элемент экрана | спрашивает доступ hotspot | Сеть площадки по QR (netqr) | Сеть площадки по QR (netqr) | `com.apple.developer.networking.HotspotConfiguration` |
| `netqr` | Вернуться к отметке | элемент экрана | открывает экран | Прогулка (walk) | — | — |
| `shareext` | Отмена | кнопка «назад» | возвращает к родительскому экрану | Настройки (settings) | — | — |
| `shareext` | Сохранить в черновик | элемент экрана | показывает подтверждение «Сохранено в черновик» | Новый момент (create) | — | — |
<!-- @end -->

## Граница «без бэкенда»

<!-- @generated:backendless -->
| Требовало бы сервера | Решение без сервера |
|---|---|
| Лента и профили | Статический контент-пак и локальные действия в прототипе |
| Сообщения и уведомления | Firebase SDK и APNs без собственного API |
| Прогулки рядом | MapKit и статический каталог парков |
<!-- @end -->
