# Хвосты — визуальный аудит

Дата проверки: 14 августа 2026. Все 16 экранов просмотрены в полном размере 376×812 после финального `capture`.

Финальные автоматические результаты: proof 1/1, flows 57/57, lint без расхождений, visual audit без замечаний. Живая HTTP-сборка вернула 200, отдала все 16 экранов без console/page errors и прошла переход Home → Communities → Back.

---

> **Состав концепта изменился после этого прохода.** Проход выше описывает состав на момент проверки — числа в нём оставлены как есть, потому что перечисленные ниже экраны в тот проход не входили.
>
> Добавлены позже: `ads`, `background`, `call`, `codefail`, `course`, `lock`, `mates`, `netqr`, `shareext`, `vaccine`, `vetnote`. Сейчас в концепте **27 экранов** и **22 доступа**; актуальный состав — в генерируемых блоках `02-architecture.md`. Последний прогон `npm run audit -- tails` — чисто.
