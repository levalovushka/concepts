# Двор — дизайн-система (концепт)

Соседская сеть одного дома. Ограничение продукта задаёт визуальный язык: на экране всегда чужие люди, чужие квартиры и чужие цифры — восемнадцать жильцов подъезда, четыре открытые заявки, два счётчика, три корпуса. Значит плотность важнее воздуха, а список важнее карточки-героя. Отсюда серая страница с белыми блоками во всю ширину, инсет 12 вместо 16–20 и строка без хайрлайна внутри карточки.

Визуальный язык взят из набора VK-типа (Base Kit: `vk-style-visual-guide.en.md` + `design-tokens.json`) и **осознанно отстроен**, чтобы не быть копией. Что заменено — в [04-references.md](04-references.md), раздел «Что не копируем».

Системный iOS-хром — статус-бар, permission-алерт, снекбар, home indicator, sheet с грабером, свитч и поля формы — берётся из ядра (`kernel/base.css`, `kernel/ios-chrome.md`) **без единой правки**. Иконки — из спрайта Lucide (`kernel/icons.svg`), своих SVG-путей в экранах нет.

---

## Направление

**Плотный список на серой странице.** Не «уютный двор», не иллюстрации, не тёплая палитра ЖКХ. Продукт про инфраструктуру, поэтому интерфейс выглядит как рабочий инструмент: белая карточка, строка с цветным кружком-иконкой, значение справа, шеврон. Всё, что читается как данные — показания счётчика, идентификатор фоновой задачи, номера квартир в поэтажном плане, журнал задачи — набирается моно (`JetBrains Mono`).

Разделение поверхностей:

Корни экранов, посчитанные грепом по `screens/*.html` (`class="screen …"`): 21 + 2 + 6 = 29.

Один акцент на весь продукт: синий VK `#0077FF`. Им красятся активная вкладка дока, кольцо сторис, первичная кнопка, плашка строки, запрашивающей доступ, «мой дом» на схеме двора и своя квартира в поэтажном плане. Второго акцента нет. Декоративные плитки меню — отдельный набор из восьми пар цветов, он в семантические токены не входит и на другие компоненты не распространяется.

Брендовой капсулы в концепте больше нет: она стояла отдельным рядом под статус-баром и изображала элемент, которого в приложении не существует, — подробнее в [04-references.md](04-references.md). От неё остался только квадрат марки в виджете, `.d-widget-ico`.

Избегаем: капсульных кнопок, large title, segmented control, разделителей внутри карточки, хвостиков у бабблов, эмодзи в интерфейсе, теней как высоты. `box-shadow` в концепте есть, но ни разу не в роли elevation: мягкая тень — только на активном чипе-фильтре; всё остальное — обводки (`.d-ava.xl`, `.d-badge`, `.d-story-plus`, `.d-gridsel`, `.d-plan-me`), внутренние хайрлайны (`.d-search`, `.d-btn.quiet`, `.d-story-in`, круглый `.ph`) и одна системная механика — затемнение вне рамки сканера тенью 2000px. Плюс `drop-shadow` на глифах, которые лежат поверх медиа: метка типа в гриде, пин на схеме, глиф плитки.

---

## Токены

Значения — из `:root` в `concepts/dvor/styles.css` (строки 15–43). Страница итогового файла залочена в светлую тему (`kernel/page.html`: `<html data-theme="light">`), поэтому второго набора значений у концепта нет.

### Цвет

| Токен | Hex | Где |
|---|---|---|
| `--d-page` | `#ECEDF1` | серая страница под белыми карточками |
| `--d-card` | `#FFFFFF` | белая карточка |
| `--d-ink` | `#0B0B0C` | основной текст |
| `--d-sub` | `#6D7885` | второстепенный текст: подписи строк, лид, расшифровка, значение строки |
| `--d-mute` | `#6E7887` | третичный текст, шевроны, неактивная вкладка дока, заголовки секций, плейсхолдеры |
| `--d-dim` | `#99A2AD` | заведён под выключенное — «там, где нажать нельзя». **В `styles.css` ни разу не использован**: `grep -c 'var(--d-dim)'` → `0`. Либо применить, либо убрать |
| `--d-quiet` | `#E8EAF0` | тихая поверхность на серой странице: `.d-btn.quiet`, `.d-field`, `.d-story-add`, `.d-tag.mute` |
| `--d-quiet-in` | `#EFF1F5` | та же тихая поверхность **внутри** белой карточки: `.d-card .d-btn.quiet`, `.d-card .d-chip`, `.d-card .d-search` |
| `--d-line` | `#E1E3E6` | `.d-topline` под шапкой, хайрлайн между колонками статистики, просмотренное кольцо сторис |
| `--d-red` | `#FF3347` | бейдж непрочитанных, пин проблемы на схеме двора |
| `--d-green` | `#4BB34B` | статусная плашка «дом подтверждён» |
| `--d-orange` | `#F8A01C` | только заливка плашек: срок на исходе |
| `--d-orange-ink` | `#A8690A` | тот же смысл текстом — 4.6:1 на белом. Через `var()` берёт только `.d-row-val.warn`; `.d-note.warn`, `.d-tag.warn` и `.d-widget-tag` держат то же значение литералом — расхождения в цвете нет, но токен обходят |
| `--d-dark` | `#0A0A0A` | страница тёмных системных поверхностей |
| `--d-surf` | `#19191A` | карточка на тёмном (`.d-card.night`, `.d-log`) |

Три значения — `#FF3347`, `#4BB34B`, `#F8A01C` — взяты из Base Kit как есть (`accent.red`, `accent.green`, `icon.warning`): это статусные цвета, отстраивать их бессмысленно, красный «непрочитано» должен быть красным. Но оранжевый оставлен **только заливкой**: текстом он давал 2.1:1, поэтому под текст заведён отдельный `--d-orange-ink`.

Шкала серых от первоисточника отошла в одном месте, и намеренно. `--d-sub` `#6D7885` совпадает с `text.subhead`, а вот `icon.secondary` `#99A2AD` через третичную типографику пропускать нельзя: через `--d-mute` идут заголовки секций, мета постов, неактивные чипы и шевроны, а `#99A2AD` на серой странице даёт 2.2:1. Токен переопределён в `#6E7887` (3.2:1), а исходное значение осталось под именем `--d-dim` — для того, что действительно выключено. Отстройка от первоисточника по-прежнему идёт по акценту, гарнитуре и радиусу, а не по шкале серого; это правка контраста, а не стиля.

Акцент приходит из ядра, а не из `:root` концепта — его подставляет сборка из блока `brand` в `concept.json`:

| Токен | Значение | Примечание |
|---|---|---|
| `--accent-fill` | `#0077FF` | заливки внутри устройства; **не меняется** при тёмной теме страницы |
| `--accent` | `#0077FF` / `#529EF5` | хром страницы вокруг устройства; тёмный вариант — на случай, если лок темы снимут |

Внутри экранов используется только `var(--accent-fill)`: интерфейс приложения не должен перекрашиваться от темы страницы-обёртки.

Производные акцента, заданные литералами (нужны альфа-варианты, которых в токенах нет):

Локальные палитры, не входящие в семантику:

- **Плитки меню** — восемь пар «база + фигура»: azure `#3D5AF0`/`#6A3CF5`, fire `#F0431A`/`#FF7A45`, magenta `#7B2EE0`/`#D93FE8`, teal `#0B8577`/`#14B39C`, amber `#C97600`/`#EE9E1F`, red `#D62840`/`#F2536B`, slate `#3F4A59`/`#66768C`, violet `#5B4CE0`/`#8B7BF5`. База пары violet — акцент бренда; это единственная плитка, совпадающая с ним. Вторые фигуры притемнены относительно исходных тонов набора: белый глиф центрирован и всегда заезжает на фигуру, а на светлой заливке (`#FFC062`, `#FF9A6B` и подобных) он выцветал до 1.6:1.
- **Схема двора** — `#DFE6DC` газон, `#CFD6DE` проезд, `#B9C2CE` корпус с подписью `#4A5462`, `rgba(75,179,75,.3)` зона с рамкой пунктиром и подписью `#2C6B2C`, `#2688EB` точка «вы здесь» (системный синий геопозиции, а не бренд — так его читает пользователь), `#3C4653` подпись схемы на белой плашке.
- **Этикетка счётчика** — `#F7F5EE` бумага, `#E4DFCE` рамка, `#6B6244` шапка, `#23252B` разряд, `#B5121B` дробная часть, `#8A7F5F` подвал.
- **Поэтажный план** — `#E7EAF0` квартира, акцент — своя, `rgba(91,76,224,.22)` — сосед из контактов.
- **Тёмные поверхности** — светлые варианты текста: `#F2F3F5` заголовок, `#9AA3B2` подпись, `#8B93A5` ключ, `#E1E3E6` значение, `#7A828F` заголовок секции, `#6FDB8F` успешная строка журнала.
- **Баббл** — `#6B7280` время во входящем, `#5C5A7A` в исходящем: на сиреневом `#E4E0FF` серый терял контраст.

### Типографика

`--face` — `Manrope` (400/500/600/700/800), `--mono` — `JetBrains Mono` (400/500). Подключение — через `brand.fonts` в `concept.json`.

| Роль | Класс | Размер / вес |
|---|---|---|
| Заголовок экрана | `.d-top-title` | 22 / 28, 800, tracking −0.5 |
| Заголовок экрана, узкий | `.d-top-title.sm` | 17 / 22, 700, −0.3 |
| Заголовок героя | `.d-hero-t` | 22 / 28, 800, −0.5 |
| Пояснение героя | `.d-hero-d` | 15 / 21, 400, `--d-sub` |
| Заголовок секции | `.d-sec` | 13 / 16, 700, uppercase, tracking +0.2, `--d-mute` |
| Лид секции | `.d-lead` | 14 / 19, 400, `--d-sub` |
| Заголовок строки | `.d-row-title` | 15 / 20, 600 |
| Подпись строки | `.d-row-sub` | 14 / 18, 400, `--d-sub` |
| Значение строки | `.d-row-val` | 15, 400, `--d-sub` (варианты `.accent` 600, `.warn` 600 на `--d-orange-ink`) |
| Автор поста | `.d-post-name` | 15 / 19, 600 |
| Время и источник поста | `.d-post-meta` | 13 / 16, 400, `--d-mute` |
| Текст поста | `.d-post-body` | 15 / 21, 400 |
| Тег | `.d-tag` | 12, 600 |
| Действие под постом | `.d-act` | 14, 600, `--d-sub` |
| Кнопка | `.d-btn` | 16, 700, −0.2 |
| Кнопка малая | `.d-btn.sm` | 15, 700 |
| Чип-кнопка | `.d-mini` | 12, 700 |
| Фильтр | `.d-chip` | 15, 500 → `.is-on` 600 |
| Счётчик в фильтре | `.d-chip-n` | 12, 700 |
| Таб | `.d-tab` | 15, 400 → `.is-on` 700 |
| Подпись плитки | `.d-tile-label` | 12 / 15, 400 |
| Подпись сторис | `.d-story-label` | 12 / 15, 500 |
| Баббл | `.d-bub` | 15 / 20, 400 |
| Имя в баббле | `.d-bub-name` | 13 / 16, 700, акцент |
| Время в баббле | `.d-bub-time` | 11, 400, `#6B7280`; в исходящем — `#5C5A7A` |
| Расшифровка голосового | `.d-script` | 14 / 19, 400, `--d-sub` |
| Сноска | `.d-note` | 13 / 18, 400, акцент (варианты `.warn` — `#A8690A`, `.mute` — `--d-sub`, `.center`) |
| Ключ пары | `.d-kv-k` | 14, 400, `--d-sub` |
| Значение пары | `.d-kv-v` | 14, 500, **mono**; вариант `.text` — обратно на `--face` 400, применён 4 раза (см. ниже) |
| Число статистики | `.d-stat-n` | 17, 800 |
| Подпись статистики | `.d-stat-l` | 12 / 15, 400, `--d-mute` |
| Разряд счётчика | `.d-digit` | 19, 500, **mono** |
| Шапка этикетки | `.d-tape-top` | 11, 700, uppercase, tracking +0.4 |
| Подвал этикетки | `.d-tape-foot` | 12, 400, **mono** |
| Номер этажа | `.d-floor-n` | 11, **mono** |
| Номер квартиры | `.d-flat` | 10, **mono** |
| Строка журнала задачи | `.d-log-line` | 11 / 17, **mono** |
| Корпус на схеме | `.d-plan-bld` | 10, 800 |
| Зона на схеме | `.d-plan-zone` | 9, 700 |
| Подпись схемы | `.d-plan-cap` | 11, 700 |
| Плейсхолдер поиска | `.d-search-ph` | 15, `--d-mute` |
| Подпись поля | `.d-field-lab` | 13, `--d-mute` |
| Значение поля | `.d-field-val` | 16, 600 |
| Поле композера | `.d-composer-field` | 15, `--d-mute` |
| Часы локскрина | `.d-lock-time` | 74 / 80, 300, tracking −2 |
| Дата локскрина | `.d-lock-date` | 15, 600 |
| Имя приложения в пуше | `.d-notif-app` | 12, 700, tracking +0.2 |
| Заголовок пуша | `.d-notif-name` | 15, 700 |
| Текст пуша | `.d-notif-text` | 14 / 19, 400 |
| Имя виджета | `.d-widget-name` | 11, 800, uppercase, tracking +0.3 |
| Главная строка виджета | `.d-widget-big` | 15 / 19, 700 |
| Подпись виджета | `.d-widget-sub` | 12 / 16, 400 |
| Бейдж непрочитанных | `.d-badge` | 11, 800 |
| Подсказка камеры | `.d-shot-hint` | 13, 600 |
| URL в Safari | `.d-safari-url` | 15 |
| Поле в Safari | `.d-safari-input` | 16 (16 — чтобы iOS не зумил поле при фокусе) |
| Подсказка автозаполнения | `.d-keysug` | 14, 600 |
| Счётчик у шеврона «назад» | `.d-back-n` | 15, 600 |

Правило: моно получает всё, что читается как данные — показания, номер счётчика, номер квартиры, идентификатор задачи, время в журнале, контейнер App Group, ключ Keychain.

Под исключение заведён класс `.d-kv-v.text`: он возвращает `--face` 400 там, где значение — не данные, а проза, потому что моно рвёт русский текст провалами между словами. **Теперь он применён** — четыре раза из пятнадцати `.d-kv-v`: «жильцы с подтверждённым домом» и «старший по подъезду, 3 апреля» на `passwords`, «до 25 апреля» и «14–17 апреля» на `settings`. Остальные одиннадцать — данные и набраны моно правомерно: `Dvor-Guest`, `WPA2`, `dvor-2026`, `app.dvor.refresh`, `fetch + remote-notification`, `04:12`, `group.app.dvor`, `Keychain`, `18`.

Шкала весов уехала вверх относительно первоисточника: там Title 1 — 24/28 Semibold, здесь заголовок экрана — 22/28 **ExtraBold**. Это часть отстройки: Manrope в 800 даёт другой силуэт строки, чем SF Pro Semibold.

### Радиусы

| Токен / значение | Где | Base Kit |
|---|---|---|
| `--d-r-ctl` `8px` | кнопка, поле, `.d-field` | 10pt iOS — **заменено** |
| `--d-r-card` `16px` | белая карточка | 12pt paper — **заменено** |
| `--d-r-tile` `12px` | плитка меню | — |
| `18px` | баббл, `.d-notif`, пилюля `.d-search` и `.d-composer-field` | — |
| `13px` | чип-фильтр | пилюля |
| `12px` | `.d-plan`, `.d-log`, `.d-fillbar` | — |
| `10px` | `.d-bul`, этикетка счётчика, `.d-safari-url` | — |
| `7px` | `.d-tag` | — |
| `6px` | `.d-widget-ico`, `.d-widget-tag`, зона на схеме | — |
| `5px` | `.d-mini`, подпись схемы | — |
| `4px` | разряд счётчика | — |
| `3px` | квартира в поэтажном плане, корпус на схеме, площадка QR | — |
| `0` | ячейка медиа-грида | — |
| `50%` | все аватары, кружки схемы и плеера | 50% — совпадает |
| `14px` / `20px` / `22px` | иконка приложения, рамка сканера, виджет | системные величины iOS |

Капсульных кнопок нет ни одной — это первое, что отличает раскладку и от Apple, и от `--r-btn: 9999px` в ядре.

### Отступы и плотность

| Токен / значение | Где |
|---|---|
| `--d-ins` `12px` | инсет контента по горизонтали — единственное боковое поле в концепте |
| `--d-gap` `7px` | зазор между белыми карточками |
| `4px` | боковой вылет карточки (`.d-card` не во всю ширину, оставляет 4px серого) |
| `1px` | гаттер медиа-грида; `2px` — зазор этажей |
| min-height `48px` | `.d-row` |
| height `48px` | `.d-btn` (`.sm` — 38) |
| height `36px` | `.d-search`, `.d-composer-field`, слот дока, `.d-safari-url` |
| height `26px` | чип-фильтр; `24px` — `.d-mini` и `.d-pin-x`; `22px` — квартира в плане |
| `52px` | плитка меню; `66px` — кольцо сторис; `44px` — аватар в строке (`sm` 32, `lg` 52, `xl` 72) |
| `calc(52px + var(--safe-bottom))` | высота дока |

Шаг сетки — 1 / 2 / 4 / 6 / 7 / 8 / 10 / 12 / 14 / 16 / 18 / 20 / 24 / 26. Он **не** кратен 4 сплошь: 7 (зазор карточек), 9, 11, 13 (внутренние паддинги чипа и поиска) взяты, чтобы попасть в нужную оптическую плотность, а не чтобы соблюсти шкалу. Первоисточник даёт 0/2/4/6/8/12/16/20/24/32/40 — она сохранена там, где ничего не мешало, и нарушена сознательно там, где мешала.

Инсет 12 — прямо из Base Kit («base content/card padding: 12pt»), и он же на шаг плотнее Apple (16–20). Это единственное место, где следование первоисточнику само по себе работает как отстройка от системного языка.

### Safe areas

Значения — из ядра, не переопределяются: `--safe-top` `47px`, `--safe-bottom` `34px` (iPhone X-class 375×812). Концепт обращается к ним в семи правилах `styles.css`:

| Правило | Что делает |
|---|---|
| `.d-safe` | `padding-top: calc(var(--safe-top) + 10px)` — верхний отступ прокручиваемой области. В разметке на **18 экранах** |
| `.d-head` | тот же отступ + белая заливка + `flex: none` — шапка, прижатая к верху. На **4 экранах**: `chat`, `post`, `problem`, `chronicle` |
| `.d-dock` | высота `calc(52px + var(--safe-bottom))` и нижний паддинг |
| `.d-composer` | нижний паддинг `calc(8px + var(--safe-bottom))` |
| `.d-keybar` | то же — подсказка автозаполнения над клавиатурой |
| `.body-scroll > .d-cta:last-child`, `.screen > .d-cta:last-child` | кнопка последним блоком экрана: без этого её срезает нижняя кромка (поймано на `code`) |

`.d-safe` и `.d-head` — новые классы. До них отступ под статус-бар прописывался инлайном на каждом экране по-своему: 19 раз, и величины не совпадали. Инлайнового `safe-top` осталось **шесть** вхождений: пять на чужих и системных поверхностях, где своя раскладка (`fill` — бар Safari, `lockscreen`, `scan`, `shoot`, `widget`), и одно неоправданное — `voice` рисует ту же белую шапку с закреплённым сообщением, что и `chat`, но инлайном и с `+4px` вместо `+10px`. Это остаток, а не решение: шапку `voice` надо подвести под `.d-head`.

Отступ снизу везде идёт от `--safe-bottom`: `.home-ind` из ядра позиционирован абсолютно и места не занимает, поэтому полагаться на него нельзя.

---

## Требования HIG, которые прототип обязан соблюдать

---

## Каталог компонентов

Всё своё — с префиксом `d-`. Всё системное — из ядра, без переопределений.

**Каркас экрана.** Три класса, заведённые вместо инлайновых повторов:

Этими тремя классами закрыт основной инлайновый долг разметки: было **167** атрибутов `style=` на 29 экранов, стало **133** (экранов тогда было 29, сейчас 30). Остаток — не единый: 32 инлайна держат высоты полосок звуковой волны (`chat` 14, `voice` 18), 10 — геометрию схемы двора на `yard`, 19 — цвет текста (`background` 10, `widget` 4, `lock` 3, `lockscreen` и `verify` по одному), 11 — заливки (из них 7 — чужие иконки на домашнем экране `widget`). Геометрии данных место в разметке: полоса волны — это значение, а не стиль. Цветам на тёмном — не место, их надо свести в правила `.d-night`; это открытый хвост.

**Марка приложения.** `.d-widget-ico` — квадрат 20 r6 акцентной заливкой с глифом дома. Одно вхождение, в шапке виджета на `widget`. Всё, что осталось от брендовой капсулы: больше приложение себя нигде не подписывает.

**Шапка экрана.** `.d-top` — ряд заголовка. `.d-top-title` — 22/800 слева (`.sm` — 17/700, применён на 16 экранах из 29 — вложенные экраны носят короткий заголовок). `.d-top-caret` — шеврон-дропдаун рядом с заголовком, там где заголовок переключаемый (`home` — выбор дома, `yard` — выбор корпуса). `.d-top-acts` — правая группа. `.d-icon` — акцентная аутлайн-иконка 22 со штрихом 1.9 (`.mute` — серая, `.sm` — 18). `.d-back` — шеврон «назад» с `.d-back-n`: числом непрочитанных на `chat` или словом «Закрыть» на `problem` и `ads`. `.d-topline` — хайрлайн под шапкой там, где ниже идёт прокрутка.

**Карточки.** `.d-card` — белый блок r16 с зазором 7 и вылетом 4 по бокам (`.pad` — с инсетом внутри; `.night` — тёмная поверхность `#19191A`). `.d-sec` — заголовок секции 13/700 uppercase над карточкой. `.d-lead` — лид секции под заголовком, три вхождения.

Тихие поверхности внутри карточки берут отдельный тон: `.d-card .d-btn.quiet`, `.d-card .d-chip` и `.d-card .d-search` переключаются с `--d-quiet` на `--d-quiet-in`. Причина простая: серое на серой странице и серое на белой карточке — это два разных серых, одним значением обе подложки не обслужить.

**Строки.** `.d-row` — строка min-height 48 (`.top` — выравнивание по верху для многострочных). `.d-row-main` — растягивающаяся середина. `.d-row-title` / `.d-row-sub` — заголовок и подпись, по умолчанию в одну строку с эллипсисом, `.wrap` разрешает перенос (26 вхождений на `.d-row-sub`, 6 на `.d-row-title`). `.d-row-val` — значение справа (`.accent`, `.warn`). `.d-caret` — шеврон 8×14. Хайрлайнов между строками внутри карточки нет — плотность держится на паддингах.

**Плашка строки.** `.d-bul` — квадрат 36 r10 с глифом 19. Цвет значит ровно одно, и это правило единственное во всей системе, которое разделяет строки по смыслу:

| Вариант | Вид | Смысл | Вхождений |
|---|---|---|---|
| `.d-bul` | тинт акцента 10%, акцентный глиф | обычная строка списка | **18** |
| `.d-bul.accent` | насыщенная заливка акцентом, белый глиф | строка **запрашивает доступ** — несёт `data-ask` или `data-activate` | **10** |
| `.d-bul.orange` | `--d-orange`, белый глиф | статус: срок на исходе | **4** |
| `.d-bul.green` | `--d-green`, белый глиф | статус: подтверждено | **2** |

Итого 34 вхождения. Все десять `.accent` проверены грепом: каждая сидит на узле с `data-ask` / `data-activate` (`home`, `post`, `problem`, `chat`, `guest`, `meters`, `passwords`, `settings` ×3). До этой правки акцентом красилось всё подряд — 19 плашек из 32, — и цвет не различал ничего; модификатор `.mute` (серая плашка) удалён вовсе, его роль теперь у базового тинта.

**Аватары.** `.d-ava` — круг 44 (`.sm` 32, `.lg` 52, `.xl` 72 с белой обводкой 3). `.d-stack` — стопка аватаров 24 с наложением −7 и белой обводкой 2 (общие контакты в `menu`). `.d-media` — полноширинный носитель медиа под текстом поста, высота 190.

**Чипы-фильтры.** `.d-chips` — прокручиваемый ряд. `.d-chip` — пилюля h26 r13 на `#E3E5EC` (в карточке — `--d-quiet-in`), текст 15/500 `--d-mute`; `.is-on` — белая пилюля с акцентным текстом 600 и мягкой тенью. `.d-chip-n` — счётчик в чипе на `--d-mute`. Это осознанная замена segmented control: ни рамки, ни трека, ни движущегося индикатора. Неактивный чип получил собственную поверхность не для красоты: голым серым текстом он читался как выключенный, хотя нажимается.

**Табы.** `.d-tabs` / `.d-tab` / `.d-tab-bar` — подчёркивание во всю ширину таба (а не по ширине текста), 2px акцентом. Стоят на `profile` («Во дворе» / «Обмен»).

**Сторис.** `.d-stories` — прокручиваемый ряд. `.d-story-ring` — акцентное кольцо 66 с паддингом 3 (`.seen` — серое `--d-line`). `.d-story-in` — круглый носитель медиа с белой внутренней обводкой 2. `.d-story-add` — своя ячейка на `--d-quiet` с `.d-story-plus` — акцентным кружком 22 в правом нижнем углу. `.d-story-label` — подпись 12/500 в одну строку.

**Пост.** `.d-post` — тело поста. `.d-post-head` — аватар, имя `.d-post-name`, метаданные `.d-post-meta`. `.d-post-body` — текст 15/21 (объявлен вариант `.dim`, приглушённый; в разметке не применён — см. «Мёртвые модификаторы»). `.d-tagrow` / `.d-tag` — теги темы r7: тинт акцента (`.warn` — оранжевый тинт с `#A8690A`, `.mute` — серый `--d-quiet`). `.d-acts` / `.d-act` — ряд действий 14/600 с иконками 19 (`.is-on` — акцент, `.last` — прижат вправо). Подписей «нравится» / «комментарий» нет, только иконка и число.

**Кнопки.** `.d-btn` — h48 r8, акцентная заливка, 16/700 (`.quiet` — тихая `--d-quiet` с внутренним хайрлайном `#D9DCE4`, `.sm` — h38/15, `.night` — белая 16% на тёмном). `.d-btnrow` — две кнопки в ряд равной ширины. `.d-cta` — контейнер главной кнопки экрана, инсет 16 = 4 вылета карточки + 12 внутренних, чтобы левый край кнопки совпал с текстом внутри карточки. Отдельное правило `.body-scroll > .d-cta:last-child` добавляет `--safe-bottom`: кнопка последним блоком экрана иначе срезается нижней кромкой. `.d-mini` — чип-кнопка h24 r5 12/700 на тинте акцента («Тема» на `home`, «Копировать» ×3 на `passwords`; `.on` — насыщенная заливка). Серая заливка здесь давала 1.09:1 к белой карточке, поэтому тинт, а не `--d-quiet`.

**Поиск и ввод.** `.d-search` — пилюля h36 r18 с лупой 17 и плейсхолдером `.d-search-ph`: **белая с хайрлайном** на серой странице (`chats`) и `--d-quiet-in` без хайрлайна внутри карточки (`neighbors`). Заливка поля не может совпадать с цветом страницы — иначе поля просто нет. `.d-field` — поле min-height 46 r8 на `--d-quiet` с подписью `.d-field-lab` и значением `.d-field-val`. `.d-fieldrow` — поля в ряд; `.d-field.cc` — узкая ячейка 74 (`phone` — код страны, `manual` — номер дома).

**Док.** `.d-dock` — навешивается **вместе** с `.tabbar` из ядра: раскладку, blur и safe area даёт ядро, цвет и размеры иконок — концепт. Четыре слота без подписей, аутлайн-иконки 26 со штрихом 1.9. `.d-dock > .tab.is-on` перекрашивает активную вкладку в акцент и снимает заливку глифа (иначе ядро красит её в белый — см. дефект 2 в [05-visual-audit.md](05-visual-audit.md)). `.is-off` гасит вкладку в сценарном срезе. `.d-badge` — красная пилюля непрочитанных, посаженная на угол иконки, с белой обводкой 2. Селекторы висят на прямых детях, а не на `.tab`, чтобы сборщик мог заменить класс на `is-off`, не разрушив раскладку.

**Медиа-грид.** `.d-grid` — 3 колонки, гаттер 1px, радиус 0. `.d-gridcell` — квадратная ячейка. `.d-gridmark` — метка типа в правом верхнем углу с drop-shadow. `.d-gridsel` — акцентный кружок выбора 20 с белой обводкой в левом верхнем. Стоит на `chronicle` и на вкладке `profile`.

**Данные, нарисованные кодом.** `.d-plan` + `.d-plan-road` / `-bld` (`.mine`) / `-zone` / `-me` / `-pin` / `-cap` — схема двора 208px. `.d-tape` + `.d-tape-top` / `.d-digits` / `.d-digit` (`.frac`) / `.d-tape-foot` — этикетка счётчика с разрядным окном. `.d-floors` + `.d-floor` / `.d-floor-n` / `.d-flat` (`.here`, `.joined`) — поэтажный план подъезда, `flex-direction: column-reverse`, чтобы первый этаж был снизу. Подробнее — раздел «Медиа» ниже.

**Служебные блоки.** `.d-note` — сноска 13/18 с иконкой (`.warn` — 1 вхождение, `.mute` — 8, `.center` — 3). **21 вхождение на 15 экранах.** Это уже не объяснение доступа, а короткая подпись состояния: «Без камеры — имя и пароль ниже», «Без календаря — событие останется здесь». Медиана — 36 знаков, максимум — 68 (юридическая сноска на `phone`). Соответственно и правило другое, чем раньше: сноска стоит там, где надо назвать **что остаётся без доступа**, а не на каждом экране с доступом — на `join`, `verify`, `passwords`, `settings`, `widget`, `meters` её нет вовсе, потому что там результат отказа виден по самой строке. `.d-kv` / `.d-kv-k` / `.d-kv-v` — пара ключ-значение с моно справа (`.text` возвращает пропорциональный шрифт, 4 вхождения). `.d-stat` / `.d-stat-col` / `.d-stat-n` / `.d-stat-l` — ряд статистики с вертикальными хайрлайнами между колонками. `.d-hero` / `.d-hero-t` / `.d-hero-d` — центрированный блок заголовка на экранах без списка сверху (`phone`, `code`, `lock`, `ads`, `profile`; на `fill` от группы взят только `.d-hero-t`).

**Тёмные системные поверхности.** Камера: `.d-shot` / `.d-shot-view` / `.d-shot-bar` / `.d-shot-slot` / `.d-shutter` (68 с ореолом 4) / `.d-shot-hint`. Сканер: `.d-frame` — рамка 208 r20 с обводкой 2 и внешней тенью 2000px, дающей затемнение вне кадра; `.d-frame-code` / `.d-frame-code i` / `i.off` — имитация QR: сетка **21×21** модуль по 6px (441 `<i>`, из них 218 с классом `off` — белые), на белой площадке с полем и тремя якорными квадратами по углам. Сетка 7×7 читалась как россыпь квадратиков, а не как код. Локскрин: `.d-lock` / `.d-lock-time` (74/300) / `.d-lock-date` / `.d-notif` + `-head` / `-app` / `-time` / `-name` / `-text` (`.plain` — вариант без entitlement). Домашний экран: `.d-springboard` / `.d-apps` / `.d-appicon` / `.d-appicon-sq` (`.mine`) / `.d-appicon-l`; виджет `.d-widget` + `-top` / `-name` / `-big` / `-sub` / `-tag`. Журнал: `.d-log` / `.d-log-line` (`.ok`). Автозаполнение: `.d-fillbar` / `.d-fillbar-key`, `.d-safari` / `-bar` / `-url` / `-form` / `-input` (`.focus`, `.dots`), `.d-keybar` / `.d-keysug`.

**Мёртвые модификаторы.** Было шесть, остался **один**. Пять удалены из `styles.css` (`.d-icon.ink`, `.d-card.flat`, `.d-btn.ghost`, `.d-btn.line`, `.d-bul.red`), шестой — `.d-kv-v.text` — наоборот, применён в разметке четыре раза. Не применён ни разу только `.d-post-body.dim`: вариант заведён под цитаты и продолжения, а на `post` второй абзац набран тем же `.d-post-body` с утилитой `.mt8`.

Отдельная проверка, тоже вручную: `--d-dim` объявлен и не использован (см. таблицу цвета). Токены линтер не проверяет вовсе.

Отдельно: `.is-off` в разметке экранов тоже отсутствует, но он живой — его навешивает сборка при гашении вкладки в сценарном срезе (`scripts/build.mjs`, строка 102).

**Из ядра, без правок.** Каркас: `.device`, `.status`, `.screen` / `.is-on`, `.body-scroll`, `.home-ind`. Sheet: `.sheet` / `.sheet.light` / `.sheet-body` / `.grabber`. Оверлеи: `.sysask` (permission-алерт), `.snackbar`. Контролы: `.switch` (три штуки на `settings`: push, faceid, tracking — отражают реальное состояние доступа). Регистрация: `.auth-mark`, email-поле и общие состояния формы. Медиа: `.ph` / `.ph.on-dark`. Утилиты: `.tap` (79 обёрток), `.push` (`margin-top:auto`, 12 экранов — отжимает кнопку экрана к низу), `.mt4 … .mt24` (14 вхождений на 10 классах), `.perm-hidden`. Иконки: `.ico-svg` + `<use href="#i-…">` из спрайта Lucide.

---

## Медиа: только плейсхолдер

Правило платформы: визуал в концептах не делаем. Везде, где по замыслу фотография — стоит `.ph` из ядра, серое поле с иконкой камеры (`.ph.on-dark` на тёмной подложке). Стоковых снимков в концепте нет, генераторов картинок нет, `concepts/dvor/media.mjs` создаёт каталог `assets/media` и ничего в него не пишет — файл оставлен как точка расширения, не как генератор.

**Что считается исключением.** Данные, нарисованные кодом, — это содержимое экрана, без которого фича непонятна, а не визуал:

| Что | Классы | Экран | Почему не плейсхолдер |
|---|---|---|---|
| Схема двора | `.d-plan`, `-road`, `-bld`, `-zone`, `-me`, `-pin`, `-cap` | `yard` | На ней видно, что проверяет геопозиция: границы двора, точка «вы здесь», расстояние до подъезда. С серым полем вместо схемы фича «пропуск по месту» перестаёт читаться, а отказ в доступе — перестаёт быть наблюдаемым: смысл fallback в том, что точка и расстояния исчезают, а схема остаётся |
| Этикетка счётчика | `.d-tape`, `.d-tape-top`, `.d-digits`, `.d-digit`, `.frac`, `.d-tape-foot` | `meters` | Разрядное окно с красной дробной частью — то, что пользователь переписывает. Это и есть данные экрана: без цифр непонятно, что именно обновляет тихий пуш |
| Поэтажный план подъезда | `.d-floors`, `.d-floor`, `.d-floor-n`, `.d-flat`, `.here`, `.joined` | `neighbors` | Fallback отказа в контактах: когда список совпадений исчезает, поиск соседа идёт по номеру квартиры. План — единственное, что остаётся работать, поэтому он обязан быть настоящим |
| QR-код в сканере | `.d-frame-code`, `i`, `i.off` | `scan` | Цель наведения. Серое поле в рамке сканера означало бы, что сканировать нечего |

Границу можно проверить одним вопросом: если заменить это серым полем, фича останется понятна? Для аватара, кадра в посте и сетки хроники — да, останется. Для схемы двора, разрядного окна и поэтажного плана — нет.

---

## Экран → визуальное решение

Копирайт на всех экранах переписан по одному правилу: **подзаголовок несёт данные, а не пользу**. «42 снимка за апрель» вместо «соберём хронику двора»; «18 жильцов, 4 сейчас в сети» вместо обещания. Имена технологий из интерфейса убраны — кроме `background` и `widget`, где технический журнал и есть содержание экрана. Сноски отказа сокращены до фразы: «Без камеры — фото из медиатеки».

### Вход и пропуск в дом (5 экранов, доступов на первых двух нет)

| Экран | Решение |
|---|---|
| `phone` | Знак `.auth-mark` из ядра, `.d-hero-t` «Вход в «Двор»», `.d-hero-d` «Код придёт в SMS. Номер закреплён за вашей квартирой.», `.d-fieldrow` с узкой ячейкой `.d-field.cc` «+7» и номером, `.d-note.mute` «Номер виден только соседям подъезда.», `.d-cta` «Получить код» и юридическая сноска `.d-note.mute.center` под ней |
| `join` | `.d-lead` «Писать в ветку дома можно после проверки.», найденный дом строкой со значением «найден», затем секция **«Две проверки»** — две строки `.d-row.top` с базовыми плашками: «Вы в границах двора / 150 м от дома» и «Телефон в домашней сети / Сеть Polevaya-12». Ниже `.d-cta` «Я дома — проверить» (`data-ask="location"`) и `.d-btn.quiet` «Ввести адрес вручную» равнодоступной кнопкой. Сноски нет: обе проверки названы строками |
| `verify` | Результат сверки как данные, без плашек и иконок: «Сеть → Polevaya-12» и «В профиле дома → Polevaya-12» акцентными значениями, «Совпало → да», ниже вторая карточка «Граница двора → 150 м», «До дома → 38 м». `.d-cta` «Подтвердить дом» активирует `wifiinfo`. Ни карты, ни спиннера — только то, что сверялось |
| `manual` | Fallback отказа в геопозиции: `.d-note` «Без геопозиции адрес проверит старший по подъезду» (появляется по `data-show-denied`), `.d-lead` «Заявку смотрит старший по подъезду.», **четыре** `.d-field` в двух рядах (улица + дом, квартира + подъезд), `.d-cta` «Отправить на проверку», `.d-note.mute` «Проверка до суток. До неё ветка дома — только чтение.» |

### Дом (5 экранов)

| Экран | Решение |
|---|---|
| `home` | Заголовок «Полевая, 12» с `.d-top-caret`, ряд сторис (своя ячейка `.d-story-add` первой), `.d-chips` со счётчиком объявлений, карточка-строка **«Хроника двора / 42 снимка за апрель»** с акцентной плашкой и `data-ask="photos"`, сноска отказа под ней, затем лента `.d-post`: объявление УК с тегами «Вода» / «Весь дом» и чипом `.d-mini.on` «Тема», заявка соседки с `.d-media`. Внизу `.d-btn.quiet` «Хроника двора» и док |
| `post` | Один пост во весь экран под `.d-head`: два абзаца `.d-post-body`, три тега, ряд действий, строка **«Следить за темой / Уведомление, когда УК ответит»** с акцентной плашкой (`data-ask="push"`) и сноской «Без уведомлений — тема с точкой в ленте», секция «Обсуждение · 12 ответов» с двумя ответами строками, внизу «Пожаловаться на объявление» базовой плашкой |
| `problem` | Sheet во всю высоту: `.d-head` с «Закрыть», `.d-chips` категорий, `.d-field` с описанием, карточка «Место → 3 подъезд, 1 этаж» и «Видно соседям / 18 жильцов, 3 подъезд», карточка-строка **«Фото с места / пока не добавлено»** (`data-ask="camera"`), `.d-cta` «Отправить заявку» |
| `shoot` | Тёмная системная камера: `.d-shot-view` с `.ph.on-dark`, подсказка `.d-shot-hint` «Дверь и доводчик в кадре», `.d-shot-bar` с `.d-shutter` 68 и двумя слотами |
| `chronicle` | `.d-head`, `.d-lead` «42 снимка за апрель, радиус 150 м от дома», две группы по дате (`.d-sec` «12 апреля · субботник», «6 апреля · двор после ветра»), `.d-grid` с `.d-gridsel` на отмеченных и `.d-gridmark` на видео, `.d-cta` «Добавить 6 кадров». Сноски нет: то, что библиотека обойдена локально, сказано лидом через число |

### Двор (6 экранов)

| Экран | Решение |
|---|---|
| `yard` | Заголовок «Двор» с `.d-top-caret`, схема двора `.d-plan` 208px: три корпуса (свой — акцентом), проезд, две зоны с подписями «площадка» / «парковка», точка «вы здесь», красный пин, подпись «Полевая, 12 · 150 м · метка — заявка». Сноска fallback «Без геопозиции — точки «вы здесь» нет». Ниже «Сервисы двора» — четыре строки: гостевая сеть (базовая плашка), счётчики (зелёная плашка, значение «до 25 апреля» оранжевым), события (оранжевая плашка), мусорные баки |
| `guest` | Три `.d-kv` (сеть, шифрование, «Действует → до 30 апреля»), строка **«Сканировать QR с лавочки / Код меняют раз в месяц»** акцентной плашкой, `.d-cta` «Подключиться к Dvor-Guest» (`data-ask="hotspot"`). Двухуровневый fallback фразами: «Без камеры — имя и пароль ниже», «Пароль вводить не нужно» / `.d-note.warn` «Сеть придётся выбрать руками», и карточка с именем и паролем + «Настройки → Wi-Fi → Dvor-Guest». Единственное вхождение `.d-note.warn` в концепте |
| `scan` | Тёмный сканер: `.d-frame` 208 r20 с внешней тенью 2000px, внутри `.d-frame-code` — QR сеткой 21×21, подсказка «Наведите на QR-код», под рамкой `.d-btn.night` «Распознано: Dvor-Guest» |
| `meters` | Строка «Срок показаний / до 25 апреля» с оранжевой плашкой и значением «6 дней», две `.d-tape`: холодная вода (семь разрядов, два последних красные) и электричество (шесть), под каждой предыдущее показание моно. Строка **«Обновление в фоне / последний запуск 04:12»** (`data-activate="remotenotif"`), `.d-cta` «Сохранить показания» |
| `background` | Тёмная поверхность: `.d-card.night` с тремя `.d-kv` (`app.dvor.refresh`, `fetch + remote-notification`, `04:12`), затем `.d-log` — пять строк журнала моно с временем и подсвеченными значениями (`+2`, `25 апреля`), строка «Проверить задачу / зарегистрирована», сноска «Info.plist · app.dvor.refresh». Один из двух экранов, где имя технологии в интерфейсе оставлено намеренно: журнал и есть содержание экрана |
| `events` | `.d-chips` по месяцам, три события `.d-row.top`: субботник с оранжевой плашкой, кнопкой `.d-btn.sm.quiet` «Добавить в Календарь» (`data-ask="calendar"`) и парой сносок «В календаре 12 апреля» / «Без календаря — событие останется здесь»; собрание с тегом «нужен кворум» и кнопкой «Голосовать»; опрессовка с тегом «весь дом» |

### Меню и настройки (9 экранов)

---

## Четыре ловушки CSS и как они закрыты в этом концепте

Три ловушки описаны в `CLAUDE.md` платформы, четвёртая найдена здесь. Ни одну из них не ловят тесты — только линтер (первую) и глаза на PNG.

### 1. Коллизия имён с ядром

Класс, объявленный с нуля и в `kernel/base.css`, и в `styles.css` концепта, молча ломает страницу: `styles.css` подключается позже и переопределяет то, что не собирался.

**Закрыто по построению:** все свои классы имеют префикс `d-`, а в `base.css` такого префикса нет ни у одного класса. Коллизия невозможна, а не «мы проверили и её нет».

Единственные «голые» селекторы в `styles.css` — модификаторы внутри своего контекста (`.d-card.night`, `.d-dock > .tab.is-on`, `.d-ava .ph`, `.d-frame-code i.off`) и одна страховка `.screen:not(.is-on)` — она специально написана как `:not()`, чтобы линтер не счёл её объявлением класса `.screen` с нуля.

### 2. Шорткат `background` сбрасывает `background-image`

`background: #fff` в своём классе гасит `background-image`, который подставляет класс-носитель медиа, — картинка молча исчезает, остаётся пустой блок.

**Закрыто:** в `styles.css` концепта **ноль** объявлений `background:` — только `background-color`. Проверяется одной строкой: `grep -c 'background:' concepts/dvor/styles.css` → `0`.

### 3. Шорткат `margin` глушит утилиты `.mt*` из ядра

`.mt8` из `base.css` перебивается любым `margin:` в `styles.css`, потому что концепт подключается позже: заголовок секции слипается с предыдущим блоком.

**Закрыто там, где утилиты применяются:** все классы, на которые в разметке навешивается `.mt4 … .mt24`, задают отступы longhand — `.d-card` (`margin-bottom` / `-left` / `-right`), `.d-btn`, `.d-note`, `.d-post-body`, `.d-post-meta`, `.d-hero-t`, `.d-lead`, `.d-chips`, `.d-row-sub`, `.d-safari-input`.

**Оговорка, честная.** Шорткат `margin` остался в **четырёх** классах, которым нужен асимметричный отступ `0 var(--d-ins) N` и которые утилиты не принимают: `.d-plan`, `.d-tape`, `.d-fillbar` и `.d-frame` (у последнего `margin` — часть механики центрирования, `-104px 0 0 -104px`). `.d-search` из этого списка ушёл: он теперь задаёт `margin-left` / `-right` / `-bottom` longhand. Ни на одном из четырёх в разметке нет `.mt*`, и появляться не должно — если такой отступ понадобится, шорткат надо разложить на longhand до правки разметки.

Комментарий в шапке `styles.css` («Шорткаты background и margin здесь не используются») по-прежнему шире факта: про `background` он верен — `grep -c 'background:'` даёт `0`, — а про `margin` нет, и его надо переписать в оговорку. Инлайновых `margin`-шорткатов в разметке ещё пять (`ads`, `background`, `code`, `lock`, `phone`), но они стоят на конкретных узлах, а не на классах-носителях утилит.

### 4. Свой `display` на корне экрана перебивает `.screen{display:none}`

**Найдено в этом концепте.** Ядро гасит невыбранный экран правилом `.screen { display: none }` (специфичность 0,1,0). Любой свой класс с `display: flex`, попавший на тот же узел, имеет ту же специфичность — и побеждает, потому что `styles.css` подключается после `base.css`. Экран становится виден всегда, поверх всех прототипов сразу.

Случилось на `fill.html`: корень был `<div class="screen d-safari">`, а `.d-safari { display: flex }` нужен, чтобы `.d-keybar` прижался вниз через `margin-top: auto`.

**Закрыто двумя правками.** В `styles.css` — правило `.screen:not(.is-on) { display: none }` (специфичность 0,2,0) гасит невыбранный экран при любом наборе классов на нём. В `fill.html` — `.d-safari` снят с корня и вынесен во вложенный узел, корень остался `<div class="screen d-page">`. Правило страхует и будущие экраны: приём «свой flex-контейнер на корне» может повториться, и падать он должен на ревью PNG, а не в продакшене доки.

Отдельно стоит помнить, почему это не поймал ни один тест: автотест проверяет переходы, fallback и хит-таргеты в собранном документе, а сломанный экран остаётся достижимым и валидным — он просто нарисован поверх остальных. Единственный сигнал был визуальный.

## Актуальная навигация

Дом · События · Двор · Меню. Удалённые коммуникационные поверхности не входят в таб-бар, граф переходов и состояния доступов.
