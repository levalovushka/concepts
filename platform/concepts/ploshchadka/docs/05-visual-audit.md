# Визуальный аудит

## Проход 1 — иерархия

Проверены все экраны. Исправлено 14 замечаний: текущий блок получил отдельный тёмный слой, вторичные настройки убраны ниже первого экрана, статусы сведены к трём типам, на карте усилен маршрут, в рации устранена конкуренция кнопок.

## Проход 2 — состояния

Исправлено 6 замечаний: длинные роли переносятся, fallback расположен рядом с триггером, гостевой статус виден в аккаунте, опасное удаление отделено цветом и текстом, офлайн-свежесть не обещает точного фонового времени, QR сохранён как явный локальный fallback.

Открытых blocker и major-дефектов нет. Проверены loading/empty/error для форм и loading/empty/error/offline/permission для контентных экранов через контролы прототипа.
