# Архитектура

Клиентский каталог выступлений приходит статическим пакетом. История, подписки и моменты хранятся в Core Data; черновики — в FileManager. AVPlayer, Cast SDK, APNs provider и AASA закрывают системные сценарии без собственного сервера.

## Навигация

Четыре root-раздела: `home`, `clips`, `subscriptions`, `profile`. Экран `create` не является вкладкой: он открывается из `+` на главной и закрывается обратно на `home`. Камера и медиатека возвращаются в `create`; публикация после обоих путей также возвращается в `create`, а успешная публикация ведёт в `clips`.

Основной просмотр: `home → watch → moment`. Канал артиста открывается из подписок и городской афиши. Cast, фоновое воспроизведение и universal link являются системными ответвлениями плеера и возвращаются в `watch`.

## Доступы и fallback

- `tracking`: после объяснения бесплатной модели; отказ оставляет контекстную рекламу на главной.
- `location`: только по нажатию «Рядом»; отказ открывает городскую афишу с ручным городом.
- `localnet`: только из Cast; отказ продолжает просмотр на iPhone.
- `photoadd`: только при экспорте момента; отказ сохраняет его внутри «Сцены».
- `camera + mic`: последовательная JIT-цепочка после «Снять клип»; отказ на любом шаге оставляет импорт.
- `photo`: только после «Выбрать видео»; отказ оставляет съёмку.
- `push`: только из переключателя напоминаний; отказ сохраняет расписание внутри приложения.
- `audio` и `domains`: entitlements без системного alert, активируются действиями плеера.

## Карта экранов

<!-- @generated:screen-map -->
| ID | Название | Тип | Доступы |
|---|---|---|---|
| `phone` | Регистрация по почте | старт, без таб-бара | — |
| `ads` | Бесплатный просмотр | onboarding | tracking |
| `home` | Главная | tab (root) | location |
| `nearby` | Рядом | push | — |
| `watch` | Концерт | push | localnet, audio (activate), domains (activate) |
| `moment` | Момент | push | photoadd |
| `background` | Экран погас | system | — |
| `cast` | На телевизоре | sheet | — |
| `deeplink` | По ссылке | system handoff | — |
| `clips` | Клипы | tab (root) | — |
| `create` | Создать клип | modal task | mic, camera, photo |
| `camera` | Камера | fullscreen | — |
| `upload` | Выбор видео | system picker | — |
| `publish` | Публикация | push | — |
| `subscriptions` | Подписки | tab (root) | push |
| `artist` | Артист | push | — |
| `profile` | Профиль | tab (root) | — |
<!-- @end -->

## Матрица доступов

Каждая строка читается так: пользователь делает жест — система спрашивает — при отказе остаётся рабочий путь.

<!-- @generated:perm-matrix -->
| Ключ | Жест пользователя | Экран | Если отказ | Риск Review |
|---|---|---|---|---|
| `NSPhotoLibraryAddUsageDescription` | «Сохранить в Фото» в моменте | Момент | Момент остаётся в коллекции «Сцены» | Низкий |
| `NSLocalNetworkUsageDescription (+ NSBonjourServices)` | Кнопка Cast в плеере | Концерт | Концерт продолжает играть на iPhone | Низкий |
| `NSMicrophoneUsageDescription` | «Снять клип» после разрешения камеры | Создать клип | Можно импортировать готовое видео | Низкий |
| `NSLocationWhenInUseUsageDescription` | Чип «Рядом» на главной | Главная | Показывается афиша выбранного города | **Условный** — JIT-запрос после выбора Рядом |
| `NSUserTrackingUsageDescription` | «Смотреть бесплатно» после входа | Бесплатный просмотр | Контекстная реклама без трекинга | **Условный** — ATT после объяснения |
| `NSCameraUsageDescription` | «Снять клип» в экране создания | Создать клип | Остаётся импорт готовой записи | Низкий |
| `NSPhotoLibraryUsageDescription` | «Выбрать из медиатеки» | Создать клип | Можно снять новый момент камерой | Низкий |
| `UIBackgroundModes: audio` | «Слушать в фоне» в плеере | Концерт | Видео останавливается при блокировке | **Условный** — AVPlayer, Now Playing и remote-команды |
| `aps-environment (Push Notifications capability)` | Свитч «Премьеры» в подписках | Подписки | Премьеры отмечаются внутри вкладки | **Условный** — APNs topics артистов |
| `com.apple.developer.associated-domains` | «Поделиться» в плеере | Концерт | Открывается публичная веб-страница | **Условный** — AASA для /show/* и /moment/* |
<!-- @end -->

## Информационная архитектура

Дерево выведено из разметки экранов, а не нарисовано руками: рукописная схема расходится с прототипом первой же правкой.

<!-- @generated:ia-tree -->
```
Регистрация по почте (phone) — старт, без таб-бара · открывается: старт
    └─ Бесплатный просмотр (ads) — onboarding · открывается: «Создать аккаунт» · tracking

Главная (home) — tab (root) · открывается: «Смотреть бесплатно» (tracking) · location
    ├─ Рядом (nearby) — push · открывается: «Рядом» (location)
    ├─ Концерт (watch) — push · открывается: «ПРЕМЬЕРА СЕГОДНЯ», «58:12» … · localnet, audio, domains
    │   ├─ Момент (moment) — push · открывается: «Момент», «18:42» … · photoadd
    │   ├─ Экран погас (background) — system · открывается: «Слушать в фоне» (audio)
    │   ├─ На телевизоре (cast) — sheet · открывается: «Смотреть на телевизоре» (localnet)
    │   └─ По ссылке (deeplink) — system handoff · открывается: «Открыть по ссылке» (domains)
    └─ Создать клип (create) — modal task · открывается: «Добавить видео» · mic, camera, photo
        ├─ Камера (camera) — fullscreen · открывается: «Снять клип» (camera + mic)
        ├─ Выбор видео (upload) — system picker · открывается: «Выбрать видео» (photo), «Открыть медиатеку»
        └─ Публикация (publish) — push · открывается: «Начать запись», «Далее»

Клипы (clips) — tab (root) · открывается: «Опубликовать клип»

Подписки (subscriptions) — tab (root) · открывается: вкладка таб-бара · push
    └─ Артист (artist) — push · открывается: «Тёплый воздух», «LIVE»

Профиль (profile) — tab (root) · открывается: вкладка таб-бара
```
<!-- @end -->

## Переходы

<!-- @generated:transitions -->
| Экран | Что можно сделать | Ведёт на | Доступ | Тип перехода |
|---|---|---|---|---|
| `phone` | «Создать аккаунт» | `ads` | — | переход |
| `ads` | «Назад» | `phone` | — | возврат по IA |
| `ads` | «Смотреть бесплатно» | `home` | `NSUserTrackingUsageDescription` | доступ разрешён |
| `home` | «Добавить видео» | `create` | — | переход |
| `home` | «Рядом» | `nearby` | `NSLocationWhenInUseUsageDescription` | доступ разрешён |
| `home` | «Рядом» | `home` | `NSLocationWhenInUseUsageDescription` | отказ → fallback |
| `home` | «ПРЕМЬЕРА СЕГОДНЯ», «58:12» … | `watch` | — | переход |
| `nearby` | «Назад» | `home` | — | возврат по IA |
| `nearby` | «Тёплый воздух» | `artist` | — | переход |
| `watch` | «Назад» | `home` | — | возврат по IA |
| `watch` | «Смотреть на телевизоре» | `cast` | `NSLocalNetworkUsageDescription (+ NSBonjourServices)` | доступ разрешён |
| `watch` | «Смотреть на телевизоре» | `watch` | `NSLocalNetworkUsageDescription (+ NSBonjourServices)` | отказ → fallback |
| `watch` | «Открыть по ссылке» | `deeplink` | `com.apple.developer.associated-domains` | entitlement, без alert |
| `watch` | «Момент», «18:42» | `moment` | — | переход |
| `watch` | «Слушать в фоне» | `background` | `UIBackgroundModes: audio` | entitlement, без alert |
| `moment` | «Назад» | `watch` | — | возврат по IA |
| `moment` | «Сохранить в Фото» | `moment` | `NSPhotoLibraryAddUsageDescription` | доступ разрешён |
| `background` | — | — | — | дальше переходов нет |
| `cast` | «Закрыть» | `watch` | — | возврат по IA |
| `deeplink` | «Открыть в «Сцене»» | `watch` | — | переход |
| `clips` | «Сохранить момент» | `moment` | — | переход |
| `clips` | «Свет остаётся — финал» | `watch` | — | переход |
| `create` | «Закрыть» | `home` | — | возврат по IA |
| `create` | «Снять клип» | `camera` | `NSCameraUsageDescription + NSMicrophoneUsageDescription` | доступ разрешён |
| `create` | «Снять клип» | `create` | `NSCameraUsageDescription + NSMicrophoneUsageDescription` | отказ → fallback |
| `create` | «Выбрать видео» | `upload` | `NSPhotoLibraryUsageDescription` | доступ разрешён |
| `create` | «Выбрать видео» | `create` | `NSPhotoLibraryUsageDescription` | отказ → fallback |
| `camera` | «Закрыть» | `create` | — | возврат по IA |
| `camera` | «Начать запись» | `publish` | — | переход |
| `camera` | «Открыть медиатеку» | `upload` | — | переход |
| `upload` | «Далее» | `publish` | — | переход |
| `publish` | «Назад» | `create` | — | возврат по IA |
| `publish` | «Опубликовать клип» | `clips` | — | подтверждение |
| `subscriptions` | «Напоминания о премьерах» | `subscriptions` | `aps-environment (Push Notifications capability)` | доступ разрешён |
| `subscriptions` | «LIVE» | `artist` | — | переход |
| `subscriptions` | «СЕГОДНЯ · 21:00» | `watch` | — | переход |
| `artist` | «Назад» | `subscriptions` | — | возврат по IA |
| `artist` | «LIVE», «1:24:08» | `watch` | — | переход |
| `profile` | «Электрический сад» | `watch` | — | переход |
<!-- @end -->

## Действия на экранах

Каждый интерактивный элемент прототипа: что он делает, куда ведёт и какой ключ при этом запрашивается. В отличие от таблицы переходов выше, вкладки таб-бара, кнопки возврата и подтверждения без перехода тоже здесь — раздел выводится из разметки и отвечает на вопрос «что делает эта кнопка», не заставляя открывать HTML.

<!-- @generated:actions -->
| Экран | Элемент | Роль | Что делает | Ведёт на | При отказе | Ключ |
|---|---|---|---|---|---|---|
| `phone` | Создать аккаунт | элемент экрана | открывает экран | Бесплатный просмотр (ads) | — | — |
| `phone` | Помощь | элемент экрана | показывает подтверждение «Помощь · scene.video/help» | остаётся на экране | — | — |
| `phone` | Поддержка | элемент экрана | показывает подтверждение «Поддержка · alex@inbox.ru» | остаётся на экране | — | — |
| `phone` | Пользовательское соглашение | элемент экрана | показывает подтверждение «Соглашение · scene.video/terms» | остаётся на экране | — | — |
| `ads` | Назад | кнопка «назад» | возвращает к родительскому экрану | Регистрация по почте (phone) | — | — |
| `ads` | Смотреть бесплатно | элемент экрана | спрашивает доступ tracking | Главная (home) | Главная (home) | `NSUserTrackingUsageDescription` |
| `home` | Добавить видео | элемент экрана | открывает экран | Создать клип (create) | — | — |
| `home` | Поиск | элемент экрана | показывает подтверждение «Поиск откроется здесь» | остаётся на экране | — | — |
| `home` | Рядом | элемент экрана | спрашивает доступ location | Рядом (nearby) | Главная (home) | `NSLocationWhenInUseUsageDescription` |
| `home` | ПРЕМЬЕРА СЕГОДНЯ | элемент экрана | открывает экран | Концерт (watch) | — | — |
| `home` | 58:12 | элемент экрана | открывает экран | Концерт (watch) | — | — |
| `home` | 42:08 | элемент экрана | открывает экран | Концерт (watch) | — | — |
| `home` | Раздел Главная | вкладка | переключает вкладку таб-бара | Главная (home) | — | — |
| `home` | Раздел Клипы | вкладка | переключает вкладку таб-бара | Клипы (clips) | — | — |
| `home` | Раздел Подписки | вкладка | переключает вкладку таб-бара | Подписки (subscriptions) | — | — |
| `home` | Раздел Профиль | вкладка | переключает вкладку таб-бара | Профиль (profile) | — | — |
| `nearby` | Назад | кнопка «назад» | возвращает к родительскому экрану | Главная (home) | — | — |
| `nearby` | Выбрать город | элемент экрана | показывает подтверждение «Город: Алматы» | остаётся на экране | — | — |
| `nearby` | Тёплый воздух | элемент экрана | открывает экран | Артист (artist) | — | — |
| `watch` | Назад | кнопка «назад» | возвращает к родительскому экрану | Главная (home) | — | — |
| `watch` | Смотреть на телевизоре | элемент экрана | спрашивает доступ localnet | На телевизоре (cast) | Концерт (watch) | `NSLocalNetworkUsageDescription (+ NSBonjourServices)` |
| `watch` | Канал Маяка | элемент экрана | показывает подтверждение «Канал Маяка» | остаётся на экране | — | — |
| `watch` | Открыть по ссылке | элемент экрана | включает entitlement domains, системного alert нет | По ссылке (deeplink) | — | `com.apple.developer.associated-domains` |
| `watch` | Момент | элемент экрана | открывает экран | Момент (moment) | — | — |
| `watch` | Слушать в фоне | элемент экрана | включает entitlement audio, системного alert нет | Экран погас (background) | — | `UIBackgroundModes: audio` |
| `watch` | 18:42 | элемент экрана | открывает экран | Момент (moment) | — | — |
| `moment` | Назад | кнопка «назад» | возвращает к родительскому экрану | Концерт (watch) | — | — |
| `moment` | Ещё | элемент экрана | показывает подтверждение «Действия с моментом» | остаётся на экране | — | — |
| `moment` | Сохранить в Фото | элемент экрана | спрашивает доступ photoadd | Момент (moment) | Момент (moment) | `NSPhotoLibraryAddUsageDescription` |
| `moment` | Скопировать ссылку | элемент экрана | показывает подтверждение «Ссылка скопирована» | остаётся на экране | — | — |
| `background` | — | — | интерактивных элементов нет | — | — | — |
| `cast` | Закрыть | кнопка «назад» | возвращает к родительскому экрану | Концерт (watch) | — | — |
| `deeplink` | Открыть в «Сцене» | элемент экрана | открывает экран | Концерт (watch) | — | — |
| `clips` | Поиск | элемент экрана | показывает подтверждение «Поиск по клипам» | остаётся на экране | — | — |
| `clips` | Нравится | элемент экрана | показывает подтверждение «Добавлено в понравившиеся» | остаётся на экране | — | — |
| `clips` | Комментарии | элемент экрана | показывает подтверждение «Открыты комментарии» | остаётся на экране | — | — |
| `clips` | Поделиться | элемент экрана | показывает подтверждение «Открыт Share Sheet» | остаётся на экране | — | — |
| `clips` | Сохранить момент | элемент экрана | открывает экран | Момент (moment) | — | — |
| `clips` | Подписаться | элемент экрана | показывает подтверждение «Вы подписались» | остаётся на экране | — | — |
| `clips` | Свет остаётся — финал | элемент экрана | открывает экран | Концерт (watch) | — | — |
| `clips` | Раздел Главная | вкладка | переключает вкладку таб-бара | Главная (home) | — | — |
| `clips` | Раздел Клипы | вкладка | переключает вкладку таб-бара | Клипы (clips) | — | — |
| `clips` | Раздел Создать клип | вкладка | переключает вкладку таб-бара | Создать клип (create) | — | — |
| `clips` | Раздел Подписки | вкладка | переключает вкладку таб-бара | Подписки (subscriptions) | — | — |
| `clips` | Раздел Профиль | вкладка | переключает вкладку таб-бара | Профиль (profile) | — | — |
| `create` | Закрыть | кнопка «назад» | возвращает к родительскому экрану | Главная (home) | — | — |
| `create` | Снять клип | элемент экрана | спрашивает доступы цепочкой: camera → mic | Камера (camera) | Создать клип (create) | `NSCameraUsageDescription + NSMicrophoneUsageDescription` |
| `create` | Выбрать видео | элемент экрана | спрашивает доступ photo | Выбор видео (upload) | Создать клип (create) | `NSPhotoLibraryUsageDescription` |
| `camera` | Закрыть | кнопка «назад» | возвращает к родительскому экрану | Создать клип (create) | — | — |
| `camera` | Начать запись | элемент экрана | открывает экран | Публикация (publish) | — | — |
| `camera` | Открыть медиатеку | элемент экрана | открывает экран | Выбор видео (upload) | — | — |
| `upload` | Далее | элемент экрана | открывает экран | Публикация (publish) | — | — |
| `publish` | Назад | кнопка «назад» | возвращает к родительскому экрану | Создать клип (create) | — | — |
| `publish` | Опубликовать клип | элемент экрана | показывает подтверждение «Клип опубликован» | Клипы (clips) | — | — |
| `subscriptions` | Поиск | элемент экрана | показывает подтверждение «Поиск по подпискам» | остаётся на экране | — | — |
| `subscriptions` | Напоминания о премьерах | элемент экрана | спрашивает доступ push | Подписки (subscriptions) | Подписки (subscriptions) | `aps-environment (Push Notifications capability)` |
| `subscriptions` | LIVE | элемент экрана | открывает экран | Артист (artist) | — | — |
| `subscriptions` | СЕГОДНЯ · 21:00 | элемент экрана | открывает экран | Концерт (watch) | — | — |
| `subscriptions` | Раздел Главная | вкладка | переключает вкладку таб-бара | Главная (home) | — | — |
| `subscriptions` | Раздел Клипы | вкладка | переключает вкладку таб-бара | Клипы (clips) | — | — |
| `subscriptions` | Раздел Подписки | вкладка | переключает вкладку таб-бара | Подписки (subscriptions) | — | — |
| `subscriptions` | Раздел Профиль | вкладка | переключает вкладку таб-бара | Профиль (profile) | — | — |
| `artist` | Назад | кнопка «назад» | возвращает к родительскому экрану | Подписки (subscriptions) | — | — |
| `artist` | Ещё | элемент экрана | показывает подтверждение «Действия с каналом» | остаётся на экране | — | — |
| `artist` | LIVE | элемент экрана | открывает экран | Концерт (watch) | — | — |
| `artist` | 1:24:08 | элемент экрана | открывает экран | Концерт (watch) | — | — |
| `profile` | Настройки | элемент экрана | показывает подтверждение «Настройки профиля» | остаётся на экране | — | — |
| `profile` | Электрический сад | элемент экрана | открывает экран | Концерт (watch) | — | — |
| `profile` | Раздел Главная | вкладка | переключает вкладку таб-бара | Главная (home) | — | — |
| `profile` | Раздел Клипы | вкладка | переключает вкладку таб-бара | Клипы (clips) | — | — |
| `profile` | Раздел Подписки | вкладка | переключает вкладку таб-бара | Подписки (subscriptions) | — | — |
| `profile` | Раздел Профиль | вкладка | переключает вкладку таб-бара | Профиль (profile) | — | — |
<!-- @end -->

## Граница «без бэкенда»

<!-- @generated:backendless -->
| Требовало бы сервера | Решение без сервера |
|---|---|
| Каталог выступлений | Статический редакционный пакет и CDN только на чтение |
| Публикация моментов | Локальный черновик и системный share sheet |
| Подписки и премьеры | Core Data и APNs topics провайдера |
| Ссылки | Статические страницы scene.video и AASA |
<!-- @end -->
