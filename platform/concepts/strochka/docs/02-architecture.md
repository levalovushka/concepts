# Строчка — карта экранов и архитектура

Заполняется по PLAYBOOK.md. Блоки `@generated:*` пересобираются командой `npm run docs -- strochka` — внутрь них руками не пишем.

## Информационная архитектура

Дерево выведено из разметки экранов и `concept.json`: вложенность — по `parent`, подпись «открывается» — реальный элемент прототипа. Первые два узла всегда одни и те же: вход по номеру (`phone` → `code`).

<!-- @generated:ia-tree -->
```
Вход по номеру (phone) — старт, без таб-бара · открывается: старт
    ├─ Пароль (password) — push, без таб-бара · открывается: «Далее»
    │   └─ Аккаунт (account) — push, без таб-бара · открывается: «Управление»
    │       └─ Удаление аккаунта (deleteaccount) — push, без таб-бара · открывается: «Удалить аккаунт»
    └─ Создать аккаунт (register) — push, без таб-бара · открывается: «Нет аккаунта? Создать»
        └─ Пароль нового аккаунта (registerpassword) — push, без таб-бара · открывается: «Далее»

Главная (program) — tab (root) · открывается: «Продолжить без аккаунта», «Войти» …
    ├─ Свет над рекой (piece) — push · открывается: «Альт · буква В», «Весенний свет» …
    │   ├─ Партия альта (player) — fullscreen · открывается: «Свет над рекой», «Слушать альт» … · audio
    │   │   └─ Погасший экран (background) — system · открывается: «В фоне» (audio)
    │   └─ Ноты рядом (sheet) — push · открывается: «Открыть ноты», «Открыть ноты рядом» … · camera, photo
    │       ├─ Сканирование нот (camera) — fullscreen · открывается: «Снять страницы» (camera)
    │       └─ Импорт из «Фото» (import) — push · открывается: «Импорт из «Фото»» (photo)
    ├─ Весенний концерт (releases) — push · открывается: «Радио», «Все номера» …
    └─ Настройки (settings) — push · открывается: «Профиль», «Уведомления» … · push
        └─ Дополнение к программе (notif) — system · открывается: «Сообщать о дополнениях» (push)

Поиск (notes) — tab (root) · открывается: «Обзор»

Моя музыка (journal) — tab (root) · открывается: «Моя музыка»
```
<!-- @end -->

## Карта экранов

<!-- @generated:screen-map -->
| ID | Название | Тип | Доступы |
|---|---|---|---|
| `phone` | Вход по номеру | старт, без таб-бара | — |
| `password` | Пароль | push, без таб-бара | — |
| `register` | Создать аккаунт | push, без таб-бара | — |
| `registerpassword` | Пароль нового аккаунта | push, без таб-бара | — |
| `account` | Аккаунт | push, без таб-бара | — |
| `deleteaccount` | Удаление аккаунта | push, без таб-бара | — |
| `program` | Главная | tab (root) | — |
| `piece` | Свет над рекой | push | — |
| `player` | Партия альта | fullscreen | audio (activate) |
| `background` | Погасший экран | system | — |
| `sheet` | Ноты рядом | push | camera, photo |
| `camera` | Сканирование нот | fullscreen | — |
| `notes` | Поиск | tab (root) | — |
| `import` | Импорт из «Фото» | push | — |
| `journal` | Моя музыка | tab (root) | — |
| `releases` | Весенний концерт | push | — |
| `notif` | Дополнение к программе | system | — |
| `settings` | Настройки | push | push |
<!-- @end -->

## Переходы: откуда куда и чем

Каждая строка — элемент, который есть в прототипе. Вкладки таб-бара опущены: они ведут в корни разделов и одинаковы на всех экранах.

<!-- @generated:transitions -->
| Экран | Что можно сделать | Ведёт на | Доступ | Тип перехода |
|---|---|---|---|---|
| `phone` | «Далее» | `password` | — | переход |
| `phone` | «Нет аккаунта? Создать» | `register` | — | переход |
| `phone` | «Продолжить без аккаунта» | `program` | — | переход |
| `password` | «Назад» | `phone` | — | возврат по IA |
| `password` | «Войти» | `program` | — | переход |
| `register` | «Назад» | `phone` | — | возврат по IA |
| `register` | «Далее» | `registerpassword` | — | переход |
| `register` | «Уже есть аккаунт? Войти» | `phone` | — | переход |
| `registerpassword` | «Назад» | `register` | — | возврат по IA |
| `registerpassword` | «Создать аккаунт» | `program` | — | переход |
| `account` | «Назад» | `password` | — | возврат по IA |
| `account` | «Выйти» | `phone` | — | переход |
| `account` | «Удалить аккаунт» | `deleteaccount` | — | переход |
| `deleteaccount` | «Назад» | `account` | — | возврат по IA |
| `deleteaccount` | «Удалить аккаунт» | `phone` | — | переход |
| `program` | «Профиль», «Уведомления» | `settings` | — | переход |
| `program` | «Моя музыка» | `journal` | — | переход |
| `program` | «Радио», «Все номера» … | `releases` | — | переход |
| `program` | «Обзор» | `notes` | — | переход |
| `program` | «Альт · буква В», «Весенний свет» … | `piece` | — | переход |
| `program` | «Свет над рекой» | `player` | — | переход |
| `piece` | «Назад» | `program` | — | возврат по IA |
| `piece` | «Открыть ноты», «Открыть ноты рядом» | `sheet` | — | переход |
| `piece` | «Слушать альт», «Свет над рекой» | `player` | — | переход |
| `player` | «Закрыть плеер» | `piece` | — | возврат по IA |
| `player` | «Легенда», «Весенний свет» | `piece` | — | переход |
| `player` | «Открыть ноты» | `sheet` | — | переход |
| `player` | «В фоне» | `background` | `UIBackgroundModes: audio` | entitlement, без alert |
| `background` | — | — | — | дальше переходов нет |
| `sheet` | «Назад» | `piece` | — | возврат по IA |
| `sheet` | «Снять страницы» | `camera` | `NSCameraUsageDescription` | доступ разрешён |
| `sheet` | «Снять страницы» | `sheet` | `NSCameraUsageDescription` | отказ → fallback |
| `sheet` | «Импорт из «Фото»» | `import` | `NSPhotoLibraryUsageDescription` | доступ разрешён |
| `sheet` | «Импорт из «Фото»» | `sheet` | `NSPhotoLibraryUsageDescription` | отказ → fallback |
| `sheet` | «Открыть настройки» | `settings` | — | переход |
| `sheet` | «Свет над рекой» | `player` | — | переход |
| `camera` | «Настройки камеры», «Открыть настройки» | `settings` | — | переход |
| `camera` | «Снять страницу» | `sheet` | — | переход |
| `notes` | «№5 · весенний концерт», «Анна Лебедева» | `piece` | — | переход |
| `notes` | «Весенний концерт» | `releases` | — | переход |
| `notes` | «Альт · буква В · т. 41–74», «Свет над рекой» | `player` | — | переход |
| `import` | «Назад» | `sheet` | — | возврат по IA |
| `import` | «Добавить 4 страницы», «Вернуться к камере» | `sheet` | — | переход |
| `journal` | «Настройки» | `settings` | — | переход |
| `journal` | «Открыть программы», «22 марта · большой зал» | `releases` | — | переход |
| `journal` | «Открыть сохранённые номера», «Весенний свет» … | `piece` | — | переход |
| `journal` | «Открыть загруженные ноты» | `sheet` | — | переход |
| `journal` | «Свет над рекой» | `player` | — | переход |
| `releases` | «Назад» | `program` | — | возврат по IA |
| `releases` | «Настройки программы» | `settings` | — | переход |
| `releases` | «Слушать программу,», «Свет над рекой» | `player` | — | переход |
| `releases` | «Сосна», «Северное сияние» … | `piece` | — | переход |
| `notif` | «СТРОЧКА» | `piece` | — | переход |
| `settings` | «Назад» | `program` | — | возврат по IA |
| `settings` | «Управление» | `account` | — | переход |
| `settings` | «Сообщать о дополнениях» | `notif` | `aps-environment` | доступ разрешён |
| `settings` | «Сообщать о дополнениях» | `settings` | `aps-environment` | отказ → fallback |
| `settings` | «Проверить сейчас» | `releases` | — | переход |
| `settings` | «Открыть настройки» | `settings` | — | переход |
| `settings` | «Свет над рекой» | `player` | — | переход |
<!-- @end -->

## Действия на экранах

Каждый интерактивный элемент прототипа: что он делает, куда ведёт и какой ключ при этом запрашивается. В отличие от таблицы переходов выше, вкладки таб-бара, кнопки возврата и подтверждения без перехода тоже здесь — раздел выводится из разметки и отвечает на вопрос «что делает эта кнопка», не заставляя открывать HTML.

<!-- @generated:actions -->
| Экран | Элемент | Роль | Что делает | Ведёт на | При отказе | Ключ |
|---|---|---|---|---|---|---|
| `phone` | Далее | элемент экрана | открывает экран | Пароль (password) | — | — |
| `phone` | Нет аккаунта? Создать | элемент экрана | открывает экран | Создать аккаунт (register) | — | — |
| `phone` | Продолжить без аккаунта | элемент экрана | открывает экран | Главная (program) | — | — |
| `phone` | пользовательское соглашение | элемент экрана | показывает подтверждение «Соглашение · strochka.app/terms» | остаётся на экране | — | — |
| `phone` | политику конфиденциальности | элемент экрана | показывает подтверждение «Политика · strochka.app/privacy» | остаётся на экране | — | — |
| `phone` | Помощь и поддержка | элемент экрана | показывает подтверждение «Справка · strochka.app/help · support@strochka.app» | остаётся на экране | — | — |
| `password` | Назад | кнопка «назад» | возвращает к родительскому экрану | Вход по номеру (phone) | — | — |
| `password` | Войти | элемент экрана | открывает экран | Главная (program) | — | — |
| `password` | Забыли пароль? | элемент экрана | показывает подтверждение «Ссылка для восстановления отправлена» | остаётся на экране | — | — |
| `register` | Назад | кнопка «назад» | возвращает к родительскому экрану | Вход по номеру (phone) | — | — |
| `register` | Далее | элемент экрана | открывает экран | Пароль нового аккаунта (registerpassword) | — | — |
| `register` | Уже есть аккаунт? Войти | элемент экрана | открывает экран | Вход по номеру (phone) | — | — |
| `registerpassword` | Назад | кнопка «назад» | возвращает к родительскому экрану | Создать аккаунт (register) | — | — |
| `registerpassword` | Создать аккаунт | элемент экрана | открывает экран | Главная (program) | — | — |
| `registerpassword` | пользовательское соглашение | элемент экрана | показывает подтверждение «Соглашение · strochka.app/terms» | остаётся на экране | — | — |
| `registerpassword` | политику конфиденциальности | элемент экрана | показывает подтверждение «Политика · strochka.app/privacy» | остаётся на экране | — | — |
| `registerpassword` | Помощь и поддержка | элемент экрана | показывает подтверждение «Справка · strochka.app/help · support@strochka.app» | остаётся на экране | — | — |
| `account` | Назад | кнопка «назад» | возвращает к родительскому экрану | Пароль (password) | — | — |
| `account` | Выйти | элемент экрана | открывает экран | Вход по номеру (phone) | — | — |
| `account` | Удалить аккаунт | элемент экрана | открывает экран | Удаление аккаунта (deleteaccount) | — | — |
| `deleteaccount` | Назад | кнопка «назад» | возвращает к родительскому экрану | Аккаунт (account) | — | — |
| `deleteaccount` | Удалить аккаунт | элемент экрана | открывает экран | Вход по номеру (phone) | — | — |
| `program` | Профиль | элемент экрана | открывает экран | Настройки (settings) | — | — |
| `program` | Уведомления | элемент экрана | открывает экран | Настройки (settings) | — | — |
| `program` | Профиль | элемент экрана | открывает экран | Настройки (settings) | — | — |
| `program` | Моя музыка | элемент экрана | открывает экран | Моя музыка (journal) | — | — |
| `program` | Радио | элемент экрана | открывает экран | Весенний концерт (releases) | — | — |
| `program` | Обзор | элемент экрана | открывает экран | Поиск (notes) | — | — |
| `program` | Альт · буква В | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `program` | Все номера | элемент экрана | открывает экран | Весенний концерт (releases) | — | — |
| `program` | Весенний концерт | элемент экрана | открывает экран | Весенний концерт (releases) | — | — |
| `program` | Весенний свет | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `program` | Тихая вода | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `program` | Свет над рекой | элемент экрана | открывает экран | Партия альта (player) | — | — |
| `program` | Раздел Главная | вкладка | переключает вкладку таб-бара | Главная (program) | — | — |
| `program` | Раздел Поиск | вкладка | переключает вкладку таб-бара | Поиск (notes) | — | — |
| `program` | Раздел Моя музыка | вкладка | переключает вкладку таб-бара | Моя музыка (journal) | — | — |
| `piece` | Назад | кнопка «назад» | возвращает к родительскому экрану | Главная (program) | — | — |
| `piece` | Открыть ноты | элемент экрана | открывает экран | Ноты рядом (sheet) | — | — |
| `piece` | Слушать альт | элемент экрана | открывает экран | Партия альта (player) | — | — |
| `piece` | Открыть ноты | элемент экрана | открывает экран | Ноты рядом (sheet) | — | — |
| `piece` | Открыть ноты рядом | элемент экрана | открывает экран | Ноты рядом (sheet) | — | — |
| `piece` | Свет над рекой | элемент экрана | открывает экран | Партия альта (player) | — | — |
| `player` | Закрыть плеер | кнопка «назад» | возвращает к родительскому экрану | Свет над рекой (piece) | — | — |
| `player` | Легенда | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `player` | Весенний свет | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `player` | Открыть ноты | элемент экрана | открывает экран | Ноты рядом (sheet) | — | — |
| `player` | В фоне | элемент экрана | включает entitlement audio, системного alert нет | Погасший экран (background) | — | `UIBackgroundModes: audio` |
| `background` | — | — | интерактивных элементов нет | — | — | — |
| `sheet` | Назад | кнопка «назад» | возвращает к родительскому экрану | Свет над рекой (piece) | — | — |
| `sheet` | Снять страницы | элемент экрана | спрашивает доступ camera | Сканирование нот (camera) | Ноты рядом (sheet) | `NSCameraUsageDescription` |
| `sheet` | Снять страницы | элемент экрана | спрашивает доступ camera | Сканирование нот (camera) | Ноты рядом (sheet) | `NSCameraUsageDescription` |
| `sheet` | Импорт из «Фото» | элемент экрана | спрашивает доступ photo | Импорт из «Фото» (import) | Ноты рядом (sheet) | `NSPhotoLibraryUsageDescription` |
| `sheet` | Открыть настройки | элемент экрана | открывает экран | Настройки (settings) | — | — |
| `sheet` | Свет над рекой | элемент экрана | открывает экран | Партия альта (player) | — | — |
| `camera` | Настройки камеры | элемент экрана | открывает экран | Настройки (settings) | — | — |
| `camera` | Снять страницу | элемент экрана | открывает экран | Ноты рядом (sheet) | — | — |
| `camera` | Открыть настройки | элемент экрана | открывает экран | Настройки (settings) | — | — |
| `notes` | №5 · весенний концерт | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `notes` | Весенний концерт | элемент экрана | открывает экран | Весенний концерт (releases) | — | — |
| `notes` | Анна Лебедева | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `notes` | Альт · буква В · т. 41–74 | элемент экрана | открывает экран | Партия альта (player) | — | — |
| `notes` | Свет над рекой | элемент экрана | открывает экран | Партия альта (player) | — | — |
| `notes` | Раздел Главная | вкладка | переключает вкладку таб-бара | Главная (program) | — | — |
| `notes` | Раздел Поиск | вкладка | переключает вкладку таб-бара | Поиск (notes) | — | — |
| `notes` | Раздел Моя музыка | вкладка | переключает вкладку таб-бара | Моя музыка (journal) | — | — |
| `import` | Назад | кнопка «назад» | возвращает к родительскому экрану | Ноты рядом (sheet) | — | — |
| `import` | Добавить 4 страницы | элемент экрана | открывает экран | Ноты рядом (sheet) | — | — |
| `import` | Вернуться к камере | элемент экрана | открывает экран | Ноты рядом (sheet) | — | — |
| `journal` | Настройки | элемент экрана | открывает экран | Настройки (settings) | — | — |
| `journal` | Открыть программы | элемент экрана | открывает экран | Весенний концерт (releases) | — | — |
| `journal` | Открыть сохранённые номера | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `journal` | Открыть загруженные ноты | элемент экрана | открывает экран | Ноты рядом (sheet) | — | — |
| `journal` | 22 марта · большой зал | элемент экрана | открывает экран | Весенний концерт (releases) | — | — |
| `journal` | Весенний свет | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `journal` | Тихая вода | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `journal` | Свет над рекой | элемент экрана | открывает экран | Партия альта (player) | — | — |
| `journal` | Раздел Главная | вкладка | переключает вкладку таб-бара | Главная (program) | — | — |
| `journal` | Раздел Поиск | вкладка | переключает вкладку таб-бара | Поиск (notes) | — | — |
| `journal` | Раздел Моя музыка | вкладка | переключает вкладку таб-бара | Моя музыка (journal) | — | — |
| `releases` | Назад | кнопка «назад» | возвращает к родительскому экрану | Главная (program) | — | — |
| `releases` | Настройки программы | элемент экрана | открывает экран | Настройки (settings) | — | — |
| `releases` | Слушать программу, | элемент экрана | открывает экран | Партия альта (player) | — | — |
| `releases` | Сосна | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `releases` | Северное сияние | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `releases` | Тихая вода | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `releases` | Весенний свет | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `releases` | Свет над рекой | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `releases` | Сосна | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `releases` | Свет над рекой | элемент экрана | открывает экран | Партия альта (player) | — | — |
| `notif` | СТРОЧКА | элемент экрана | открывает экран | Свет над рекой (piece) | — | — |
| `settings` | Назад | кнопка «назад» | возвращает к родительскому экрану | Главная (program) | — | — |
| `settings` | Управление | элемент экрана | открывает экран | Аккаунт (account) | — | — |
| `settings` | Сообщать о дополнениях | элемент экрана | спрашивает доступ push | Дополнение к программе (notif) | Настройки (settings) | `aps-environment` |
| `settings` | Проверить сейчас | элемент экрана | открывает экран | Весенний концерт (releases) | — | — |
| `settings` | Открыть настройки | элемент экрана | открывает экран | Настройки (settings) | — | — |
| `settings` | Свет над рекой | элемент экрана | открывает экран | Партия альта (player) | — | — |
<!-- @end -->

## Матрица доступов

<!-- @generated:perm-matrix -->
| Ключ | Жест пользователя | Экран | Если отказ | Риск Review |
|---|---|---|---|---|
| `NSCameraUsageDescription` | «Снять страницы» | Ноты рядом | Остаются буквы и такты из пака программы, страницы можно добрать из галереи | Низкий |
| `NSPhotoLibraryUsageDescription` | «Импорт из Фото» | Ноты рядом | Остаётся съёмка страниц камерой; список найденного и счётчик снимков исчезают | **Условный** — PHPhotoLibrary .readWrite и выборка по creationDate: PHPicker дал бы ручной выбор, но не нашёл бы снимки сам — а фича именно в том, что приложение обходит библиотеку по датам репетиций |
| `UIBackgroundModes: audio` | «В фоне» | Партия альта | Прослушивание на открытом экране остаётся, повтор фрагмента задаётся кнопкой | **Условный** — AVAudioSession .playback, заполненный MPNowPlayingInfoCenter (номер, голос, такт) и MPRemoteCommandCenter: play/pause, skipBackward как повтор фрагмента, next как следующая буква |
| `aps-environment` | Свитч «Сообщать о дополнениях» | Настройки | Дополнения всегда можно проверить вручную на этом экране | **Условный** — SDK push-провайдера в сборке, регистрация в APNs и дашборд редакции для отправки: своего API у приложения нет |
<!-- @end -->

## Почему здесь нет бэкенда

<!-- @generated:backendless -->
| Требовало бы сервера | Решение без сервера |
|---|---|
| Каталог программ и записи партий по голосам | Статический пак: program.json и аудио на CDN, только чтение |
| Права на записи | Пак собран из произведений общественного достояния, партии синтезированы по партитуре |
| Снятые страницы, пометки, журнал уверенности | FileManager и CoreData на устройстве: наружу не уходит ничего |
| Обмен страницами между хористами | Вырезано: UGC нет, значит нет ни модерации, ни чужих данных |
| Опциональные вход и регистрация по номеру телефона | SDK провайдера аутентификации, пароль и токен сессии в Keychain |
| Уведомление о дополнении к программе | SDK push-провайдера и его дашборд: своего обработчика нет |
<!-- @end -->

## Правила IA

**Три вкладки.** «Программа» — номера концерта и состояние загрузок; «Ноты» — снятые и найденные страницы по номерам; «Журнал» — уверенность по номерам и прогоны. Профиля нет: учётной записи в концепте тоже нет, есть номер телефона, и он лежит в «Настройках».

**Первый запуск без промптов.** `phone` → `code` → `program`. На входе доступов не спрашиваем вовсе; первый системный алерт возможен только после явного жеста на третьем экране и глубже.

**Один доступ — один жест, и жест назван предметно.** «Снять страницы», «Ноты с репетиции», «Погасить экран», «Сообщать о дополнениях». Ни один из них не называется «Разрешить доступ»: кнопка обещает результат, а не разрешение.

**Куда приводит отказ.** Отказ всегда возвращает на экран-источник, и именно там лежит видимый fallback:

| Отказ | Экран | Что исчезает | Что остаётся |
|---|---|---|---|
| Камера | `sheet` | Съёмка страниц и кнопка «переснять с. 3» | Буквы и такты идут с записью; страницы добираются из снимков в галерее |
| Фото | `notes` | Счётчик найденных снимков и весь разбор по датам | Ручная съёмка страниц в номере |
| Уведомления | `releases` | Автоматическое сообщение о дополнении | Строка «Проверить сейчас» на том же экране |

Правило соблюдено по построению: у камеры два триггера на `sheet` (кнопка и строка «с. 3»), у обоих ветка отказа ведёт на `sheet`, где `data-show-denied="camera"` и лежит.

**Сбой фичи ≠ отказ доступа.** На `import` три снимка из двенадцати не привязались к номеру: в кадр не попала шапка страницы. Это отдельное состояние с причиной человеческим языком, одним основным путём («выбрать номер вручную») и одним запасным («переснять развороты») — и оно показывается при *выданном* доступе.

**Фоновый режим активируется сценарием, а не алертом.** `UIBackgroundModes: audio` — entitlement: на `player` кнопка «Погасить экран» ведёт на `background` без системного запроса. Ровно там ревьюер видит Now Playing с номером, голосом и текущим тактом — то, что требует 2.5.4.

**Повторно не переспрашиваем.** Отвеченный ключ второй раз системный алерт не вызывает: свитчи на `settings` отражают статус (`data-switch`), а путь к смене — «Открыть Настройки» в снекбаре.
