# Друзья — карта экранов и архитектура

Заполняется по PLAYBOOK.md. Блоки `@generated:*` пересобираются командой `npm run docs -- druzya` — внутрь них руками не пишем.

## Информационная архитектура

Дерево выведено из разметки экранов и `concept.json`: вложенность — по `parent`, подпись «открывается» — реальный элемент прототипа. Первые два узла всегда одни и те же: вход по номеру (`phone` → `code`).

<!-- @generated:ia-tree -->
```
Вход по номеру (phone) — старт, без таб-бара · открывается: старт
    ├─ Пароль (password) — push, без таб-бара · открывается: «Далее»
    │   └─ Аккаунт (account) — push, без таб-бара · открывается: «Открыть»
    │       └─ Удаление аккаунта (deleteaccount) — push, без таб-бара · открывается: «Удалить аккаунт»
    └─ Создать аккаунт (register) — push, без таб-бара · открывается: «Создать аккаунт»
        └─ Пароль нового аккаунта (registerpassword) — push, без таб-бара · открывается: «Далее»

Лента (feed) — tab (root) · открывается: «Продолжить без аккаунта», «Войти» … · tracking
    ├─ Публикация (post) — push · открывается: «День рождения Марины, завтра», «День рождения Паши через неделю» … · photosadd
    │   └─ Телевизор (tv) — modal · открывается: «Смотреть на телевизоре», «Суббота у Дениса, во дворе» … · localnetwork, wifiinfo
    │       └─ Идёт показ (cast) — fullscreen · открывается: «Телевизор в гостиной» (localnetwork)
    ├─ Встреча (meetup) — push · открывается: «Суббота у Дениса», «Ответить про субботу» … · locationalways, commnotif, voip
    │   └─ Кто в пути (route) — push · открывается: «Отмечать дорогу» (locationalways), «Кто в пути»
    └─ Оповещения (notif) — modal · открывается: «Оповещения», «Настроить оповещения» · push, remotenotif

Новая публикация (compose) — modal · открывается: «Новая публикация», «Что нового?» … · camera, photos, location
    ├─ Съёмка (shoot) — fullscreen · открывается: «Снять фото», «Снять видео» …, «Снять» (camera)
    ├─ Медиатека (picker) — modal · открывается: «Из медиатеки» (photos), «Открыть медиатеку», «Выбрать из медиатеки»
    └─ Место (place) — modal · открывается: «Отметить место» (location)

Обсуждения (discussions) — tab (root) · открывается: вкладка таб-бара
    └─ Обсуждение (thread) — push · открывается: «Ответить Марине», «Ответить Свете» …

Музыка (music) — tab (root) · открывается: «Открыть плейлист «В дорогу к Денису»» · mic
    ├─ Видео (videos) — push · открывается: «Открыть видео», «Смотреть на телефоне» …
    └─ Плейлист (player) — push · открывается: «Слушать плейлист Светы», «Записать голосовую заметку» … · audio
        └─ Плеер на локскрине (background) — system · открывается: «Слушать» (audio)

Профиль (profile) — tab (root) · открывается: «Профиль Паши Ерохина», «Профиль Светы Хабаровой»
    ├─ Друзья (friends) — push · открывается: «Света, не виделись год», «Друзья» · contacts
    │   ├─ Найденные (match) — push · открывается: «Найти друзей» (contacts)
    │   └─ Приглашение (invite) — modal · открывается: «Пригласить ссылкой», «Позвать Лену Ким» …, «Ссылки открываются в приложении» (associateddomains)
    └─ Настройки (settings) — push · открывается: «Настройки», «Изменить профиль» · fetch, processing, bgtask, keychain, associateddomains

Круги (circles) — tab (root) · открывается: «Открыть карточку Дениса Кима», «Открыть карточку Юли Тарасовой» …
```
<!-- @end -->

## Карта экранов

<!-- @generated:screen-map -->
| ID | Название | Тип | Доступы |
|---|---|---|---|
| `phone` | Вход по номеру | старт, без таб-бара | — |
| `password` | Пароль | push, без таб-бара | — |
| `register` | Создать аккаунт | push, без таб-бара | — |
| `registerpassword` | Пароль нового аккаунта | push, без таб-бара | — |
| `account` | Аккаунт | push, без таб-бара | — |
| `deleteaccount` | Удаление аккаунта | push, без таб-бара | — |
| `feed` | Лента | tab (root) | tracking |
| `post` | Публикация | push | photosadd |
| `videos` | Видео | push | — |
| `compose` | Новая публикация | modal | camera, photos, location |
| `shoot` | Съёмка | fullscreen | — |
| `picker` | Медиатека | modal | — |
| `place` | Место | modal | — |
| `discussions` | Обсуждения | tab (root) | — |
| `thread` | Обсуждение | push | — |
| `music` | Музыка | tab (root) | mic |
| `player` | Плейлист | push | audio (activate) |
| `background` | Плеер на локскрине | system | — |
| `profile` | Профиль | tab (root) | — |
| `friends` | Друзья | push | contacts |
| `match` | Найденные | push | — |
| `circles` | Круги | tab (root) | — |
| `meetup` | Встреча | push | locationalways, commnotif (activate), voip (activate) |
| `route` | Кто в пути | push | — |
| `tv` | Телевизор | modal | localnetwork, wifiinfo (activate) |
| `cast` | Идёт показ | fullscreen | — |
| `notif` | Оповещения | modal | push, remotenotif (activate) |
| `settings` | Настройки | push | fetch (activate), processing (activate), bgtask (activate), keychain (activate), associateddomains (activate) |
| `invite` | Приглашение | modal | — |
<!-- @end -->

## Переходы: откуда куда и чем

Каждая строка — элемент, который есть в прототипе. Вкладки таб-бара опущены: они ведут в корни разделов и одинаковы на всех экранах.

## Действия на экранах

Каждый интерактивный элемент прототипа: что он делает, куда ведёт и какой ключ при этом запрашивается. В отличие от таблицы переходов выше, вкладки таб-бара, кнопки возврата и подтверждения без перехода тоже здесь — раздел выводится из разметки и отвечает на вопрос «что делает эта кнопка», не заставляя открывать HTML.

## Матрица доступов

## Почему здесь нет бэкенда

<!-- @generated:backendless -->
| Требовало бы сервера | Решение без сервера |
|---|---|
| Опциональные вход и регистрация по номеру телефона | SDK провайдера аутентификации, пароль и токен сессии в Keychain |
| Лента круга, обсуждения и подборки людей | Firestore как хранилище с security rules — конфигурация, а не свой код |
| Пуши о публикациях, ответах и днях рождения | Cloud Messaging: регистрация в APNs через SDK, отправка из дашборда провайдера |
| Снимки, видео и аудиозаметки | Storage провайдера, ссылки в документах Firestore |
| Место на публикации и подсказка «места рядом» | MKLocalSearch и CLGeocoder — карты Apple на устройстве |
| Показ снимков на телевизоре | Bonjour-поиск Google Cast в локальной сети, приём на стороне телевизора |
| Разбор снимков вечера | Vision на устройстве в BGProcessingTask, результат в локальную базу |
| Ссылки-приглашения в круг | Статический apple-app-site-association на druzya.app, обработка маршрута в приложении |
<!-- @end -->

## Правила IA

Дописать словами то, чего в таблицах не видно: почему экран открывается именно отсюда, что убирает fallback, где сбой фичи отличается от отказа в доступе.
