#!/usr/bin/env node
/**
 * Фотографии в «Земляках» показывает только ядровой `.ph`.
 * В assets/media хранится исключительно схема Гдова — данные экрана места,
 * а не декоративная иллюстрация или сгенерированная обложка.
 */
console.log('«Земляки»: декоративные медиа не генерируются; схема места хранится как данные.');
