# Архитектура

## Модули

- `RunPlan` и `RunSession` в SwiftData.
- `FrameGuide` на AVFoundation и Vision, без записи звука.
- `TimelineEngine` сравнивает фактические метки с планом.
- `LocalPlayback` играет локальную дорожку и подсказки.
- `PhoneAuthService` использует SDK провайдера; токен хранится в Keychain.
- `PermissionsService` содержит четыре системные возможности и не запрашивает их на входе.

## Внешняя инфраструктура

SDK входа нужен для номера и SMS-кода, APNs и push-SDK — только для напоминания. Ни одна из этих систем не хранит планы или видео.

## Карта экранов

<!-- @generated:screen-map -->
| ID | Название | Тип | Доступы |
|---|---|---|---|
| `phone` | Вход по номеру | старт, без таб-бара | — |
| `password` | Пароль | push, без таб-бара | — |
| `register` | Создать аккаунт | push, без таб-бара | — |
| `registerpassword` | Пароль нового аккаунта | push, без таб-бара | — |
| `account` | Аккаунт | push, без таб-бара | — |
| `deleteaccount` | Удаление аккаунта | push, без таб-бара | — |
| `today` | Сегодня | корневая рабочая поверхность | — |
| `setup` | Настройка | push | camera |
| `frame` | Проверка кадра | полноэкранная камера | — |
| `recording` | Прогон | рабочий таймер | — |
| `paused` | Пауза | состояние записи | — |
| `analysis` | Разбор | временная шкала | — |
| `moment` | Момент выступления | push фрагмента | audio (activate) |
| `history` | История | push из navigation bar | photo |
| `session` | Экран блокировки | системная поверхность | — |
| `settings` | Настройки | sheet из navigation bar | push |
<!-- @end -->

## Матрица доступов

Каждая строка читается так: пользователь делает жест — система спрашивает — при отказе остаётся рабочий путь.

<!-- @generated:perm-matrix -->
| Ключ | Жест пользователя | Экран | Если отказ | Риск Review |
|---|---|---|---|---|
| `NSCameraUsageDescription` | «Проверить кадр» | Настройка | Прогон по таймеру запускается без видео | Низкий |
| `NSPhotoLibraryUsageDescription` | «Импортировать видео» в истории | История | Можно начать новый прогон или открыть локальную историю | Низкий |
| `UIBackgroundModes: audio` | «Слушать с погашенным экраном» | Момент выступления | Дорожка и подсказки играют только пока приложение открыто | **Условный** — AVPlayer реально играет локальный файл; Now Playing и play/pause доступны на экране блокировки |
| `aps-environment` | Свитч «Напомнить вечером» | Настройки | План на сегодня остаётся на главном экране | **Условный** — Регистрация в APNs и запланированный push через панель провайдера |
<!-- @end -->

## Информационная архитектура

Дерево выведено из разметки экранов, а не нарисовано руками: рукописная схема расходится с прототипом первой же правкой.

<!-- @generated:ia-tree -->
```
Вход по номеру (phone) — старт, без таб-бара · открывается: старт
    ├─ Пароль (password) — push, без таб-бара · открывается: «Далее»
    │   └─ Аккаунт (account) — push, без таб-бара · открывается: «Управление»
    │       └─ Удаление аккаунта (deleteaccount) — push, без таб-бара · открывается: «Удалить аккаунт»
    ├─ Создать аккаунт (register) — push, без таб-бара · открывается: «Нет аккаунта? Создать»
    │   └─ Пароль нового аккаунта (registerpassword) — push, без таб-бара · открывается: «Далее»
    └─ Сегодня (today) — корневая рабочая поверхность · открывается: «Продолжить без аккаунта», «Войти» …
        ├─ Настройка (setup) — push · открывается: «Демо для команды», «Защита диплома» … · camera
        │   ├─ Проверка кадра (frame) — полноэкранная камера · открывается: «Проверить кадр» (camera)
        │   └─ Прогон (recording) — рабочий таймер · открывается: «Начать прогон», «Кадр готов» …
        │       ├─ Пауза (paused) — состояние записи · открывается: «Пауза»
        │       └─ Разбор (analysis) — временная шкала · открывается: «Пятница, 7:24», «Завершить» …, «Импортировать видео» (photo)
        │           └─ Момент выступления (moment) — push фрагмента · открывается: ««Решение» длиннее плана на 38 секунд», «Три метки внутри одного блока» … · audio
        ├─ История (history) — push из navigation bar · открывается: «История», «Все прогоны» · photo
        │   └─ Экран блокировки (session) — системная поверхность · открывается: «Слушать с погасшим экраном» (audio)
        └─ Настройки (settings) — sheet из navigation bar · открывается: «Настройки» · push
```
<!-- @end -->

## Переходы

<!-- @generated:transitions -->
| Экран | Что можно сделать | Ведёт на | Доступ | Тип перехода |
|---|---|---|---|---|
| `phone` | «Далее» | `password` | — | переход |
| `phone` | «Нет аккаунта? Создать» | `register` | — | переход |
| `phone` | «Продолжить без аккаунта» | `today` | — | переход |
| `password` | «Назад» | `phone` | — | возврат по IA |
| `password` | «Войти» | `today` | — | переход |
| `register` | «Назад» | `phone` | — | возврат по IA |
| `register` | «Далее» | `registerpassword` | — | переход |
| `register` | «Уже есть аккаунт? Войти» | `phone` | — | переход |
| `registerpassword` | «Назад» | `register` | — | возврат по IA |
| `registerpassword` | «Создать аккаунт» | `today` | — | переход |
| `account` | «Назад» | `password` | — | возврат по IA |
| `account` | «Выйти» | `phone` | — | переход |
| `account` | «Удалить аккаунт» | `deleteaccount` | — | переход |
| `deleteaccount` | «Назад» | `account` | — | возврат по IA |
| `deleteaccount` | «Удалить аккаунт» | `phone` | — | переход |
| `today` | «История», «Все прогоны» | `history` | — | переход |
| `today` | «Настройки» | `settings` | — | переход |
| `today` | «Пятница, 7:24» | `analysis` | — | переход |
| `today` | «Демо для команды», «Защита диплома» … | `setup` | — | переход |
| `setup` | «Проверить кадр» | `frame` | `NSCameraUsageDescription` | доступ разрешён |
| `setup` | «Проверить кадр» | `setup` | `NSCameraUsageDescription` | отказ → fallback |
| `setup` | «Начать прогон» | `recording` | — | переход |
| `frame` | «Закрыть» | `setup` | — | возврат по IA |
| `frame` | «Кадр готов» | `recording` | — | переход |
| `recording` | «Завершить» | `analysis` | — | переход |
| `recording` | «Пауза» | `paused` | — | переход |
| `paused` | «Завершить», «Завершить и разобрать» | `analysis` | — | переход |
| `paused` | «Продолжить» | `recording` | — | переход |
| `analysis` | ««Решение» длиннее плана на 38 секунд», «Три метки внутри одного блока» … | `moment` | — | переход |
| `analysis` | «Повторить прогон» | `setup` | — | переход |
| `moment` | «Слушать с погасшим экраном» | `session` | `UIBackgroundModes: audio` | entitlement, без alert |
| `moment` | «Повторить только этот блок» | `setup` | — | переход |
| `history` | «Воскресенье, 21:40», «−12» … | `analysis` | — | переход |
| `history` | «Импортировать видео» | `analysis` | `NSPhotoLibraryUsageDescription` | доступ разрешён |
| `history` | «Импортировать видео» | `history` | `NSPhotoLibraryUsageDescription` | отказ → fallback |
| `session` | — | — | — | дальше переходов нет |
| `settings` | «Закрыть» | `today` | — | возврат по IA |
| `settings` | «Напомнить вечером» | `settings` | `aps-environment` | доступ разрешён |
| `settings` | «Управление» | `account` | — | переход |
<!-- @end -->

## Действия на экранах

Каждый интерактивный элемент прототипа: что он делает, куда ведёт и какой ключ при этом запрашивается. В отличие от таблицы переходов выше, вкладки таб-бара, кнопки возврата и подтверждения без перехода тоже здесь — раздел выводится из разметки и отвечает на вопрос «что делает эта кнопка», не заставляя открывать HTML.

<!-- @generated:actions -->
| Экран | Элемент | Роль | Что делает | Ведёт на | При отказе | Ключ |
|---|---|---|---|---|---|---|
| `phone` | Далее | элемент экрана | открывает экран | Пароль (password) | — | — |
| `phone` | Нет аккаунта? Создать | элемент экрана | открывает экран | Создать аккаунт (register) | — | — |
| `phone` | Продолжить без аккаунта | элемент экрана | открывает экран | Сегодня (today) | — | — |
| `phone` | пользовательское соглашение | элемент экрана | показывает подтверждение «Соглашение · rehearsal.app/terms» | остаётся на экране | — | — |
| `phone` | политику конфиденциальности | элемент экрана | показывает подтверждение «Политика · rehearsal.app/privacy» | остаётся на экране | — | — |
| `phone` | Помощь и поддержка | элемент экрана | показывает подтверждение «Справка · rehearsal.app/help · support@rehearsal.app» | остаётся на экране | — | — |
| `password` | Назад | кнопка «назад» | возвращает к родительскому экрану | Вход по номеру (phone) | — | — |
| `password` | Войти | элемент экрана | открывает экран | Сегодня (today) | — | — |
| `password` | Забыли пароль? | элемент экрана | показывает подтверждение «Ссылка для восстановления отправлена» | остаётся на экране | — | — |
| `register` | Назад | кнопка «назад» | возвращает к родительскому экрану | Вход по номеру (phone) | — | — |
| `register` | Далее | элемент экрана | открывает экран | Пароль нового аккаунта (registerpassword) | — | — |
| `register` | Уже есть аккаунт? Войти | элемент экрана | открывает экран | Вход по номеру (phone) | — | — |
| `registerpassword` | Назад | кнопка «назад» | возвращает к родительскому экрану | Создать аккаунт (register) | — | — |
| `registerpassword` | Создать аккаунт | элемент экрана | открывает экран | Сегодня (today) | — | — |
| `registerpassword` | пользовательское соглашение | элемент экрана | показывает подтверждение «Соглашение · rehearsal.app/terms» | остаётся на экране | — | — |
| `registerpassword` | политику конфиденциальности | элемент экрана | показывает подтверждение «Политика · rehearsal.app/privacy» | остаётся на экране | — | — |
| `registerpassword` | Помощь и поддержка | элемент экрана | показывает подтверждение «Справка · rehearsal.app/help · support@rehearsal.app» | остаётся на экране | — | — |
| `account` | Назад | кнопка «назад» | возвращает к родительскому экрану | Пароль (password) | — | — |
| `account` | Выйти | элемент экрана | открывает экран | Вход по номеру (phone) | — | — |
| `account` | Удалить аккаунт | элемент экрана | открывает экран | Удаление аккаунта (deleteaccount) | — | — |
| `deleteaccount` | Назад | кнопка «назад» | возвращает к родительскому экрану | Аккаунт (account) | — | — |
| `deleteaccount` | Удалить аккаунт | элемент экрана | открывает экран | Вход по номеру (phone) | — | — |
| `today` | История | элемент экрана | открывает экран | История (history) | — | — |
| `today` | Настройки | элемент экрана | открывает экран | Настройки (settings) | — | — |
| `today` | Пятница, 7:24 | элемент экрана | открывает экран | Разбор (analysis) | — | — |
| `today` | Все прогоны | элемент экрана | открывает экран | История (history) | — | — |
| `today` | Демо для команды | элемент экрана | открывает экран | Настройка (setup) | — | — |
| `today` | Защита диплома | элемент экрана | открывает экран | Настройка (setup) | — | — |
| `today` | Новое выступление | элемент экрана | открывает экран | Настройка (setup) | — | — |
| `today` | Начать прогон | элемент экрана | открывает экран | Настройка (setup) | — | — |
| `setup` | Готово | элемент экрана | показывает подтверждение «План сохранён» | остаётся на экране | — | — |
| `setup` | Проверить кадр | элемент экрана | спрашивает доступ camera | Проверка кадра (frame) | Настройка (setup) | `NSCameraUsageDescription` |
| `setup` | Начать прогон | элемент экрана | открывает экран | Прогон (recording) | — | — |
| `frame` | Закрыть | кнопка «назад» | возвращает к родительскому экрану | Настройка (setup) | — | — |
| `frame` | Кадр готов | элемент экрана | открывает экран | Прогон (recording) | — | — |
| `recording` | Завершить | элемент экрана | открывает экран | Разбор (analysis) | — | — |
| `recording` | Отметить | элемент экрана | показывает подтверждение «Момент отмечен · 03:12» | остаётся на экране | — | — |
| `recording` | Пауза | элемент экрана | открывает экран | Пауза (paused) | — | — |
| `paused` | Завершить | элемент экрана | открывает экран | Разбор (analysis) | — | — |
| `paused` | Завершить и разобрать | элемент экрана | открывает экран | Разбор (analysis) | — | — |
| `paused` | Продолжить | элемент экрана | открывает экран | Прогон (recording) | — | — |
| `analysis` | Готово | элемент экрана | показывает подтверждение «Разбор сохранён» | остаётся на экране | — | — |
| `analysis` | «Решение» длиннее плана на 38 секунд | элемент экрана | открывает экран | Момент выступления (moment) | — | — |
| `analysis` | Три метки внутри одного блока | элемент экрана | открывает экран | Момент выступления (moment) | — | — |
| `analysis` | «Следующий шаг» короче плана на 10 секунд | элемент экрана | открывает экран | Момент выступления (moment) | — | — |
| `analysis` | Повторить прогон | элемент экрана | открывает экран | Настройка (setup) | — | — |
| `moment` | Слушать с погасшим экраном | элемент экрана | включает entitlement audio, системного alert нет | Экран блокировки (session) | — | `UIBackgroundModes: audio` |
| `moment` | Повторить только этот блок | элемент экрана | открывает экран | Настройка (setup) | — | — |
| `history` | Воскресенье, 21:40 | элемент экрана | открывает экран | Разбор (analysis) | — | — |
| `history` | Воскресенье, 21:40 | элемент экрана | открывает экран | Разбор (analysis) | — | — |
| `history` | −12 | элемент экрана | открывает экран | Разбор (analysis) | — | — |
| `history` | 7:31 | элемент экрана | открывает экран | Разбор (analysis) | — | — |
| `history` | 7:58 | элемент экрана | открывает экран | Разбор (analysis) | — | — |
| `history` | 8:41 | элемент экрана | открывает экран | Разбор (analysis) | — | — |
| `history` | Импортировать видео | элемент экрана | спрашивает доступ photo | Разбор (analysis) | История (history) | `NSPhotoLibraryUsageDescription` |
| `session` | — | — | интерактивных элементов нет | — | — | — |
| `settings` | Закрыть | кнопка «назад» | возвращает к родительскому экрану | Сегодня (today) | — | — |
| `settings` | Напомнить вечером | элемент экрана | спрашивает доступ push | Настройки (settings) | Настройки (settings) | `aps-environment` |
| `settings` | Экспортировать архив | элемент экрана | показывает подтверждение «Архив подготовлен» | остаётся на экране | — | — |
| `settings` | Управление | элемент экрана | открывает экран | Аккаунт (account) | — | — |
<!-- @end -->

## Граница «без бэкенда»

<!-- @generated:backendless -->
| Требовало бы сервера | Решение без сервера |
|---|---|
| Учёт прогонов | SwiftData на устройстве |
| Аудио и видео | Локальные файлы в Application Support; никакого стриминга |
| Разбор прогона | Vision и метки по таймкодам на устройстве |
| Напоминания | SDK push-провайдера без своего API |
| Опциональные вход и регистрация по номеру телефона | SDK провайдера аутентификации, пароль и токен сессии в Keychain |
| Перенос данных | Явный экспорт одного архива через Share Sheet |
<!-- @end -->
