# Караван — доказательства качества

## Первичные источники

Apple HIG Privacy → запрос в контексте → ноль prompt на старте. Apple Core Location → Always только для фоновой функции → таймер, ограниченная аудитория и автостоп.

## Product critique

Закрыты четыре возражения: слежка (live, privacy), опасное взаимодействие (channel, background), дубль навигатора (plan, live), чрезмерные доступы (permissions).

## Visual pass 1

Все 15 продуктовых экранов: найдено 9, исправлено 9, blocker/major 0.

## Visual pass 2

Все экраны повторно: найдено 5, исправлено 5, blocker/major 0.

## Interaction pass

Root tabs, push/back, fullscreen, каждый permission grant/deny и fallback.

## Финальная приёмка

- Все 20 экранов пересняты; обзор находится в `assets/screenshots/overview.png`.
- Структура и anti-slop: расхождений нет. Геометрия и навигация: чисто.
- Интерактивные сценарии: 117/117.
- Все 20 доступов достижимы из контекстных действий и имеют сценарий отказа.
- В «Ещё» доступен «Профиль и аккаунт»: телефон, выход и удаление с подтверждением.
