# Склейка — карта экранов и архитектура

Архитектура принципиально локальная: Core Data хранит описание и edit history, FileManager — исходники, proxy, draft и export. Блоки `@generated:*` пересобираются из спеки.

## Информационная архитектура

Дерево выведено из `parent` и реальных UI-переходов. Старт — локальные проекты; входа по номеру и аккаунта нет.

<!-- @generated:ia-tree -->
```
Вход по номеру (phone) — старт, без таб-бара · открывается: старт
    ├─ Пароль (password) — push, без таб-бара · открывается: «Далее»
    │   └─ Аккаунт (account) — push, без таб-бара · открывается: «Открыть»
    │       └─ Удаление аккаунта (deleteaccount) — push, без таб-бара · открывается: «Удалить аккаунт»
    └─ Создать аккаунт (register) — push, без таб-бара · открывается: «Создать аккаунт»
        └─ Пароль нового аккаунта (registerpassword) — push, без таб-бара · открывается: «Далее»

Локальные проекты (projects) — tab (root) · открывается: «Продолжить без аккаунта», «Войти» …
    ├─ Новое событие (create) — push · открывается: «Создать событие», «Выбрать место» … · location
    │   └─ Место события (place) — system · открывается: «Место», «Повторить» (location), «Ввести вручную»
    └─ Выходные у озера (project) — push · открывается: «38 ВИДЕО», «Алматы, длинные выходные» … · camera, mic, photo
        ├─ Запросить материалы (invite) — modal · открывается: «Попросить видео»
        │   └─ Поделиться запросом (share) — system · открывается: «Поделиться запросом», «Отправить файл» …
        ├─ Камера (camera) — fullscreen · открывается: «Снять» (camera + mic), «Продолжить»
        ├─ Выбрать видео (import) — system · открывается: «Из Фото» (photo), «Из Файлов», «Добавить» …
        ├─ Сборка черновика (processing) — push · открывается: «Собрать черновик»
        └─ Черновик готов (draft) — push · открывается: «День рождения Леры», «Открыть черновик» …
            ├─ Порядок фрагментов (editor) — push · открывается: «Править порядок»
            └─ Просмотр фильма (viewer) — fullscreen · открывается: «Финал летнего концерта», «НЕДАВНО СОХРАНЁН» … · localnet, audio
                ├─ Смотреть на телевизоре (cast) — system · открывается: «Смотреть на телевизоре», «Повторить» (localnet), «iPhone»
                ├─ Экран погашен (background) — system · открывается: «Слушать с погашенным экраном» (audio)
                └─ Экспорт (export) — push · открывается: «Поделиться фильмом», «Экспортировать» · photoadd
                    └─ Фильм сохранён (saved) — state · открывается: «Сохранить в Фото», «Повторить» (photoadd)

Настройки (settings) — tab (root) · открывается: вкладка таб-бара

Архив (archive) — tab (root) · открывается: «Готовые фильмы»
```
<!-- @end -->

## Карта экранов

<!-- @generated:screen-map -->
| ID | Название | Тип | Доступы |
|---|---|---|---|
| `phone` | Вход по номеру | старт, без таб-бара | — |
| `password` | Пароль | push, без таб-бара | — |
| `register` | Создать аккаунт | push, без таб-бара | — |
| `registerpassword` | Пароль нового аккаунта | push, без таб-бара | — |
| `account` | Аккаунт | push, без таб-бара | — |
| `deleteaccount` | Удаление аккаунта | push, без таб-бара | — |
| `projects` | Локальные проекты | tab (root) | — |
| `settings` | Настройки | tab (root) | — |
| `archive` | Архив | tab (root) | — |
| `create` | Новое событие | push | location |
| `place` | Место события | system | — |
| `project` | Выходные у озера | push | camera, mic, photo |
| `invite` | Запросить материалы | modal | — |
| `share` | Поделиться запросом | system | — |
| `camera` | Камера | fullscreen | — |
| `import` | Выбрать видео | system | — |
| `processing` | Сборка черновика | push | — |
| `draft` | Черновик готов | push | — |
| `editor` | Порядок фрагментов | push | — |
| `viewer` | Просмотр фильма | fullscreen | localnet, audio (activate) |
| `cast` | Смотреть на телевизоре | system | — |
| `background` | Экран погашен | system | — |
| `export` | Экспорт | push | photoadd |
| `saved` | Фильм сохранён | state | — |
<!-- @end -->

## Переходы: откуда куда и чем

Каждая строка — элемент, который есть в прототипе. Вкладки таб-бара опущены: они ведут в корни разделов и одинаковы на всех экранах.

<!-- @generated:transitions -->
| Экран | Что можно сделать | Ведёт на | Доступ | Тип перехода |
|---|---|---|---|---|
| `phone` | «Далее» | `password` | — | переход |
| `phone` | «Создать аккаунт» | `register` | — | переход |
| `phone` | «Продолжить без аккаунта» | `projects` | — | переход |
| `password` | «Назад» | `phone` | — | возврат по IA |
| `password` | «Войти» | `projects` | — | переход |
| `register` | «Назад» | `phone` | — | возврат по IA |
| `register` | «Далее» | `registerpassword` | — | переход |
| `register` | «Уже есть аккаунт? Войти» | `phone` | — | переход |
| `registerpassword` | «Назад» | `register` | — | возврат по IA |
| `registerpassword` | «Создать аккаунт» | `projects` | — | переход |
| `account` | «Назад» | `password` | — | возврат по IA |
| `account` | «Выйти» | `phone` | — | переход |
| `account` | «Удалить аккаунт» | `deleteaccount` | — | переход |
| `deleteaccount` | «Назад» | `account` | — | возврат по IA |
| `deleteaccount` | «Удалить аккаунт» | `phone` | — | переход |
| `projects` | «Создать событие» | `create` | — | переход |
| `projects` | «38 ВИДЕО», «Алматы, длинные выходные» | `project` | — | переход |
| `projects` | «День рождения Леры» | `draft` | — | переход |
| `projects` | «Финал летнего концерта» | `viewer` | — | переход |
| `settings` | «Готовые фильмы» | `archive` | — | переход |
| `settings` | «Открыть» | `account` | — | переход |
| `archive` | «НЕДАВНО СОХРАНЁН», «Открыть последний фильм» … | `viewer` | — | переход |
| `create` | «Назад» | `projects` | — | возврат по IA |
| `create` | «Место», «Повторить» | `place` | `NSLocationWhenInUseUsageDescription` | доступ разрешён |
| `create` | «Место», «Повторить» | `create` | `NSLocationWhenInUseUsageDescription` | отказ → fallback |
| `create` | «Ввести вручную» | `place` | — | переход |
| `create` | «Создать событие» | `project` | — | переход |
| `place` | «Закрыть» | `create` | — | возврат по IA |
| `place` | «Выбрать место», «Ввести адрес вручную» … | `create` | — | переход |
| `project` | «Назад» | `projects` | — | возврат по IA |
| `project` | «Попросить видео» | `invite` | — | переход |
| `project` | «Снять» | `camera` | `NSCameraUsageDescription + NSMicrophoneUsageDescription` | доступ разрешён |
| `project` | «Снять» | `project` | `NSCameraUsageDescription + NSMicrophoneUsageDescription` | отказ → fallback |
| `project` | «Из Фото» | `import` | `NSPhotoLibraryUsageDescription` | доступ разрешён |
| `project` | «Из Фото» | `project` | `NSPhotoLibraryUsageDescription` | отказ → fallback |
| `project` | «Из Файлов», «Добавить» | `import` | — | переход |
| `project` | «Продолжить» | `camera` | — | переход |
| `project` | «Собрать черновик» | `processing` | — | переход |
| `invite` | «Назад» | `project` | — | возврат по IA |
| `invite` | «Поделиться запросом» | `share` | — | переход |
| `share` | — | — | — | дальше переходов нет |
| `camera` | «Закрыть» | `project` | — | возврат по IA |
| `camera` | «Записать фрагмент» | `project` | — | переход |
| `camera` | «Импортировать видео» | `import` | — | переход |
| `import` | «Назад» | `project` | — | возврат по IA |
| `import` | «Готово» | `project` | — | переход |
| `processing` | «Назад» | `project` | — | возврат по IA |
| `processing` | «Открыть черновик» | `draft` | — | переход |
| `draft` | «Назад» | `project` | — | возврат по IA |
| `draft` | «На весь экран», «Воспроизвести» … | `viewer` | — | переход |
| `draft` | «Править порядок» | `editor` | — | переход |
| `editor` | «Назад» | `draft` | — | возврат по IA |
| `editor` | «Готово» | `draft` | — | переход |
| `viewer` | «Закрыть» | `draft` | — | возврат по IA |
| `viewer` | «Смотреть на телевизоре», «Повторить» | `cast` | `NSLocalNetworkUsageDescription (+ NSBonjourServices)` | доступ разрешён |
| `viewer` | «Смотреть на телевизоре», «Повторить» | `viewer` | `NSLocalNetworkUsageDescription (+ NSBonjourServices)` | отказ → fallback |
| `viewer` | «Поделиться фильмом», «Экспортировать» | `export` | — | переход |
| `viewer` | «Слушать с погашенным экраном» | `background` | `UIBackgroundModes: audio` | entitlement, без alert |
| `cast` | «iPhone», «Гостиная» … | `viewer` | — | переход |
| `background` | «Пауза» | `viewer` | — | возврат по IA |
| `background` | «iPhone» | `cast` | — | переход |
| `export` | «Назад» | `viewer` | — | возврат по IA |
| `export` | «Сохранить в Фото», «Повторить» | `saved` | `NSPhotoLibraryAddUsageDescription` | доступ разрешён |
| `export` | «Сохранить в Фото», «Повторить» | `export` | `NSPhotoLibraryAddUsageDescription` | отказ → fallback |
| `export` | «Отправить файл» | `share` | — | переход |
| `saved` | «Вернуться в проект» | `project` | — | переход |
| `saved` | «Отправить фильм» | `share` | — | переход |
<!-- @end -->

## Действия на экранах

Каждый интерактивный элемент прототипа: что он делает, куда ведёт и какой ключ при этом запрашивается. В отличие от таблицы переходов выше, вкладки таб-бара, кнопки возврата и подтверждения без перехода тоже здесь — раздел выводится из разметки и отвечает на вопрос «что делает эта кнопка», не заставляя открывать HTML.

<!-- @generated:actions -->
| Экран | Элемент | Роль | Что делает | Ведёт на | При отказе | Ключ |
|---|---|---|---|---|---|---|
| `phone` | Далее | элемент экрана | открывает экран | Пароль (password) | — | — |
| `phone` | Создать аккаунт | элемент экрана | открывает экран | Создать аккаунт (register) | — | — |
| `phone` | Продолжить без аккаунта | элемент экрана | открывает экран | Локальные проекты (projects) | — | — |
| `phone` | пользовательское соглашение | элемент экрана | показывает подтверждение «Соглашение · skleyka.video/terms» | остаётся на экране | — | — |
| `phone` | политику конфиденциальности | элемент экрана | показывает подтверждение «Политика · skleyka.video/privacy» | остаётся на экране | — | — |
| `phone` | Помощь и поддержка | элемент экрана | показывает подтверждение «Справка · skleyka.video/help · support@skleyka.video» | остаётся на экране | — | — |
| `password` | Назад | кнопка «назад» | возвращает к родительскому экрану | Вход по номеру (phone) | — | — |
| `password` | Войти | элемент экрана | открывает экран | Локальные проекты (projects) | — | — |
| `password` | Забыли пароль? | элемент экрана | показывает подтверждение «Ссылка для восстановления отправлена» | остаётся на экране | — | — |
| `register` | Назад | кнопка «назад» | возвращает к родительскому экрану | Вход по номеру (phone) | — | — |
| `register` | Далее | элемент экрана | открывает экран | Пароль нового аккаунта (registerpassword) | — | — |
| `register` | Уже есть аккаунт? Войти | элемент экрана | открывает экран | Вход по номеру (phone) | — | — |
| `registerpassword` | Назад | кнопка «назад» | возвращает к родительскому экрану | Создать аккаунт (register) | — | — |
| `registerpassword` | Создать аккаунт | элемент экрана | открывает экран | Локальные проекты (projects) | — | — |
| `registerpassword` | пользовательское соглашение | элемент экрана | показывает подтверждение «Соглашение · skleyka.video/terms» | остаётся на экране | — | — |
| `registerpassword` | политику конфиденциальности | элемент экрана | показывает подтверждение «Политика · skleyka.video/privacy» | остаётся на экране | — | — |
| `registerpassword` | Помощь и поддержка | элемент экрана | показывает подтверждение «Справка · skleyka.video/help · support@skleyka.video» | остаётся на экране | — | — |
| `account` | Назад | кнопка «назад» | возвращает к родительскому экрану | Пароль (password) | — | — |
| `account` | Выйти | элемент экрана | открывает экран | Вход по номеру (phone) | — | — |
| `account` | Удалить аккаунт | элемент экрана | открывает экран | Удаление аккаунта (deleteaccount) | — | — |
| `deleteaccount` | Назад | кнопка «назад» | возвращает к родительскому экрану | Аккаунт (account) | — | — |
| `deleteaccount` | Удалить аккаунт | элемент экрана | открывает экран | Вход по номеру (phone) | — | — |
| `projects` | Создать событие | элемент экрана | открывает экран | Новое событие (create) | — | — |
| `projects` | 38 ВИДЕО | элемент экрана | открывает экран | Выходные у озера (project) | — | — |
| `projects` | День рождения Леры | элемент экрана | открывает экран | Черновик готов (draft) | — | — |
| `projects` | Финал летнего концерта | элемент экрана | открывает экран | Просмотр фильма (viewer) | — | — |
| `projects` | Алматы, длинные выходные | элемент экрана | открывает экран | Выходные у озера (project) | — | — |
| `projects` | Проекты | вкладка | переключает вкладку таб-бара | Локальные проекты (projects) | — | — |
| `projects` | Архив | вкладка | переключает вкладку таб-бара | Архив (archive) | — | — |
| `projects` | Настройки | вкладка | переключает вкладку таб-бара | Настройки (settings) | — | — |
| `settings` | Освободить место | элемент экрана | показывает подтверждение «Освобождено 1,2 ГБ» | остаётся на экране | — | — |
| `settings` | Готовые фильмы | элемент экрана | открывает экран | Архив (archive) | — | — |
| `settings` | Камера и микрофон | элемент экрана | показывает подтверждение «Открыты Настройки iOS» | остаётся на экране | — | — |
| `settings` | Фото | элемент экрана | показывает подтверждение «Открыты Настройки iOS» | остаётся на экране | — | — |
| `settings` | Геопозиция | элемент экрана | показывает подтверждение «Открыты Настройки iOS» | остаётся на экране | — | — |
| `settings` | Проекты | вкладка | переключает вкладку таб-бара | Локальные проекты (projects) | — | — |
| `settings` | Архив | вкладка | переключает вкладку таб-бара | Архив (archive) | — | — |
| `settings` | Настройки | вкладка | переключает вкладку таб-бара | Настройки (settings) | — | — |
| `settings` | Открыть | элемент экрана | открывает экран | Аккаунт (account) | — | — |
| `archive` | Сортировать | элемент экрана | показывает подтверждение «Сортировка по дате» | остаётся на экране | — | — |
| `archive` | НЕДАВНО СОХРАНЁН | элемент экрана | открывает экран | Просмотр фильма (viewer) | — | — |
| `archive` | Открыть последний фильм | элемент экрана | открывает экран | Просмотр фильма (viewer) | — | — |
| `archive` | Финал летнего концерта | элемент экрана | открывает экран | Просмотр фильма (viewer) | — | — |
| `archive` | Алматы, длинные выходные | элемент экрана | открывает экран | Просмотр фильма (viewer) | — | — |
| `archive` | Проекты | вкладка | переключает вкладку таб-бара | Локальные проекты (projects) | — | — |
| `archive` | Архив | вкладка | переключает вкладку таб-бара | Архив (archive) | — | — |
| `archive` | Настройки | вкладка | переключает вкладку таб-бара | Настройки (settings) | — | — |
| `create` | Назад | кнопка «назад» | возвращает к родительскому экрану | Локальные проекты (projects) | — | — |
| `create` | Место | элемент экрана | спрашивает доступ location | Место события (place) | Новое событие (create) | `NSLocationWhenInUseUsageDescription` |
| `create` | Ввести вручную | элемент экрана | открывает экран | Место события (place) | — | — |
| `create` | Повторить | элемент экрана | спрашивает доступ location | Место события (place) | Новое событие (create) | `NSLocationWhenInUseUsageDescription` |
| `create` | Создать событие | элемент экрана | открывает экран | Выходные у озера (project) | — | — |
| `place` | Закрыть | кнопка «назад» | возвращает к родительскому экрану | Новое событие (create) | — | — |
| `place` | Найти место | элемент экрана | показывает подтверждение «Поиск места» | остаётся на экране | — | — |
| `place` | Выбрать место | элемент экрана | открывает экран | Новое событие (create) | — | — |
| `place` | Ввести адрес вручную | элемент экрана | открывает экран | Новое событие (create) | — | — |
| `place` | Повторить | элемент экрана | открывает экран | Новое событие (create) | — | — |
| `project` | Назад | кнопка «назад» | возвращает к родительскому экрану | Локальные проекты (projects) | — | — |
| `project` | Попросить видео | элемент экрана | открывает экран | Запросить материалы (invite) | — | — |
| `project` | Снять | элемент экрана | спрашивает доступы цепочкой: camera → mic | Камера (camera) | Выходные у озера (project) | `NSCameraUsageDescription + NSMicrophoneUsageDescription` |
| `project` | Из Фото | элемент экрана | спрашивает доступ photo | Выбрать видео (import) | Выходные у озера (project) | `NSPhotoLibraryUsageDescription` |
| `project` | Из Файлов | элемент экрана | открывает экран | Выбрать видео (import) | — | — |
| `project` | Настройки | элемент экрана | показывает подтверждение «Открыты Настройки iOS» | остаётся на экране | — | — |
| `project` | Продолжить | элемент экрана | открывает экран | Камера (camera) | — | — |
| `project` | Настройки | элемент экрана | показывает подтверждение «Открыты Настройки iOS» | остаётся на экране | — | — |
| `project` | Добавить | элемент экрана | открывает экран | Выбрать видео (import) | — | — |
| `project` | Собрать черновик | элемент экрана | открывает экран | Сборка черновика (processing) | — | — |
| `invite` | Назад | кнопка «назад» | возвращает к родительскому экрану | Выходные у озера (project) | — | — |
| `invite` | Поделиться запросом | элемент экрана | открывает экран | Поделиться запросом (share) | — | — |
| `share` | Сообщения | элемент экрана | показывает подтверждение «Карточка отправлена» | остаётся на экране | — | — |
| `share` | Копировать | элемент экрана | показывает подтверждение «Текст скопирован» | остаётся на экране | — | — |
| `share` | AirDrop | элемент экрана | показывает подтверждение «AirDrop выбран» | остаётся на экране | — | — |
| `camera` | Закрыть | кнопка «назад» | возвращает к родительскому экрану | Выходные у озера (project) | — | — |
| `camera` | Записать фрагмент | элемент экрана | открывает экран | Выходные у озера (project) | — | — |
| `camera` | Импортировать видео | элемент экрана | открывает экран | Выбрать видео (import) | — | — |
| `import` | Назад | кнопка «назад» | возвращает к родительскому экрану | Выходные у озера (project) | — | — |
| `import` | Готово | элемент экрана | открывает экран | Выходные у озера (project) | — | — |
| `processing` | Назад | кнопка «назад» | возвращает к родительскому экрану | Выходные у озера (project) | — | — |
| `processing` | Открыть черновик | элемент экрана | открывает экран | Черновик готов (draft) | — | — |
| `draft` | Назад | кнопка «назад» | возвращает к родительскому экрану | Выходные у озера (project) | — | — |
| `draft` | На весь экран | элемент экрана | открывает экран | Просмотр фильма (viewer) | — | — |
| `draft` | Воспроизвести | элемент экрана | открывает экран | Просмотр фильма (viewer) | — | — |
| `draft` | Править порядок | элемент экрана | открывает экран | Порядок фрагментов (editor) | — | — |
| `draft` | Смотреть целиком | элемент экрана | открывает экран | Просмотр фильма (viewer) | — | — |
| `editor` | Назад | кнопка «назад» | возвращает к родительскому экрану | Черновик готов (draft) | — | — |
| `editor` | Готово | элемент экрана | открывает экран | Черновик готов (draft) | — | — |
| `editor` | Раньше | элемент экрана | показывает подтверждение «Фрагмент сдвинут раньше» | остаётся на экране | — | — |
| `editor` | Позже | элемент экрана | показывает подтверждение «Фрагмент сдвинут позже» | остаётся на экране | — | — |
| `editor` | Удалить | элемент экрана | показывает подтверждение «Фрагмент удалён» | остаётся на экране | — | — |
| `viewer` | Закрыть | кнопка «назад» | возвращает к родительскому экрану | Черновик готов (draft) | — | — |
| `viewer` | Смотреть на телевизоре | элемент экрана | спрашивает доступ localnet | Смотреть на телевизоре (cast) | Просмотр фильма (viewer) | `NSLocalNetworkUsageDescription (+ NSBonjourServices)` |
| `viewer` | Поделиться фильмом | элемент экрана | открывает экран | Экспорт (export) | — | — |
| `viewer` | Слушать с погашенным экраном | элемент экрана | включает entitlement audio, системного alert нет | Экран погашен (background) | — | `UIBackgroundModes: audio` |
| `viewer` | Экспортировать | элемент экрана | открывает экран | Экспорт (export) | — | — |
| `viewer` | Повторить | элемент экрана | спрашивает доступ localnet | Смотреть на телевизоре (cast) | Просмотр фильма (viewer) | `NSLocalNetworkUsageDescription (+ NSBonjourServices)` |
| `viewer` | Настройки | элемент экрана | показывает подтверждение «Открыты Настройки iOS» | остаётся на экране | — | — |
| `cast` | iPhone | элемент экрана | открывает экран | Просмотр фильма (viewer) | — | — |
| `cast` | Гостиная | элемент экрана | открывает экран | Просмотр фильма (viewer) | — | — |
| `cast` | Проектор | элемент экрана | открывает экран | Просмотр фильма (viewer) | — | — |
| `background` | Пауза | кнопка «назад» | возвращает к родительскому экрану | Просмотр фильма (viewer) | — | — |
| `background` | iPhone | элемент экрана | открывает экран | Смотреть на телевизоре (cast) | — | — |
| `export` | Назад | кнопка «назад» | возвращает к родительскому экрану | Просмотр фильма (viewer) | — | — |
| `export` | Сохранить в Фото | элемент экрана | спрашивает доступ photoadd | Фильм сохранён (saved) | Экспорт (export) | `NSPhotoLibraryAddUsageDescription` |
| `export` | Отправить файл | элемент экрана | открывает экран | Поделиться запросом (share) | — | — |
| `export` | Повторить | элемент экрана | спрашивает доступ photoadd | Фильм сохранён (saved) | Экспорт (export) | `NSPhotoLibraryAddUsageDescription` |
| `export` | Открыть Настройки | элемент экрана | показывает подтверждение «Открыты Настройки iOS» | остаётся на экране | — | — |
| `saved` | Вернуться в проект | элемент экрана | открывает экран | Выходные у озера (project) | — | — |
| `saved` | Отправить фильм | элемент экрана | открывает экран | Поделиться запросом (share) | — | — |
<!-- @end -->

## Матрица доступов

<!-- @generated:perm-matrix -->
| Ключ | Жест пользователя | Экран | Если отказ | Риск Review |
|---|---|---|---|---|
| `NSCameraUsageDescription` | «Снять» в локальном проекте | Выходные у озера | Остаётся импорт готового видео и переход в Настройки | Низкий |
| `NSMicrophoneUsageDescription` | «Снять» в локальном проекте | Выходные у озера | Камера записывает немое видео; микрофон можно включить в Настройках | Низкий |
| `NSPhotoLibraryUsageDescription` | «Из Фото» в локальном проекте | Выходные у озера | Остаются камера и импорт из Files через системный document picker | Низкий |
| `NSLocationWhenInUseUsageDescription` | «Моё местоположение» при создании | Новое событие | Место вводится вручную; группировка по времени и metadata продолжает работать | Средний |
| `NSLocalNetworkUsageDescription (+ NSBonjourServices)` | Кнопка Cast в плеере | Просмотр фильма | Фильм продолжает играть на iPhone; локальный файл и редактор доступны | Средний |
| `UIBackgroundModes: audio` | «Слушать с погашенным экраном» | Просмотр фильма | Пока экран активен, локальный плеер работает без фонового режима | **Условный** — AVAudioSession playback, Now Playing и remote commands |
| `NSPhotoLibraryAddUsageDescription` | «Сохранить в Фото» | Экспорт | Готовый файл остаётся в локальном проекте и доступен через Share Sheet или Files | Низкий |
<!-- @end -->

## Почему здесь нет бэкенда

<!-- @generated:backendless -->
| Требовало бы сервера | Решение без сервера |
|---|---|
| Хранение проекта и истории изменений | Core Data для описания; FileManager для исходников, proxy, draft и экспортов в контейнере приложения |
| Получение роликов от друзей | AirDrop, сообщения и другие системные каналы работают вне приложения; владелец вручную импортирует полученные файлы из Photos или Files |
| Группировка и черновой монтаж | AVFoundation, ImageIO metadata, Vision и Core ML на устройстве; основная ценность доступна в авиарежиме |
| Запрос и отправка материалов | UIActivityViewController передаёт статичную карточку-инструкцию; приложение не знает получателей, доставку или ответы |
<!-- @end -->

## Правила IA

Карточка запроса не создаёт удалённую сущность. Photos и Files отдают только выбранные файлы. Denied всегда оставляет второй локальный источник или повтор через Settings. Processing и export не требуют сети.
